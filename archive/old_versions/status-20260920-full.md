## 2026-09-20 (ночь, 2) — zcode: автоверификация МСП «прогоном рубля» — гейт зелёный за секунды

- Решение владельца: после аппрува представителем заявка верифицируется АВТОМАТИЧЕСКИ —
  банк прогоняет 1 ₽ по счёту МСП (зачислил-вернул), ручного матчинга и оператора больше нет.
- Реализовано: мок `POST /av/v1/verification` (синхронный RETURNED + запись в консоли);
  ядро `AccountVerificationClient` (канал через PaymentChannelManager — смена мок↔банк
  не ломает) + `RunPartnerVerificationAction` (verifying → verified, ставит
  lovii_b2b.partners.verified_at = гейт I-5); аппрув представителем тянет прогон сам.
- Тесты: RepresentativeApprovalTest 8/8 (verified-гейт + отказ банка → failed),
  Business 16 passed. Ветка feat/tbank-mock-stand 63b9d9d. Смержить на стейджинг вместе
  с предыдущими коммитами ветки (подпись Token, консоль каналов, stand-bootstrap).
- Сценарий владельца теперь: заявка → представитель апрувит → секунды → «Точка активна».

## 2026-09-20 (ночь) — zcode: чистый сброс №2 + stand-bootstrap.sh — стартовый набор стенда

- Владелец попросил: список данных для запуска + воспроизводимый стартовый набор + сброс
  стенда и проход ролевых сценариев ВРУЧНУЮ через app-кабинеты (никаких вставок в обход —
  правило подтверждено повторно). Демо-данные прошлой сессии (точка, заказы, лишние юзеры)
  смыты сбросом — ручной чистки не потребовалось.
- **`lovii-core/scripts/stand-bootstrap.sh`** (идемпотентный, ветка feat/tbank-mock-stand
  0a1f315): схемы b2b/admin → core migrate → города (СПб/Москва/Всеволожск) → Основатель
  (OTP-bypass) → амбассадор AA + промокод AA2222 → лояльти «Кэшбэк 5%» → капитал +1000 ₽.
  Эталонный чеклист старта — сессия 046 в lovii-core/docs/sessions.
- Стенд сброшен и верифицирован: cities 3, users 1, merchants 0, AA2222 у user 1,
  кошелёк 1000 ₽, b2b/admin подняты (миграции прошли entrypoint'ом), мок пересоздан с
  чистым состоянием. ⚠️ OPERATOR_PARTNER_ID в .env закомментирован — вернуть id АКСИОМЫ
  после её создания через «ЛОВИ Бизнес». Подписка PASS — владелец активирует кнопкой в UI.
- Владелец идёт по сценариям сам: покупатель → ЛОВИ Бизнес (заявка/верификация) →
  /cabinet/msp (наполнение) → покупка → возврат кнопками консоли мока. Операторские шаги
  без UI (подтверждение заявки) — делаю по его команде с показом.

## 2026-09-19 (ночь, 4) — zcode: G6 закрыта — ролевые кабинеты по наличию промокода; тест владельца тест-1 разблокирован

- Владелец начал сквозной тест (тест-1 = +79999000001): регистрация по
  AA2222 прошла (promo_code привязан), но кабинеты не показались — это и был
  гэп G6 (флаги ролей не считали привязку).
- **Core 4c49684 → staging 3afb0dc (CI 🟢)**: representative/msp флаги = true
  при привязанном промокоде; /msp/overview отдаёт честную пустую структуру
  (role owner, пустые partners/merchants/orders) вместо 404; 404 — только
  профилям без промокода; амбассадор — по назначению (403 остаётся).
- Тесты: 6 новых сценариев G6, свип 27/27; pint/phpstan 🟢; падения pest —
  все в базлайне средовых (чужой WIP в общем чекауте не тронут, мерж через
  worktree). Тест-1 получил старт-баланс 1000 ₽ (adjustment, bootstrap).
- Ожидание: перезагрузить /profile у тест-1 — плитки «Представитель» и
  «ЛОВИ Бизнес» появятся; кабинет амба НЕ появится (назначение).

## 2026-09-19 (ночь, 5) — zcode: консоль платёжного канала + мок только по тестовым картам (be70997)

- **Консоль канала** `GET/POST /api/v1/payments/console` (HTML, без программирования):
  локальный мок / стейджинговый мок / тестовый контур банка / боевой. Выбор хранится в
  payment_settings (миграция), читается в биндинге TBankClient — смена действует сразу.
  Защита сред (PaymentChannelManager): local = ЖЁСТКО local-mock (решение владельца: до
  релиза локальный стенд не ходит в реальный банк); staging = мок/тестовый контур (боевой
  запрещён); production = только боевой. На локальном стенде консоль без ключа; на staging
  — `?key=INTERNAL_ADMIN_SECRET`. Тесты PaymentChannelManagerTest + TBankSignatureTest 🟢.
- **Мок ужесточён** (решение владельца): платежи ТОЛЬКО по тестовым картам (неизвестная
  карта → отказ 1236), состояние персистентно (`tbank-mock/data/state.json`, переживает
  рестарты, счётчик PaymentId не переиспользуется), в консоли штамп «Обновлено» + плашка
  про тестовые карты.
- **Ещё один улов мока**: дубликат PaymentId от банка ронял ядро 500-й — теперь
  CreateOrderPaymentAction помечает попытку Failed и начинает новую сессию Init.
- Всё в ветке `feat/tbank-mock-stand` (be70997, поверх acb0db8) — **мержить в staging
  вместе с фиксом подписи**. Локальный контейнер мока пересоздан вручную (docker run,
  т.к. сервис отсутствует в compose чекаута SZ-061): alias tbank-mock в lovii-core_default,
  порт 127.0.0.1:3100, MOCK_STATE_FILE=/srv/data/state.json. В чекауте SZ-061 по-прежнему
  лежит незакоммиченный набор (см. ночь,4) — не потерять.

## 2026-09-19 (ночь, 4) — zcode: tbank-mock ЖИВОЙ локально — поймал и починил баг подписи Token (важно!)

- 🔴→🟢 **БАГ В ЯДРЕ**: `TBankSignature` дописывала Password в КОНЕЦ конкатенации, а банк
  (дока oplata §4 + Java-пример) сортирует Password ВМЕСТЕ с параметрами (Pan<Password<
  PaymentId). Реальный банк отверг бы первый же Init/нотификацию; не всплывало, т.к. ядро
  с банком ещё не говорило, тесты Http::fake подпись не проверяли. ФИКС в ветке
  `feat/tbank-mock-stand` (acb0db8, тесты подписи переведены на канон) — СМЕРЖАТЬ В STAGING.
- Локальный стенд ПЕРЕВЕДЁН на мок: PAYMENTS_FAKE=false, TBANK_API_URL=http://tbank-mock:3100/v2,
  NotificationURL=http://app:8080/... (бэкап .env.bak-mockdemo). Сквозняк проверен живьём:
  Init → форма (PAN-пресеты, challenge OTP 1qwezxc) → холд → «банк подтвердил холд» →
  CONFIRMED → кэшбэк 5% → пул 40/40/20 (ledger копейка в копейку), 2 заказа, ретраи
  нотификаций видны в консоли. ⚠️ Локально e2e scenario-03 (fake-эндпоинт) теперь сломан —
  сценарий нужно перевести на мок или временно вернуть PAYMENTS_FAKE=true.
- В чекауте lovii-core (ветка SZ-061 Ф2) остались НЕЗАКОММИЧЕННЫМИ: TBankSignature.php +
  TBankSignatureTest.php (фикс, = acb0db8), tbank-mock/ (untracked, для docker-монтажа),
  отладочный лог в server.js. Не потерять при switch/мерже; после слияния ветки — Alignment.
- Локальные демо-данные созданы tinker'ом (это локальный стенд, не staging): merchant 1
  «Демо-пекарня «Мок»», branch 2, offers 3/4 (Кольцо 120₽, Пирожок 55₽); мин-заказ 600₽ = дефолт.

## 2026-09-19 (ночь, 3) — zcode: кабинет амба показывает AA2222, а не префикс

- Сообщение владельца: «у амба вижу AA, а не AA2222». Дашборд core уже отдаёт
  `code` (Ф1), но AmbassadorReps рисовал старый `prefix`. Экран «Структура» и
  кнопка копирования переведены на амбассадорский код (фолбэк — префикс),
  тексты «префикс» → «код». Проверено вживую: код AA2222 + «Скопировать код».
- staging a04266f, yarn test 🟢 (фикстура теста 1:1 с новым ответом core).

## 2026-09-19 (ночь, 2) — zcode: SZ-061 Ф1 на staging — амб-код как сущность + форма промокода; живой путь сошёлся

- **Core 76e2ab1**: миграция `ambassadors.code` (AA2222 переехал с реп-кода
  Основателя в амб-сущность, реп-строка деактивирована — «само-выданного»
  промокода больше нет); привязка (T-010 + цена подписки) по двум таблицам;
  POST /profile/promo (422/409 честно); AssignAmbassador выдаёт амб-код;
  дашборд амба отдаёт code. Тесты 8/8 + свип подписки 21/21, preflight 🟢.
- **App b4c5472**: форма «введите промокод амбассадора» в блоке подписки для
  профилей без промокода; yarn test 🟢.
- **Живой путь (локальный стенд)**: сброс → форма «aa2222» → привязка → CTA →
  активация **199 ₽ promo** (гэп цены по амб-коду найден и закрыт) → активна
  до 19.10 → личный код **AAN4PR** выдан и виден в карточке. Канон сошёлся.
- Bootstrap стенда: АКСИОМА (partner 1, verified, ИНН 7842223709) + city СПб
  в справочник; OPERATOR_PARTNER_ID=1.
- **Коллизия общего чекаута lovii-core** с параллельным агентом (tbank-mock):
  его checkout+commit поверх моей ветки, мой amend втянул мой файл в его
  коммит. Разрулено отдельным worktree; урок — личный worktree сразу.
- Карточка SZ-061 → «На приёмке» (docs 6910ed9); session core 045, app 124.
  Дальше Ф2 (карточки кодов/QR) и Ф3 (назначение амба, счёт платформы, G6).

## 2026-09-19 (вечер, 3) — zcode: tbank-mock волна 1 — мок-стенд Т-Банка смержен в staging

- Реализован `lovii-core/tbank-mock/` (zero-dep Node, RES-007 волна 1): /v2/{Init,Confirm,
  Cancel,GetState,CheckOrder}, платёжная форма с PAN-сценариями (challenge OTP `1qwezxc`),
  подписанные нотификации с ретраями 60с×24, книга сделок (SpAccumulationId), админка /admin.
  Самотест 17 проверок 🟢. Сервис в dev/prod compose (dev :3100, prod 127.0.0.1:3100);
  образ node:22-alpine на сервере проверен заранее. Core 8346d0a → staging, CI+деплой.
- Ядро НЕ переключено на мок (осталось PAYMENTS_FAKE=true): e2e scenario-03 ездит на
  fake-эндпоинте. Переключение = PAYMENTS_FAKE=false + TBANK_API_URL=http://tbank-mock:3100/v2,
  после апдейта e2e-сценария оплаты — отдельным шагом с владельцем.
- ⚠️ Инцидент (обработан): общий рабочий каталог lovii-core переключился на ветку SZ-061
  параллельной сессии — мой коммит лег поверх неё. Восстановлено: cherry-pick в worktree
  /tmp/lvcore-mock → ветка feat/tbank-mock-stand 8346d0a; ветка SZ-061 возвращена на 58a57ec,
  их staged-файл не тронут. Урок: перед коммитом в общем репо проверять `git branch --show-current`;
  для своей работы — сразу worktree (прецедент lvapp-nocats).
- Дельта ядра вне стенда остаётся открытой: SpAccumulationId → payments.deal_id из
  AUTHORIZED-нотификации; момент Confirm — решение владельца.

## 2026-09-19 (ночь, 2) — zcode: регресс SZ-059 — иконки «квадратиками» (10 масок восстановлены, staging 🟢); гейт-канон → G6; адреса на localhost — лимит Яндекс-ключа

- **Иконки**: SZ-059 срезал словарь до 28 масок при 38 живых компонентах
  icons/** — 10 классов (червоны, круги статусов, map_pin, search_categories,
  slider_horizont, tariff_business) остались без svg/правил → сплошные
  квадраты. Восстановлены из истории 438bfbd^; проверено IAB на /profile
  (computed mask = data-URI). Прекэш 964→992 КБ. Хотфикс f5d08fd на staging,
  CI 🟢 — регресс был и там. Владелец ни при чём. Урок: при удалении иконок
  grep по src/components/icons/** обязателен; lovii-demo масок не содержит
  (там инлайн-SVG).
- **Гейт кабинетов (сообщение владельца)**: канон «наличие промокода = доступ
  к реп- и МСП-кабинетам» в коде не выполняется (MspAccess 404
  partner_not_found без членства; EnsureRepresentative требует собственный
  код) → гэп G6 в SZ-061 (Ф1, core+app).
- **«Не могу добавить адрес» на локальном стенде**: известное ограничение —
  VITE_YANDEX_MAP_API_KEY привязан к Referer (lovii.ru/стенд), JS-карта на
  localhost не инициализируется (as-is/08). Варианты: адреса тестировать на
  staging; либо добавить localhost:5173 в Referer-разрешения ключа (консоль
  Яндекса — действие владельца).
- Session-док app 123 (staging 659d2b5); SZ-061 пуш 07cec5c.

## 2026-09-19 (ночь) — zcode: локальный стенд сброшен до стартовых данных; правило стража смягчено; G1 подтверждена

- **Страж**: не гоняется после каждого мержа (решение владельца) — только после
  крупных обновлений, приоритетно на локальном стенде
  (STRESS_BASE=http://localhost, сравнение относительно); staging — вручную.
- **Сброс локальной БД** (core down -v, все тома): миграции core/b2b(15)/admin(31);
  на чистом томе сначала создать схемы lovii_b2b/lovii_admin, потом миграции
  (урок). b2b → :8002 (конфликт с admin :8001, APP_PORT в .env).
- **Стартовые данные**: Основатель = user **1** (+79119287478), амб AA ✓,
  реп-код AA2222 ✓ (ручной UPDATE suffix — issue() не умеет точный), лоялти
  «Кэшбэк 5%», старт-баланс 1000 ₽ (adjustment), подписка PASS активна через
  API (599 ₽ стандарт, до 19.10, без самомо-инвайта). Личный код AA-XXXX —
  выдаст SZ-061.
- **.env core**: OPERATOR_PARTNER_ID закомментирован — вернуть после создания
  АКСИОМЫ владельцем через экраны. Юрлица/точки/товары — только через экраны.
- Session-док core 044. Стенд готов как рабочее место SZ-061 Ф1.

## 2026-09-19 (вечер, 2) — zcode: SZ-061 — карточка реворка рефералки создана (ответы владельца по всем развилкам)

- Ответы: экономика без изменений; вставка промокода при подписке (блок счёта,
  для непривязанных) вместо смены родителя; ранги — только статус/мероприятия;
  кабинеты — владелец сам; P2P — делать (SZ-062: Основатель → амбы → репы,
  стартовый баланс на профиле Основателя); термины — отложены (п.6 — владелец
  уточнил, что не понял вопрос: пояснить в отчёте конкретные места).
- G1 решена по рекомендации: `ambassadors.code` (6 симв.) как сущность; AA2222
  — амб-код Основателя; личный реп-код AA-XXXX ему же; привязка по обеим таблицам.
- Карточка `canon/TASKS/SZ-061-referral-canon-rework.md` (task_guard 🟢, пуш
  4596626): Ф1 core (миграция/привязка/активация с промокодом/личный код
  Основателю) → Ф2 app (шит промокода, QR амба, две карточки кодов) → Ф3
  (назначение амба UI/API + счёт платформы + данные Grand/269).
- Граница: доработку кабинетов владелец ведёт сам — карточка не трогает.

## 2026-09-19 (вечер) — zcode: Б-8 рефералка — диктовка владельца записана, аудит готовности продукта сделан

- Канон: без глубины (реп под амбом); амб-код 6 симв. (AA2222 = Основатель);
  амба назначает основатель; при покупке PASS — личный код нового репа; у
  Основателя два кода (амб + личный), три МСП с его ИНН, счёт платформы с
  операциями и распределениями.
- Аудит кода: ГОТОВО — генерация личного кода при активации/продлении PASS
  (префикс от привязки, SubscriptionBillingService:226–247), QR репа (?ref=КОД),
  цепочка регистрация→PASS→код→кабинет, Founder = владелец AA2222
  (ListBalanceAccountsQuery:131–141). ГЭПЫ: G1 два кода Основателя (skip-правило
  не даст второй; развилка «ambassadors.code как сущность» — рекомендую), G2
  назначение амба только artisan, G3 счёт платформы (owner_type=platform нет,
  Founder-экран распределений ledger), G4 QR амба, G5 профиль (Grand 268 не
  верифицирован, дубль Статус-92 269; АКСИОМА без брендов).
- Профиль 51 на staging: подписка активна до 19.10 ✓, амб AA ✓, реп AA2222 ✓.
- 00-док обновлён (пуш 9dccf28); реализация — SZ-061 после решения G1.

## 2026-09-19 (день, 5) — zcode: экосистема GitHub по Т-Банку + публичная база знаний в lovii-ru

- Ресёрч GitHub (по запросу владельца): готового мока эквайринга НЕТ (mock-tbank-api —
  это T-Invest); найдены: OpenAPI «Массовые выплаты 1.46» (paygen, MIT), Laravel-реализация
  ровно нашего флоу prukon/admin.k (PartnerId=shopCode, FinalPayout, pollState — без
  лицензии, только читать), продукт safe-deal pomuc/OpenMoney (MIT), TS SDK
  jfkz/tinkoff-payment-sdk (createSpDeal/closeSpDeal). Обзор: `tbank/notes/06-ecosystem.md`.
- Публичная база знаний в хабе lovii-ru/lovii-docs (пуш 5f2491d): `tbank/docs/` — 4 PDF
  банка + `tbank/notes/` — 6 конспектов (оплата, нотификации+Token, выплаты+сделки+acqapi,
  тестовые карты, мок-стенд, экосистема) + `tbank/openapi/tbank-payouts-openapi.json`.
  Обогащать по ходу разработки; секретов и условий договора там нет.
- CI Docs governance был красный ещё до нашего пуша (STALE «3,5%» в as-is/gemini-…) —
  починен маркером `doc-canon: historical` (e248597), хаб снова 🟢.
- Развилка для владельца: база живёт в хабе; если захочется ОТДЕЛЬНОЕ публичное репо —
  у lovii-ru нет API-токена, создать пустое репо в вебе сможет только владелец,
  переложить — минуты.

## 2026-09-19 (день, 4) — zcode: Т-Банк Мультисплит — ресёрч методов + дизайн мок-стенда (RES-007)

- Задача владельца: «свой тестовый стенд вместо тестового стенда банка». Прочиты все 4
  PDF Мультисплита (оплата/выплаты/продукт/регистрация точек) + TbankLib (его
  vyplaty-multisplit.txt = дубль продукт-доки, не выплаты!). Артефакт:
  `lovii_docs/artifacts/RES-007-tbank-multisplit-mock-stand.md`.
- Выбор методов: волна 1 — Init/Confirm/Cancel/GetState/CheckOrder + http-нотификации
  (SpAccumulationId участвует в Token!); волна 2 — e2c-выплаты (PartnerId=shopCode ЮЛ 0,5%,
  СБП/карта ФЛ 1%); волна 3 — рекуррент, СБП-QR, sm-register.
- Ключевые находки: в схеме с формой банка DealId приходит ТОЛЬКО в нотификации
  AUTHORIZED (SpAccumulationId) → дельта core: писать в payments.deal_id;
  TBankClient::confirm реализован, но никем не вызывается; тестовый контур банка —
  только после подписания договора + IP-WL + запросы С БОЕВОГО терминала → мок-стенд
  (standalone в lovii-core/tbank-mock, env-переключение с банком, без правок кода core).
- Ждёт владельца: «го» на мок-стенд (волна 1), решение по моменту Confirm, вопросы банку
  (чарджбэк-нотификации, срок жизни сделки, авто-подтверждение холда 4/7 дней).

## 2026-09-19 (день, 3) — zcode: сводка «что дальше/что срочно» + Б-8 реворк рефералки ждёт мнение владельца

- Прогоны стресса — по факту крупных обновлений (правило уточнено в AGENTS.md,
  крона нет — решение владельца).
- Очередь 00-дока перестроена: Б-6/Б-7 (юрист+54-ФЗ) стартовать немедленно
  (длинный цикл); Б-3/Б-4 — следующая агентская задача; Б-2 прод-деплой — после
  них и после решения по Б-8; П-5/П-6 (P2P, промо-матрица/RBAC) включаются в
  реворк рефералки, не отдельно.
- **Б-8 (новый блокер, 👤)**: реферальная система «не соответствует мнению
  владельца» — в 00-доке зафиксированы факты as-is (плоская префикс-иерархия,
  40/40/20, EC-11, AA2222-фолбэк, личный код за подписку) и 7 развилок
  (структура/экономика/привязка/ранги/кабинеты/термины/P2P). Спека — после
  ответов владельца по пунктам; если реворк в пилоте — встаёт ПЕРЕД Б-2.

## 2026-09-19 (день, 2) — zcode: перф-страж — чтобы доработки не испортили показатели (решение владельца)

- **baseline.json + smoke.sh** (`lovii-app/scripts/stress/`, staging 3387810):
  проверка одной командой ~1,5 мин (VU=10/25 по 30 с, односторонние пороги).
  Проверен вживую 🟢 (225 rps / p50 105 мс на VU=25 — даже лучше базлайна).
- **Правило агентов** — корневой `AGENTS.md` §«Страж производительности»:
  горячие пути (каталог/корзина/чекаут/кошелёк/поиск/лояльность/миграции
  тяжёлых таблиц) → smoke перед мержем; в request path запрещены N+1,
  sync-HTTP, безлимитные выборки.
- **Страж в core** (staging, CI): `db.slow_requests` в laravel.log при
  суммарных >500 мс SQL на цикл — пассивное обнаружение регрессов.
  Preflight 🟢 (pint/rector/phpstan/pest, 0 новых падений).
- Память и 00-док (операционка) обновлены (docs bcb2845).

## 2026-09-19 (день) — zcode: Б-1 закрыт — «404-стена» SZ-060 была артефактом жгута, ёмкость перемерена и поднята

- **Диагноз по access-логу caddy**: 14 204 из 14 363 запросов стресс-жгута v1 =
  404 на путях БЕЗ префикса `/api/v1` (Laravel-404 по 14 мс) + TLS-handshake на
  каждый запрос. Сервер не отдавал ни 429, ни 502 — «скрытого лимитера» нет.
- **Жгут v2** (`lovii-app/scripts/stress/`, влит в staging 35f35c4, yarn 🟢):
  правильные пути, keep-alive на поток, оффер под минимум 600 ₽. Перемер до
  поднятия: плато **175 rps**, деградация очередью, 0 ошибок до 200 VU.
- **Ёмкость поднята** (core feat/sz060-fpm-capacity → staging 346ea21, CI 🟢):
  fpm static 10 → dynamic 24, app-контейнер 768M → 2G. Пост-деплой: 50 VU = 230
  rps / p50 209 мс, 100 VU = 235 rps / p50 419 мс, 0 ошибок. Потолок ~235 rps,
  следующий узел — Postgres (~5.5 ядер в пике) — P2 при росте трафика.
- Write-путь под нагрузкой: первые прогоны 100% 422 `min_order_amount_not_met`
  — **гейт №1 минимума заказа работает**; после добора количества — 3,5 цепочки
  «заказ+оплата»/с без ошибок.
- Session-доки: core 043, app 122. 00-док: Б-1 ✅ (пуш docs 0730720).
- Грабли: токены жгута умерли (401) — ремнт через OTP dev-bypass; `+` в query
  без `--data-urlencode` = PHP видит пробел; app CI деплой 1×SSH-флап (rerun ✅);
  staging app мержить ТОЛЬКО через worktree /private/tmp/lvapp-nocats.

## 2026-09-19 — zcode: актуализация as-is (дельта 18–19.09) + сводный док «чего не хватает для запуска»

- **as-is 01–13 актуализированы**: подписка LOVII PASS (SZ-052/053/056/057), выплаты
  (SZ-054), ПЭП (SZ-055), гейт EC-11 включён, VIP-оборот 30 дней (SZ-058), минимум
  заказа (№1), T-010, чипы типов убраны, кабинет МСП v2 (свитчер/«+ Бренд»/«+ Точка»),
  геокодинг адреса точки, инвайт админом, страница платминимума в админке; балансы
  staging пересняты SQL 19.09 (АТМОСФЕРА 60 597 ₽, АКСИОМА 2 582,76 ₽, личный 3 369,74 ₽).
- **Новый 00-zapusk-chego-ne-hvataet.md** — живой док запуска: закрытое с 17.09,
  7 блокеров (Б-1 404-стена SZ-060 → Б-2 прод-деплой → Б-3/Б-4 верификация UI+авто-матчинг →
  Б-5 банк-контур → Б-6 54-ФЗ → Б-7 юрист), P2-таблица (П-1…П-10), очередь с ролями
  «агент/владелец», архив решений §C2. **Это теперь главный документ папки** — вести
  после каждого мержа/приёмки.
- README: фокус «запуск», статусы модулей «обновлён 19.09», gemini-резюме решений
  добавлены в корпус (были только в негитовой папке lovii-docs).
- Мерж с зеркалом lovii-ru (параллельные карточки SZ-056..058 — контент совпал, as-is
  взят из свежей линии); статусы карточек SZ-056/057/058 нормализованы под task_guard
  («На приёмке», в staging 19.09). Гейты: task_guard 🟢, doc-canon-check --strict
  STALE=0 🟢, fact-guard 🟢. Пуш: origin (bestdeejay-design) + mirror (lovii-ru) —
  оба до 67a3214, FF.
- Дельта-метод: базовый срез 17.09 сохранён, дельты помечены «обновлено 19.09».

## 2026-09-19 — zcode: lovii-site (превью lovii.ru) — аудит перед публикацией + правки владельца

- Репо `/Users/best/Projects/lovii-site`, Pages деплоится из `main` (без workflow).
- Аудит: все 7 страниц в Chrome (десктоп+мобайл 390, обе темы) — консоль чистая,
  битых ресурсов нет, якоря/калькуляторы/формы/бургер OK; найдены 2 бага.
- **fix 404**: динамический `<base>` первым в head — стили на любой глубине адресов
  (превью `/lovii-site/`, День X lovii.ru — корень, правок не требует). Спекулятивные
  404 сканера Chromium в консоли — норма, финальная загрузка 200.
- **feat roadmap**: секция «Подключиться» (#connect) с формой по стандарту страниц —
  закрыт мёртвый якорь шапки; форма/валидация/черновик проверены.
- **Решения владельца 19.09**: термин White Paper убран полностью (сайт сам = замена WP);
  МЛМ/каскады/пирамиды в текстах не упоминать (карточка и FAQ переписаны);
  живое демо на главной → `lovii.mobiap.com`; оферты НАВСЕГДА на
  `axiiom-ru.github.io/lovii` (витрина прозрачности); `hello@lovii.ru` жив.
- Коммиты 0d105f2…e3446a0 в `main`, Pages built; владелец принимает на живом.
- Грабли: Playwright-клики по якорям в IAB подвисают — JS `.click()`; fullpage-скрин
  IAB врёт на reveal-секциях; `.gitignore` восстановлен (потерян при force-пуше).

## 2026-09-18 (ночь, итерация 4) — zcode: SZ-056 кнопка подписки LOVII PASS — оплата только с внутреннего счёта

- **Решение владельца**: подписка на старте оплачивается ТОЛЬКО с внутреннего
  счёта (Business → PAY) — рекуррент и привязка карты не нужны; из юрдокументов
  остаются правка Публичной оферты (юрист) + чек 54-ФЗ.
- **core 23f7886** (feat/sz056-sub-activation, merge 8e8501a, CI в работе):
  `POST /subscription/activate` — подключение/возобновление, промо 199 ₽ по
  коду Амбассадора иначе 599 ₽, 409/422-честные ошибки; **FIX латентного бага
  крона SZ-053** — ключ оператора `payments.split.operator_partner_id` (старый
  путь давал бы 500 и гасил подписки в grace); session 042. В локальный .env
  добавлен OPERATOR_PARTNER_ID=265.
- **app 1fc7a27** (feat/sz056-pass-subscription-ui, merge 9827a06, CI ✓):
  блок «Подписка LOVII PASS» на «Счёте и операциях» — CTA «Подключить/
  Возобновить», честные состояния (активна до {дата} / grace / без средств),
  бейдж уровня и скин личной карты от реального статуса; session 120.
- Локальная отладка по просьбе владельца: OrbStack перезапущен, стенд поднят,
  API-сценарий (422→201→409) и оба UI-состояния проверены в браузере на :8081.
- Базлайн префлайта: +DistributeOrderPoolTest (средовое, падает и на чистом
  staging; CI EC-11 зелёный).
- Карточка SZ-056: lovii_docs/canon/TASKS/. Дальше: живая приёмка владельца,
  юрист (оферта), статус-блок профиля на apiGetSubscription, отмена подписки.

## 2026-09-18 (вечер, итерация 3) — zcode: гейт №4 EC-11 включён на staging + BACKLOG_REVIEW опубликован в корпусе

- **Гейт подписки представителя** (core da9397d, CI 35366145577 ✓ + деплой):
  `DistributeOrderPoolAction` — при включённом флаге реп без льготной подписки
  (актив/grace) теряет долю в пользу Компании; амбассадор не задет. Тесты
  20/20 пула. **Флаг включён на staging** (.env + recreate, verified true,
  контейнеры healthy) — поведение стенда изменилось: 40% пула только
  подписанным. Card-дубль T-011 (Super Z) закрыт SZ-053 — развилки в session
  041; отчёт Super Z — через владельца.
- **BACKLOG_REVIEW.md опубликован в корпусе** bestdeejay-design/lovii_docs →
  `canon/BACKLOG_REVIEW.md` (fc7947e) — пункт полосы A «от владельца: пуш»
  исполнен по «го».
- Session-док: `lovii-core/docs/sessions/041-ec11-subscription-gate.md`.

## 2026-09-18 (вечер, итерация 2) — zcode: T-010 промо-суффиксы + SZ-051 чистка медиа + preflight-процесс

- **T-010** (app 938fe42 CI ✓, core 6f9a0e0 CI ✓): любой суффикс `?ключ=` —
  носитель промокода (резервные token/cart_id/utm_* не сканируются); форма
  «Введите промокод» сохраняет ввод; ядро валидирует `referral_code` по
  `representative_promo_codes` при подтверждении номера → `users.promo_code`
  (пишется только на пустое — идемпотентно; не найден/не код — тихий игнор).
- **SZ-051**: `media:check-orphaned [--clear]` (локальный диск + HEAD для
  абсолютных URL, kuper — report-only). Отчёт на staging: 514 ссылок, битых 17,
  очищаемых 4 — 267 «Dev Restaurant» и 279 «пицца от сс» (по logo+mini, 404);
  280 «Пышки» ok. По вердикту «обнуляем» — очищено 4, в БД проверено NULL.
- **Preflight-процесс** (7bcf6b8, ответ на 2 красных CI за день): перед
  каждым мержем — `bash scripts/preflight.sh`: pint/rector/phpstan + ПОЛНЫЙ
  pest с диффом против базлайна средовых падений
  (`scripts/preflight-known-failures.txt`, 7 классов). Проверен: блокирует
  новые красные. Правила: миграция колонок → канарейки тем же коммитом;
  статических `Carbon::x()` в новом коде нет (CI-rector Date-фасад).
  Session-док: `lovii-core/docs/sessions/040-t010-sz051-preflight.md`.

## 2026-09-18 (вечер) — zcode: №6 SZ-055 ПЭП-реестр НПД — последний P1 бэклога закрыт, всё на staging

- **core `staging` 812c5f2** (feat/sz055-pep, CI **35352655831 success** + деплой;
  первый прогон был красный — см. грабли): таблица `pep_reports` (payout_id
  unique, payload jsonb, content_hash, otp_hash + expiry, статус pending|signed);
  новый домен `Domain/Compliance`: контракт + генератор кода (6 цифр),
  `PepReportService` (issue/confirm/resend + verifyIntegrity); хук в
  `PayoutService::requestPull` — после коммита выплаты создаётся документ и
  уходит пуш «Код: N …»; сбой пуша не роняет (resend). API: GET
  `/pep/reports/pending` (401 на стенде ✓), POST confirm/resend. Привязка
  контракта — AppServiceProvider.
- **Canonical-hash**: jsonb пересортировывает ключи → хеш документа считается
  по канонической форме (рекурсивная сортировка), issue и verify — одна
  функция. Гейты: 9 сценариев ПЭП ✓, свип 48/48, pint/rector/phpstan ✓.
- **Грабли**: (1) CI-rector обзавёлся `CarbonToDateFacadeRector` — статические
  вызовы Carbon переписывает на фасад Date (immutable!) → TypeError; решение —
  не использовать статические Carbon вообще, тип `CarbonInterface` +
  `now()->addMinutes()`. (2) ПЭП-код был вставлен после `return DB::transaction()`
  — мёртвый код, поймано тестом. (3) final-классы не мокаются → контракт +
  bind. Session-док: `lovii-core/docs/sessions/039-sz055-pep-reports.md`.
  Карточка SZ-055.
- **Очередь BACKLOG_REVIEW §B исчерпана**: №1–6 все P1 на staging (CI ✓).
  Дальше P2: sub-price-lock (199), promo-fallback-matrix, promo-change-rbac,
  p2p-transfers; P3 canicule; + T-009 (P2, Super Z: пуши accepted/ready +
  prune-dead в планировщик). Для юриста/владельца: текст ПЭП-документа —
  каркас, нужно утверждение. Открытые развилки T-009 (a–d) — на владельце.

## 2026-09-18 (день, итерация 2) — zcode: №4–5 SZ-054 выплаты — pull НПД + суточный push-реестр на staging

- **core `staging` 3d58f4c** (feat/sz054-payouts, merge 40150ba, CI
  **35348271039 success** + деплой): таблица `payouts` (push|pull,
  идемпотентность push за день по unique(account,type,for_date), суммы
  amount/net/commission, bearer, статус pending→sent); `users.legal_status`
  (nullable — презумпция НПД §A6); `accounts.payouts_blocked` (минус-баланс
  блокирует). `PayoutService`: **pull** — гейты подписка `grantsBenefits()`
  (в grace пускает), legal_status≠ip/ooo, blocked, минимум 100 ₽; ≥3000 ₽ →
  1% платит ОПЕРАТОР (получателю 100%, проводки комиссии нет — при
  исполнении банком), <3000 ₽ → 30 ₽ удержание (platform +30 ₽ сразу);
  **push** `payouts:push-daily` 09:00 МСК — реестр по partner-счетам
  (amount=баланс, 0,5% bearer=operator), минус → blocked-флаг, нет
  подписки/моста → пропуск со счётчиком. F-049 закрыт (Payout использован).
  API `POST /payout/request` (401 на стенде ✓, честные коды ошибок).
- Конфиг `config/payouts.php` (примеры владельца §C2 #1 в комментариях;
  канон §1.6 «min 100 ₽» устарел — владелец правит). Реестр до тестового
  контура банка уходит в интернет-банк руками (статус pending).
- Гейты: 17 сценариев выплат ✓, свип Billing+Subscription 63/63, pint/
  rector/phpstan ✓. CI-канарейка `UserTest > to array` (17→18 колонок)
  поймана и обновлена (71bb7fe). Session-док:
  `lovii-core/docs/sessions/038-sz054-payouts.md`. Карточка SZ-054.
- **Дальше — №6 ПЭП Push-OTP** (реестр подписанных отчётов при pull-выплате),
  потом P2: price-lock, промо-матрица, RBAC, P2P; P3 каникулы.

## 2026-09-18 (день) — zcode: №3 SZ-053 sub-billing — каскад списаний и retry-cron подписки на staging

- **core `staging` 35557ad** (feat/sz053-sub-billing, CI **35337745044 success** +
  деплой с миграциями): журнал попыток `subscription_charges` + каркас
  `external_payment_methods`; `LedgerEntryType::SubscriptionPayment` (дебет
  источника + кредит оператора 100% Компания); `SubscriptionBillingService`:
  каскад Business (счёта юрлиц по мосту owner_user_id) → PAY (личный) →
  внешняя карта (каркас: попытка = failed `external_unavailable`, реальное
  снятие после тестового контура банка); cron `subscriptions:charge`
  03/09/15/21 МСК (= 0,6,12,18 UTC, `schedule:list` ✓); grace 48 ч сохраняет
  льготы, аннулирование сбрасывает акционную цену (199→599, promo_code=null);
  идемпотентность: row-lock + успех за период один + уникальность ledger.
- Гейты: 24/24 подписочных ✓ (9 сценариев биллинга), свип 156/157 (1 = средовый
  геокод), pint/rector/phpstan ✓. Смоук: таблицы на staging ✓ (MCP read-only).
- Грабли: `TestCase::$connectionsToTransact = [pgsql_core, pgsql_b2b]` — B2b-модели
  и `B2bSchema::ensure()` живут в транзакции pgsql_b2b, сырой SQL по `lovii_b2b.*`
  из сервиса должен идти через `DB::connection('pgsql_b2b')`; `partners.owner_user_id`
  — мост для резолва Business-счетов (добавлен в MspStarterFixture); даты моделей
  immutable → `new Carbon($d)`, НЕ `Date::instance()`. Session-док:
  `lovii-core/docs/sessions/037-sz053-subscription-billing.md`. Карточка SZ-053.
- **Дальше — №4–5 выплаты** (push ИП/ООО + pull НПД, гейт
  `grantsBenefits()`), банк-API моками до тестового контура.

## 2026-09-18 (утро) — zcode: №2 SZ-052 sub-domain — домен подписки Представителя на staging

- **core `staging` b21ac96** (feat/sz052-sub-domain, CI **35316927963 success** +
  деплой с миграциями): таблицы `subscriptions` (одна на профиль, снимок цены
  59900/19900, календарный период, окно grace) + `subscription_status_changes`
  (append-only история); enum `SubscriptionStatus` (active/grace_period/
  suspended/cancelled, карта переходов, `grantsBenefits()`: в grace льготы
  сохраняются — §A2); машина `SubscriptionStatusMachine` (start/transition,
  окно 48 ч ставит сама, дубль → 409); конфиг `config/subscription.php`;
  read-API `GET /api/v1/subscription` (data:null — честная пустота). Связь
  `User::subscription()`.
- Гейты: новые тесты 15/15 ✓, свип Unit+Roles+Card 454/455 (1 = средовый
  геокод-тест, session 035), pint/rector/phpstan ✓. Смоук: api-staging
  `/subscription` → 401 без токена (роут жив).
- Грабли: регистр namespace (`Domain\subscription`) macOS прощает, CI на
  Linux — нет (ловит rector); Carbon 3 diff знаковый; `artisan test` свипы
  по памяти → `php -d memory_limit=2G vendor/bin/pest`. Session-док:
  `lovii-core/docs/sessions/036-sz052-subscription-domain.md`. Карточка:
  `canon/TASKS/SZ-052-sub-domain.md`.
- **Дальше — №3 sub-billing**: каскад Business→PAY→карта, cron retry 6 ч
  (09:00 МСК), авто-переходы active→grace→suspended, проводки ledger.

## 2026-09-18 (ночь) — zcode: №1 min-order СЛИТА в staging, стенд обновлён — очередь открыта для №2 sub-domain

- **core `staging` 9bf7992**: merge `feat/min-order-enforcement` + ремонт гейтов
  CI (pint/rector по ветке, phpstan 11 → 0: гвард `MspAccess::merchantOf`,
  сужение address_line, типы `$data`, no-op фильтр убран; хелпер
  `mspMobileStarterFixture` → `Tests\Support\MspStarterFixture` — CI грузит
  тест-файлы изолированно). CI **35301905120 success** + deploy.
- **app `staging` e147433**: merge `feat/min-order-ui` (полный `yarn test` 🟢
  перед пушем). CI **35303027709 success** + deploy.
- admin/b2b staging уже были на origin (ночь, параллельная сессия).
- **Смоук**: api-staging `/stores` отдаёт `min_order_amount_effective[_rubles]`
  — платформенный минимум 600/500 жив на стенде (витрина, корзина, чекаут,
  настройки точки, страница в супер-админке).
- Средовый тест: геокод-кейс MspEndpointsTest падает локально при наличии
  `YANDEX_GEOCODER_API_KEY` (тест без Http::fake) — в CI зелёный; кандидат
  на обёртку fake. Детали: `lovii-core/docs/sessions/035-staging-min-order-merge-gates.md`.
- **Дальше по BACKLOG_REVIEW §B: №2 `sub-domain`** (модель подписки) → №3
  sub-billing → №4–5 выплаты → №6 ПЭП. Открытых вопросов к владельцу нет.

## 2026-09-18 (вечер) — zcode: GitHub разблокирован (bestdeejay-design) → GitHub снова = внешняя копия всех репо

- Владелец сделал `gh auth login` (device-flow) как **bestdeejay-design** + scope
  `workflow`. SSH-ключи под этот аккаунт не понадобились (пуш по HTTPS через gh).
- **Пуши** (все fast-forward; единственный force — приватный doc-бэкап):
  - **lovii-admin**: `staging` d0e488f→6611e9e (+10: SZ-045 чарджбэк-UI, min-order этап 2) — уйдёт на стенд по зелёному CI.
  - **lovii-core**: `feat/sz-045-chargeback-mirror` 86845a8→f06ecfb (**+11 коммитов: SZ-038 фаза D, SZ-047 §3/§7, базовый МСП-слой, карты LOVII, l10n** — git cherry подтвердил: на GitHub их не было вовсе) + новые ветки `feat/msp-overview-partner-name`, `feat/min-order-enforcement`, `feat/referral-code-on-registration`. Локальный `staging` в core был ПОЗАДИ GitHub (0/24) → fast-forward к 7f5ed8b, слияние не требовалось.
  - **lovii-b2b**: merge origin/staging (OTP codeLength из канала) в локальный staging → `51e1b82` (инвайт админом + OTP-фиксы вместе).
  - **lovii-app**: `staging` e0e82fb→**bed3dab** — влит удалённый ДС-ремап кабинетов Super Z (e0e82fb от 13.09); **конфликты в 8 файлах ролей-кабинетов разрешены в пользу локальной линии сессий 096–116** (свежее, принято владельцем по живой приёмке); итоговое дерево merge-коммита бит-в-бит локальному staging tip (проверено, `git diff --cached` = 0) → CI гоняет уже протестированный код. Фича-ветки: `feat/geo-precise-address` +2, `feat/sz-038-roles-cabinets` +2, новые `feat/sz047-team-invite-canon` (3210abb) и `feat/min-order-ui` (WIP-бэкап параллельного агента).
  - **lovii-demo**: `design/roles-cabinets-preview` +78 коммитов запушены. **lovii-design**: локальный main БЫЛ ПОЗАДИ origin → fast-forward к 0e93f47.
- **Доки**: origin `bestdeejay-design/lovii_docs` догнал хаб — `main` = fe63f87 (= mirror lovii-ru); старая расходившаяся история сохранена в ветке `archive/pre-hub-main-20260918`.
- ⚠️ **NB для Super Z**: его ДС-ремап e0e82fb перекрыт в staging локальной линией — его идеи (.rc-*, сег-табы, bottom-sheet-апрувы) переносить ПОВЕРХ сессий 107–116, файлы не восстанавливать.
- Мержи в staging **SZ-047** (app+core) и **min-order** — следующий шаг, после живой приёмки владельцем (обе ветки уже на GitHub).

## 2026-09-18 — zcode: этап 2 «платформенный минимум заказа» — правка из супер-админки (lovii-admin)

- **Страница**: «Система → Платформенный минимум заказа»
  (`App\Filament\Pages\PlatformOrderSettings`) — первая и **единственная
  редактируемая** страница панели (остальное read-only, как и было).
  Правит дефолт (600 ₽) и пол (500 ₽) в строку core `platform_order_settings`
  через модель-зеркало и соединение `pgsql_core`; своей таблицы/миграции схемы
  у админки нет. Валидация: целые рубли, положительные, ≤ 100 000 ₽ и
  обязательно **пол ≤ дефолта** — пару, которую core отбрасывает целиком,
  сохранить нельзя.
- **Карточка точки**: рядом со «Минимальная сумма заказа» появился «Минимум на
  чекауте» — эффективное значение + объяснение (свой минимум / платформенный
  дефолт / значение ниже пола). Правило чтения повторяет core `MinOrderPolicy`,
  без кэша — правка настроек видна сразу.
- **Права**: право `View:PlatformOrderSettings` (миграция админки), ролям по
  умолчанию не выдано — порог платформы решает super_admin (через gate),
  остальным можно выдать в Shield → Роли. Сохранение пишет `activity('admin')`.
- Гейт: pest **510/510** 🟢 (внутри контейнера `lovii-admin-app-1`), pint 🟢,
  rector 🟢, phpstan 98 ошибок = базлайн чистого HEAD (в новых файлах 0).
  Панель поднята локально на http://localhost:8001/admin (супер-админ
  `zcode-verify@lovii.local` / `password`), страница и карточки точек проверены
  рендером на реальных данных core-БД. Session-док
  `lovii-admin/docs/sessions/005-platform-min-order-settings-page.md`.
- **Осторожно при сборке lovii-admin**: `docker compose build` дважды падал на
  базовом слое — `install-php-extensions … redis` → «No releases available for
  package pecl.php.net/redis»; помогла отдельная сборка базового слоя
  (`docker build --target base -f docker/Dockerfile .`), после неё compose-сборка
  проходит. Плюс для локального `artisan migrate` в core-БД нужна схема
  `lovii_admin` (`no schema has been selected to create in`), и файлы в репо
  остаются хостовыми при `WWWUSER=$(id -u)`.
- Этап 3 (app: минимум в чекауте для покупателя) — не начат. Коммит локальный,
  не пушен.

## 2026-09-17 (ночь) — zcode: SZ-050 Ф2 — «+ Бренд» / «+ Точка» из кабинета + правило ИНН в заявке

- **Ядро**: `POST /msp/merchants` (бренд под своим юрлицем, право
  merchant.create; active у верифицированного юрлица), `POST /msp/branches`
  (точка под брендом; адрес геокодирует ядерный Yandex-геокодер — точке нужны
  city_id и координаты). **Правило ИНН**: заявка с ИНН уже верифицированного
  своего юрлица (без промокода) → новый бренд в существующее юрлицо + заявка
  сразу Verified, без счёта 1 ₽ — дубли юрлиц («Пончики») исключены.
- **App**: шит форм роста (бренд: название + тип витрины + юрлицо с дефолтом
  = активное юрлицо свитчера; точка: название + адрес + город), CTA в пустых
  состояниях Обзора и «+ Точка» в шапке «Рабочей точки».
- **Живая проверка**: свитчер → АКСИОМА → «+ Бренд» («Кофейня АКСИОМЫ») →
  «+ Точка» («На Московском», геокод сервером) → рабочая точка «На витрине».
- Гейты: core 70/70 🟢, app 819/819 🟢. Session-док 118 (дополнен).
- Коммиты локальные, не пушены. Следующий шаг — Ф3: каталог с вложенными
  категориями (см. SZ-050).

## 2026-09-17 (вечер) — zcode: SZ-050 Ф1 — свитчер юрлиц «МСП = ИНН» + найден дубль юрлица в заявках

- **Спека кабинета v2 утверждена владельцем** («го»): юрлицо → бренды → точки,
  один кабинет, свитчер ИНН в шапке (паттерн СберБизнес/Т-Бизнес), каталог
  drill-down, iiko-готовность — `artifacts/SPEC-msp-cabinet-v2.md`, карточка
  SZ-050 (Ф1–Ф4).
- **Ф1 сделана**: core — `partners[]` в обзоре (включая пустые юрлица) + team по
  `partner_id` (62/62 🟢); app — `PartnerSwitcher` в шапке кабинета МСП
  (чип с ИНН-маской → шит «Мои юрлица» + «Добавить юрлицо»), контекст
  фильтрует Обзор/Заказы/Товары/Настройки/Команду, пустые состояния честные
  (815/815 🟢, живая проверка :8081).
- **Находка владельца (важно)**: «переключаются мерчанты в рамках одного ИНН».
  Корень — заявка 14.09 «Пончики» с тем же ИНН создала ДУБЛЬ юрлица (271).
  Правило зафиксировано в SZ-050 Ф2: существующий ИНН → новый бренд в
  существующее юрлицо; новый ИНН → новое юрлицо. Локальные данные слиты
  (мерчант «Пончики» → АТМОСФЕРА, партнёр 271 удалён) — свитчер теперь
  показывает одно юрлицо с двумя брендами, как в модели владельца.
- Session-док `lovii-app/docs/sessions/118-msp-partner-switcher.md`.

## 2026-09-17 (день, итерация 2) — zcode: юрлица на экране «Команда» — бренды по юрлицам, состав подписан юрлицом

- **Вопрос владельца**: «не увидел список точек другого юрлица… в кабинете МСП должны
  быть все юрлица». Факт: точки другого юрлица уже появлялись (выбором его бренда),
  но экран нигде не называл юрлицо. Синхронизировали термины: юрлицо → бренд
  (= мерчант, один уровень) → точки.
- **core** (`feat/msp-overview-partner-name`, `f2a4f06`): обзор отдаёт
  `partner_id`/`partner_name` мерчанта (+тест, 42/42 🟢). ⚠️ Грабли: локальная ветка
  `staging` в core устарела — актуальная база = tip `feat/sz-045-chargeback-mirror`
  (f06ecfb, «на стенде»).
- **app** (`feat/sz047-team-invite-canon`, `58546a0`): бренды на «Команде»
  сгруппированы по юрлицам (подписи групп), у одиночной карточки юрлицо второй
  строкой, состав — «ООО "АТМОСФЕРА" · 4 в доступе». Гейт 808/808 🟢, живая
  проверка: обе группы юрлиц на экране.
- **Осталось → карточка SZ-049**: мерчант под существующим юрлицем (право
  `merchant.create` есть — роута нет), юрлица без мерчантов не видны, точки из
  приложения (`branch.create`). Приглашение в любое юрлицо уже работает.

## 2026-09-17 (день) — zcode: канон приглашения в команду «бренд → точки бренда → роль → телефон» (cabinet/msp/team, SZ-047)

- **Нашёл владелец**: «не совсем корректно сделано приглашение» — и был прав. По коду:
  шага «бренд» не было вовсе (мерчант неявно из рабочей точки), список точек — плоский
  `flatMap` филиалов всех мерчантов без брендов, «Точка» и «Филиалы доступа» — два
  контрола на одном списке, при отметке точки чужого бренда ядро отвечало
  422 `branch_not_found`. Канон утверждён владельцем: бренд → точки бренда → роль →
  телефон; бренд виден всегда; заголовок «Команда»; **имени в форме нет** (сотрудник
  задаёт сам в профиле — решение 2026-09-17); матрица прав подтверждена
  (администратор = всё как владелец, сотрудник = заказы + наличие).
- **Сделано в `lovii-app`** (без правок ядра): `MspTeam.vue` — выбор бренда (чипы с
  плиткой, один бренд — карточка), точки = филиалы выбранного бренда, `branch_ids`
  при нескольких брендах уходят **явным списком** (пустота = «все точки юрлица» —
  выдала бы чужие бренды), `branch_id` = точка выбранного бренда (резолв юрлица),
  рабочая точка кабинета больше не переключается из формы; **весь экран (состав,
  форма, «Ожидают принятия») следует за выбранным брендом** — у бренда может быть
  своё юрлицо («Пончики» = partner 271 ≠ «Пышки & Пончики» = 263). `TeamTeam.vue` —
  то же про имя. Тип `MspMerchant.logo_url` опциональный (ядро пока не отдаёт —
  иконка-фолбэк; строка в SZ-048).
- **Живая проверка** (превью :8081 → локальный core, владелец +79119287478): форма по
  канону, поля имени нет; приглашение +79110000094 под «Пончиками» → в БД
  `partner_id 271`, `branch_ids [1026]`, operator; список «Ожидают принятия» после
  фикса показывает его под своим юрлицом. NB: :8081 бьёт в **локальный** core —
  staging не тронут.
- **Дальше**: карточка **SZ-048** (аудит-лог «кто/когда/с чего → на что» + настройки
  в кабинете команды + logo_url в обзоре) — исполнителю Super Z; тумблеры товара —
  отдельный разбор с владельцем (SZ-048 §5).
- **Файлы**: `MspTeam.vue`, `TeamTeam.vue`, `roles-api.ts`, `roles-msp.test.ts`
  (блок переписан, 6 тестов). Session-док `docs/sessions/117-team-invite-canon.md`.
- **Гейт**: `yarn test` 95 файлов / 808 тестов 🟢 (format/lint/unit/type-check/build).
- **Коммит**: локальный, ветка `feat/sz047-team-invite-canon`, **не пушен** (GitHub
  по-прежнему заблокирован) — ждёт приёмки владельца и мержа в `staging`.

## 2026-09-17 (ночь) — zcode: классы канона кабинета без стилей — кнопки строки товара и весь кабинет (правка сессии 115)

- **Находка AutoCoder** (сессия 115, «что не сделано» п.3): в `MspProducts.vue` кнопки-иконки
  строки товара несли класс `role-icon-btn`, а правила для класса в scoped-блоке не было —
  кнопка рендерилась **0×20 без глифа** (у `LvIcon` нет атрибутов `width`/`height`, размер
  задаёт только CSS). Правка и удаление товара были недоступны вовсе, не «мелкий таргет».
- **Сделано в `lovii-app`**: подключён существующий миксин `role-icon-btn` в «Товарах» МСП
  (+ `.role-row-mid`); в «Счёте» МСП — `.role-empty` и `.role-field__error`; в «Заказах»
  кабинета команды — `.role-card` (карточка точки и пустые состояния были без подложки);
  ручные копии стилей `role-icon-btn` в «Настройках» МСП и «Товарах» команды заменены
  миксином (копия уже разошлась с каноном — не гасила `:disabled`).
- **Аудит всего кабинета** (`src/modules/roles-module/**`, 25 файлов, скрипт
  `parity-baseline/audit-cabinet-classes.mjs`): канон-классов без правила/`@include` и
  ручных копий миксинов больше нет; локальные классы без правил вовсе
  (`.cabinet-screen`, `.rep-overview__next`, `.rep-approvals__head`, `.rep-points__search`)
  **не оформлял** — стиль для хука без миксина придумывать запрещено задачей, список в доке.
- **Замеры на стенде** (владелец `+79119287478`, 31 строка, обе темы, 390/320px):
  таргет кнопки `0x20` → **`44x44`**, глиф `0x0` → **`16x16`**; переполнений нет,
  консоль чистая (0 ошибок на девяти экранах) и до, и после.
- **Файлы**: 5 экранов `roles-module/**` (только scoped-стили). Session-док
  `docs/sessions/116-canon-classes-without-styles.md`, отчёты/кадры —
  `parity-baseline/shots/icon-btn-{before,after}-*`.
- **Гейт**: `test:unit` 95 файлов / 804 теста 🟢, `type-check` 🟢, `lint` 0/0 🟢,
  `build-only` 🟢; страж дизайн-системы зелёный, baseline не менялся (долг 522 → 522).
- **Коммит**: локальный, **не пушен**. Ждёт приёмки владельца.

## 2026-09-17 (ночь) — zcode: ядро МСП, смена роли и отзыв доступа участника команды (SZ-047 §3)

- **Задача владельца** (2026-09-16): «В кабинете МСП нет возможности сменить роль сотрудника
  или удалить у него доступ. Этот функционал должен работать и у администратора». Факт по коду:
  под `/api/v1/msp` были только `GET /team` и `POST /team/invitations`, у `manager` — только
  `team.view`/`team.invite`.
- **Сделано в `lovii-core`**: `PATCH /api/v1/msp/team/{membership}/role` (тело
  `{"role":"manager"|"operator"}`) и `DELETE /api/v1/msp/team/{membership}` (доступ снимается
  переводом членства в существующий статус `suspended` — все выборки `MspAccess` фильтруют
  `status = active`, поэтому отозванный сразу теряет точки и заказы); `GET /team` отдаёт
  `membership_id`, `branch_ids`, `status` — участник адресуем, прежние поля не тронуты.
  Защиты (`MspTeamActions`): себя не трогаем, участника с ролью не ниже своей не трогаем
  (владелец неприкосновенен), роль выше своей не выдаём, администратор — только в границах
  своих филиалов, чужой участник/партнёр и отозванный — одинаковый 404 `member_not_found`.
- **Отклонение от карточки** (сознательное, решение владельца): `Manager` получил
  `team.update_role` и `team.revoke` — §3.3 карточки SZ-047 говорил «уровень manager может
  выдать только владелец». Комментарий в `PartnerRole::abilities()`, следствия — в session-доке
  (администратор теперь может пригласить администратора; core-энум ушёл вперёд копии в b2b,
  синхронизация Core → B2B доедет следующим запуском скрипта).
- **Аудит** — существующим механизмом: события `team.role_updated` / `team.revoked` в
  `lovii_b2b.partner_audit_logs` (видны в b2b-кабинете), нового не заводили.
- **Файлы**: `app/Domain/Merchant/Services/MspTeamActions.php`, 2 контроллера, `MspTeamMemberResource`,
  `PartnerRole`, `MspAccess`, `routes/api.php`, `tests/Feature/Roles/MspTeamManagementTest.php` (12 тестов).
- **Коммит**: `f70514a` (локальный, **не пушен**). Session-док `docs/sessions/031-team-role-and-revoke.md`.
- **Гейт**: `tests/Feature/Roles` 93 passed / 490 assertions (было 81/351) 🟢, `tests/Unit` 326 passed
  (1 предсуществующее падение `UserTest` из-за чужой миграции `users.promo_code`) 🟢, `tests/Feature`
  787 passed / 21 failed — все 21 предсуществующие (Payment/VK/Events, чужой `phpunit.xml`),
  доказано прогоном на коде без изменений; `phpstan` по изменённым файлам — 0 новых ошибок (1
  предсуществующая в `MspAccess:119`); `pint` — pass; 6 мутаций ловятся тестами.
- **Ждёт**: клиент `lovii-app` (SZ-047 §5) — экран команды с кнопками смены роли и отзыва;
  флаги `can_update_role`/`can_revoke` в строке участника не делали (контракт клиента).

## 2026-09-17 — zcode: серия «LOVII как есть» — модули 01–03 (витрина, чекаут, заказы)

- **Задача владельца**: готовим код к релизу — сначала описываем весь функционал
  «как есть» (по коду), потом правим и выписываем «чего не хватает». Формат:
  **отдельный док на модуль** для поиска.
- **Сделано**: `lovii-docs/as-is/` — README-индекс + **все 13 доков**: 01 Витрина,
  02 Корзина/чекаут, 03 Заказы, 04 Баллы/кошелёк/платежи, 05 Роли/кабинеты,
  06 Вход/OTP, 07 Пуши, 08 Доставка/гео, 09 b2b, 10 Админка + по запросу владельца
  11 База данных (навигатор по схемам/таблицам), 12 Финконтур в коде и БД
  (сквозной путь заказа→платёж→пул, инварианты SQL, расхождения с каноном),
  13 Словарь сущностей b2b (юрлицо/магазин/точка — и 6 источников «бардака»
  кабинета МСП). Факты с `file:line` + живой SQL staging (счета владельца:
  АТМОСФЕРА 52 587 ₽, АКСИОМА 2 003 ₽, личный 3 004,49 ₽; дубль юрлиц
  Grand/Статус-92 по ИНН 970512345688). Владелец читает, дальше — сверка по
  модулю → правки → фаза «чего не хватает».
- **Готовые находки для фазы правок** (детали в доках, раздел «Кандидаты»):
  min_order не энфорсится; зоны доставки не проверяются гео-точкой; списание
  баллов бэкенд умеет — UI нет; пушей на accepted/ready нет; чеков нет;
  Idempotency-Key клиент не шлёт; `OTP_DEV_BYPASS` в конфиге default true (прод-риск);
  верификация юрлиц — только ручной UPDATE (UI нет в b2b и admin); финконтур заявки
  (invoice→verified) не реализован; форма заявки app не передаёт промокод (2 формата
  ссылок ?ref= vs ?promo=); `INTERNAL_ADMIN_SECRET` отсутствует в admin .env.example.
- Попутно починен перелив индекса памяти zcode (25,4K → 23,8K симв.).

## 2026-09-17 (ночь) — zcode: сессия 114, дубль «перки уровня ↔ карточки привилегий» в профиле закрыт

- **Задача владельца** («делай так, чтобы всё работало как надо»): в блоке «Статус LOVII PAY»
  выгоды подавались дважды — плашками перков уровня и карточками «Привилегии статуса».
  У PASS подписи совпадали **дословно**, у VIP — по смыслу. Не видно было только потому,
  что `payTierStatus(EMPTY_TIER_STATE)` всегда даёт базовый уровень.
- **Решение**: карточки привилегий — единственный источник описания выгод, ряд перков
  показывает только то, чего в карточках нет. Фильтр — нормализация (регистр, «ё» → «е»,
  кавычки, дефис/тире → пробел) + вложение в обе стороны: `Ранний доступ к новинкам` снят
  карточкой `Ранний доступ`. Пусто — ряда нет в DOM (ни плашки, ни отступа).
  Словарь синонимов не вводился: VIP-пары `Лимитированный мерч` / `Мерч Лови`,
  `Приоритет в мероприятиях` / `Приоритетная запись`, `Персональные скидки у МСП` /
  `Спец-скидки у МСП` **остаются в ряду** — маппинг требует решения владельца, зафиксировано
  в комментарии хелпера и в session-доке.
- **Файлы**: `pay-privileges-match.ts` (новый хелпер), `ProfileModule.vue` (tierPerks + `v-if`),
  тесты хелпера и компонента. Стили, канон `PayTier.perks` и карточки не тронуты.
- **Коммит**: `006ff6e` (локальный, **не пушен**). Session-док `docs/sessions/114-tier-perks-vs-privilege-cards.md`.
- **Гейт**: `test:unit` 95 файлов / 792 теста 🟢, страж ДС 🟢 (baseline не менялся),
  `type-check` 🟢, `lint` 🟢, `build-only` 🟢; стенд 390×844 обе темы — перков 3, карточек 5/5,
  errors 0, overflow 0px. Мутационная проверка: без фильтра PASS/VIP-тесты падают.
- **Ждёт владельца**: маппинг VIP-пар и источник состояния уровня (подписка/оборот из API).

## 2026-09-15 (вечер) — auto-designer: аудит кнопок/перекрытий, адрес, избранное, логотип (app)

- **Точка возврата** для отката: тег `checkpoint/audit-2026-09-15` (коммит `4eb26c7`) в `lovii-app`. Итог сессии: коммиты `efd156b` + `ad3c7d2` + `3d17373` + `e9d8984` + `5281b21` (не пушены).
- **Системная причина «неоформленных блоков»** — фантомные токены ДС: 20+ имён
  (`--Text-Base-Normal-*`, `--bg-primary`, `--stroke-border`, `--text-solid`,
  `--content-normal-success`, …) использовались компонентами, но в слое токенов их
  не было → браузер отбрасывал свойства целиком (сегмент-контроль «Способ получения»
  на чекауте, карточка/квитанция заказа, инфо-лист точки, business-экраны, auth-binding).
  Добавлен блок компат-алиасов в `src/scss/tokens.scss` (канон `--lv-*`, обе темы).
- **Кнопки-фиксаторы под нижней навигацией**: 16 экранов брали `60px` < высоты
  таббара `64px` → всё переведено на `--lv-navbar-offset` (+стicky CTA/фильтры).
  Проверено живьём: отступ `72px`, кнопки доступны и прокручиваются.
- **Адрес**: саджест закрывается при уходе из поля и не всплывает поверх «Дом»;
  в хедере — только название улицы на всю ширину; в гео-шите «Сохранить/Отменить»
  в одном ряду.
- **Избранное с витрины вернулось**: `favorites.store` (localStorage) + ♥ на карточке
  витрины и странице точки + живой список «Избранные МСП» в профиле. Серверного
  избранного в ядре пока нет — синхронизация между устройствами отдельной задачей.
- **ПИН/Face ID** на экране блокировки и в сетапе — реальный логотип (не буква «L»);
  способы быстрого входа теперь взаимно исключаются.
- **Избранное перепроектировано по месту** (доп. правка после замечания владельца
  «подойди как дизайнер»): вместо трех копий и наклейки поверх строки — единый
  `FavoriteButton` с двумя контекстными видами (тихая иконка в строке списка с
  тап-таргетом 44px; плавающая пилюля на «герое» карточки точки по канону демо).
  На витрине избранное стало колонкой сетки `[логотип | текст | действие]`, статус
  точки — в строке метаданных. Коммиты `ad3c7d2` + `3d17373`.
- Гейт app: `type-check` 🟢, `test:unit` 702/702 🟢, `build-only` 🟢, живые проверки
  стенда (:8081) 33/33 🟢. Тесты app 710/710, ядро Loyalty+Billing 37. Коммиты локальные, **не пушены**.
- **Страница «Все заведения» (/stores)** по концепции: плитка логотипа 40 → 48px
  со скруглением 4 → 12px и обрезкой картинки по контуру; состояние точки —
  строкой под названием (наезда на сердечко нет). Коммит `e9d8984`.
- **Категории на витрине**: проверено по трём репо и живой базе — `merchant_categories`
  пуста (0 категорий, 0 связей), пишет только выключенная интеграция Kuper, поэтому
  раздел «Категории» в фильтре рендерился пустым → убран. Типы заведений (`merchant_type`)
  живые (26/1/1, ставятся заявкой и в кабинетах) — не удалялись, решение за владельцем.
  Коммит `5281b21`.
- **Дизайн-система: единый ритм блоков и страж отступлений.** В профиле пары блоков
  получали 16px и 0px (правило `&__nav { margin: 0 }` перебивало контейнер) — теперь
  зазор задаёт только контейнер через токен `--lv-block-gap`; все 12 зазоров 16px.
  Введён автоматический храповик `design-system.guard.test.ts` (радиусы-литералы 162,
  тени 59, hex-цвета 352 в 101 файле) — уровень долга можно только понижать.
  Правила для агентов — `lovii-app/docs/DESIGN_RULES.md`. Коммит `1c3ceb0`.
- **Английские подписи операций убраны** (запрос владельца): история LOVII PAY печатала
  сырой комментарий с бэкенда («Earned from order #160»). App теперь берёт русскую
  подпись из своего словаря (`9375bc3`), ядро пишет проводки по-русски
  (`6f5c5b0`), миграция привела уже записанные строки — латиницы 0.
- **Низ профиля приведён к канону демо** (`979f9c3`): одна карточка с группами
  «Документы» / «Поддержка» / «Аккаунт» (выход переехал сюда из навигации),
  строки 44px, радиус/тень — токены; ниже юр. подпись. Живые проверки 39/39.
  Страж ДС уточнён (тень с токеном и 3-значный hex больше не «литералы»):
  долг — радиусы 162 · тени 25 · цвета 351. Страж сразу поймал radius-литерал
  в `MspBranchSettings.vue`, который правит другой агент (его работа не тронута).
- **Низ профиля — ровно канон, настройки пересобраны** (`1c0a970`): юридическая
  карточка снова только «Документы + Поддержка» (моя группа «Аккаунт» была
  отступлением), выход переехал в настройки. Шит настроек: было 14 обведённых
  коробок → карточки + надзаголовки + строки; выбор — чипом (tiffany), булевы —
  новым китовым `AppSwitch` (канон `.sw`), дубль переключателей снят.
  Правило канона: «включено/выбрано» = tiffany, розовый = действие.
  Тесты 714/714, живые проверки 44/44.
- **Акценты по ролям и решение по ритму** (`298142f`): введена цепочка `--lv-accent*` —
  цвет выбора (чипы/табы/сегменты/чекбоксы/радио) берётся из контекста: в клиенте
  бренд-розовый, в кабинете роли подменяется цветом роли (тиффани/золото). Вне цепочки:
  тиффани — состояние, золото — премиум, розовый — действия. Ритм канона сведён к
  двум уровням: 16px внутри группы, 24px между секциями (28px — только лендинг).
  Оба решения — в `docs/DESIGN_RULES.md`. Гейт 779/779, живые проверки 46/46.
- Замечание по работе в репо: параллельно в `lovii-app` и `lovii-core` работают другие агенты
  (радио-стандарт, `AppRadio`); его незакоммиченные файлы не трогал и не коммитил. Session-док:
  `lovii-app/docs/sessions/107-action-buttons-address-favorites-audit.md`.
- **Открыто (следующая сессия)**: экран «Безопасность» (устройства, мессенджеры
  MAX/VK/Telegram + инструкции, маршрут оповещений, назначение e-mail), пересборка
  композиции настроек профиля, решение по типографике, серверное избранное.

## 2026-09-15 (вечер) — zcode: сессия кабинетов закрыта, коммиты подготовлены, стенд поднят

- **Все три кабинета ролей** (Амбассадор, Представитель, МСП) пересобраны по канону
  демо на честных данных, стоят на одном визуальном языке (`styles/cabinet-ui.scss`).
  Гейт app: 686 тестов, ядро: 1108. Отдельно закрыт класс «битые иконки» системно
  (`normalize.scss`: наследование цвета узлов svg) и заменена корзина в шапке на
  профиль (канон V11 «Correct»).
- **Реферальная цепочка работает end-to-end**: QR/ссылка на текущем origin,
  перехват `?ref=`, привязка при регистрации, роли МСП/Представитель от промокода,
  гейты кабинетов по промокоду. Проверено живьём: приглашённый — 200 по всем
  эндпоинтам кабинетов, профиль без промокода — 403 и редирект в профиль.
- **Профиль**: единый логический порядок блоков, две колонки CSS multi-column без
  пустых мест (768/1440 — 8 и 10 блоков, «дыр» нет), 404 заявки убран (200 + null).
- **Коммиты:** app `4e89338` + коммит этой сессии (ветка `staging`); ядро `839a1f1`
  + коммит этой сессии (ветка `feat/referral-code-on-registration`); demo `56710db`.
  Пуш не делался: **GitHub отдаёт 403 «account suspended»**.
- **Handover для новой сессии:** `lovii-app/docs/sessions/105-profile-flow-and-handover.md`
  (там же список непроверенных состояний и порядок пуша). Ядро: сессии 030–032.
- Стенд поднят (core :80, превью :8081, OrbStack) — владелец не все состояния успел
  посмотреть.

## 2026-09-15 — zcode: кабинеты ролей РАБОТАЮТ по промокоду (БАТЧ 3.2, core + app)

- Канон владельца: доступ к кабинетам ролей — по **наличию промокода у профиля**;
  нет промокода — нет доступа полностью; есть — пользователь сам выбирает роль.
  Форма ввода промокода в настройках не делалась (отдельная задача).
- **Ядро** (`lovii-core`, worktree `lovii-core-referral`, ветка
  `feat/referral-code-on-registration`, сессия 031):
  `EnsureRepresentative` пускает и приглашённого по промокоду; на `/msp/*`
  появился гейт `role.msp` (`EnsureMsp`) — «данные точки ИЛИ привязка», иначе 403
  (раньше роли не проверялись вовсе). Профиль с промокодом без точки получает
  **честные состояния вместо 404**: `overview {merchants:[],latest_orders:[]}`,
  `orders []`, `products []`, `branch/settings data:null`, `payment data:null`.
  Найдена и закрыта мина: `isReferredByRepresentative()` падала 500 при
  strict-режиме Eloquent, если колонка не в модели (fail-closed).
  Тесты: 55 по ролям, полный `composer test` — 1108 passed.
- **App** (`lovii-app`, сессия 103): экраны МСП и профиль представителя доведены
  до честных состояний («Точки пока нет» / «Настраивать пока нечего» /
  «Заявки пока нет» / «Своего кода пока нет» вместо «—» и пустого QR);
  `rolesGuard` проверен живьём — без промокода прямой URL кабинета уводит в профиль.
  Гейт: unit 666/666, type-check, build.
- Живой прогон: ядро на `:3001` (приглашённые 339/342 — 200 с пустыми данными;
  340 без промокода — 403 на всех входах; реальные 51/336 — как раньше),
  app на `:8081` (390×844). Не коммичено — владелец должен проверить перед пушем.

## 2026-09-15 (утро 2) — zcode: lovii-app — кабинет МСП переведён на общий язык кабинетов (БАТЧ 3)

- Все 7 экранов МСП (`msp/*`) — на общем партиале
  `roles-module/styles/cabinet-ui.scss`; партиал расширен 20 миксинами (сегмент,
  тона статусов заказа, тег типа исполнения, поля ввода, свитч, kv-строки, шаг
  чек-листа, дорожка прогресса, пульс ожидания, плитка фото). Экраны
  амбассадора/представителя и `CabinetLayout` не менялись.
- Найдено и починено 17 дефектов, в т.ч.: `MspPayment` использовал `AppButton`
  без импорта — кнопки экрана не рендерились вообще (доказано по бандлу:
  `resolveComponent("AppButton")` без резолвера); Стартер грузил данные БЕЗ
  `branch_id` (показывал чужую точку) и рисовал выдуманное «5 из 5 готово»;
  история заказов была некликабельна; деньги без ₽ на всех экранах; тумблер
  товара перезагружал каталог целиком; ошибка чтения настроек оставляла форму с
  дефолтами (сохранение затёрло бы расписание); перевёрнутый интервал уходил на
  витрину; названия заказов и товаров обрезались.
- Живой прогон стенда :8081 (390×844): ключевые блоки в DOM, все числа сверены с
  ответами API (29 товаров, 2/15 заказов, 600 ₽, 08:00–21:00), переполнение на
  320px = 0 на всех экранах, тапы ≥44px, эмодзи нет, клик из списка и из истории
  открывает деталь, консоль без ошибок Vue. Фейковая оплата пройдена вживую
  (`POST /orders/160/payment` → `/payment/fake` → `/payment/result`, в ядре
  `payment.status = confirmed`).
- Замеры: переходы SPA 95–414 мс (Товары с 29 позициями — 107 мс), длинных задач
  (>50 мс) нет; Товары на 320px — 0 переполнения, 639 узлов DOM.
- Гейт: unit **662/662** 🟢, type-check 🟢, build 🟢, eslint изменённых файлов 🟢.
  НЕ коммитил — дерево поверх незакоммиченных 096–101. Сессия app: 102.

## 2026-09-15 — zcode: lovii-app — иконочная тема закрыта системно + тапы шапки + кошелёк 320px

- Корень класса: `* { color: var(--content-normal-primary) }` в `normalize.scss`
  красит каждый узел svg, `currentColor` резолвится по своему узлу → иконка теряла
  цвет контейнера (замер «до»: 93 svg с чужим цветом на 7 экранах). Прежние
  `:deep(svg *) { color: inherit }` дефект НЕ лечили — `<svg>` тоже пойман `*`.
- Системный фикс: `:where(svg, svg *) { color: inherit; }` сразу после `*`
  (нулевая специфичность: явные правила с классами не перебиваются). Иконочных
  дефектов 0 на всех экранах, светлая и тёмная темы.
- Сняты 43 избыточных `:deep`-блока + 20 строк `color: inherit` в 17 файлах —
  доказано no-op замером (0 расхождений computed color/fill/stroke до/после).
- Шапка: логотип/пилюля адреса/кнопка темы доведены до тап-зоны ≥44px
  (визуальный ритм прежний); кошелёк на 320px больше не уезжает (49px → 0,
  канон `.pay-cols .pay-hints` — перенос подсказок); ручные глифы chev-right/copy
  в кошельке → `LvIcon`.
- Гейт: unit 560/560 🟢, type-check 🟢, build 🟢, eslint изменённых файлов 🟢.
  НЕ коммитил — дерево поверх незакоммиченных 096–100. Сессия app: 101.

## 2026-09-14 (вечер 2) — zcode: путь подключения точки МСП на локальном стенде — пройден ЦЕЛИКОМ

- Заявка #7 «Пекарня "Заветный хлеб"» (user 336, AA2222): реп-апрув был →
  верификация проставлена вручную (матчинга платежа ещё нет — бэклог §3.7),
  филиал 1025 доматериализован (заявка была без координат), membership для
  pu 262 дозаполнен (его не создало при заявке — GAP, заявка #8 создала штатно).
- Пончики (заявка #8, ИНН 7842216839): комплаенс-отметка владельцу поставлена
  (verified), филиал 1026 Думская 4 создан + имена филиалов дозаполнены.
- Кабинет МСП (user 51): 2 точки (Пышки 1024 + Пончики 1026, membership 258→263
  дозаполнен), селектор «Рабочая точка» работает.
- **Прод-аудит Обзора МСП (решение владельца) — переверстка**: дубли точек
  слиты в один селектор (карточка = выбор), заказы Обзора подчиняются выбранной
  точке (core branch_id-фильтр + watch в app), строки заказов тапабельны в
  деталь со статусом («Выдан клиенту»). Время в настройках — селекты ЧЧ/ММ
  (24ч, AM/PM локали убраны); ошибка сохранения больше не вытесняет форму;
  чужая активная точка при смене аккаунта сбрасывается валидацией.
- Тест-режим: VITE_DISABLE_SW=true (SW off, кэши чистятся при старте) —
  рассинхрон SW-кэша↔бандла после серии пересборок больше не ломает SPA-переходы.
- Гейт: yarn test 454/454 🟢; core pint PASS, overview live 200. НЕ коммитил —
  в дереве поверх 096/097/098; ⚠️ перед пушем вернуть staging-URL в .env.
  Сессия: lovii-app/docs/sessions/099.

## 2026-09-14 (вечер) — zcode: lovii-app — выравнивание иконок профиля

- Запрос владельца: размеры иконок в профиле — как у блоков «Кошелёк»/«Мои баллы».
- Корень: стили демо-блоков оформлены селектором `> .icon`, а `LvIcon` рендерит голый
  `<svg>` без класса — размеры молча не применялись, иконки разъезжались до дефолтных
  30–189px (кабинеты, шевроны блоков, «Выписка»/«Скопировать номер» 71/43px,
  ops-link 189px). Эталон: плитка 36px + иконка 17px.
- Фикс: 11 селекторов `> .icon` → `> svg` в 4 файлах (ProfileModule, ProfileCollapse,
  ProfileCabinets, WalletHistoryList); правило для будущих использований `LvIcon` —
  размер задавать селектором `svg`, не `.icon`. Гейт 🟢 (format/lint/unit/type/build),
  живая проверка DOM-метриками: все профильные svg 12/16/17px. Сессия app: 098.
- Дополнение (фидбек «иконки у кабинетов отъехали»): секция `.cabinets` ловила чужое
  правило `.profile__nav > section` (подложка+паддинг) → двойной отступ плиток 32px.
  Исключена через `> section.cabinets` (без подложки), gap строк 10→8px; теперь плитки
  x=32 / текст x=76 — 1:1 с меню на обеих раскладках. Гейт 🟢 повторно (454/454).
- Дополнение 2 (фидбек «справа к тексту отступ маловат»): канон демо `.row-item` —
  gap 12px плитка→текст; в app было 8px → поднят до 12px в строках меню, переключателях
  и кабинетах (свод кабинетов к 8px откачен). Метрики: gap 12px везде, плитка от края
  карточки 16px. Гейт 🟢 (454/454), dist обновлён.
- НЕ коммитил — в дереве поверх незакоммиченных правок Sisyphus (096/097); превью
  :8081 обновлено (dist пересобран), владельцу сбросить PWA-кэш.



- `MspPayment.vue`: `useRouter` вместо шаблонного `$router` (ломал навигацию),
  статус «verifying» → тиффани, комплаенс-пульс CSS (keyframes 1.6s +
  `prefers-reduced-motion`). Тест payload заявки починен (промо `promo: null`
  в expected) + новый тест прохождения/валидации промо → `yarn test` 🟢 454/454,
  vue-tsc, lint, build. Сессия app: 097-msp-payment-handoff.md. НЕ коммитил —
  жду «го» (в дереве ~20 правок поверх 096/097).
- ⚠️ Стейдж-сервер 89.19.223.16 недоступен (TCP/ICMP таймают, все протоколы) —
  похоже, вне репо (хостер/машина). Деплой и e2e-staging отложить до выяснения.
- Сотрудники МСП (§9.3, итерация 3): вертикали нет ни в core (нет
  `/msp/members`, invite), ни в app (нет экрана/guard) — следующая большая
  задача, начать с core-эндпоинтов.

## 2026-09-14 (день) — SZ-038 фаза D: multi-point МСП + полировка заявки (Sisyphus)

- Владелец прошёл подключение точки на локальном стейдже: в кабинете МСП нет
  «Добавить точку» (вторая точка сети), нет выбора активной точки, ИНН в
  повторной заявке не подставляется. Явало и старую заявку без ИНН в демке.
- lovii-app (`staging` рабочая копия, поверх чужих правок — не коммитил):
  «Добавить точку» в Обзоре кабинета; селектор «Рабочая точка» (+ msp-стор
  `activeBranchId` с persist; unit-тесты); branch_id в Заказы/Товары/Настройки;
  prefill ИНН в заявке (§9.5). `yarn test` 🟢 полный (448+). Сессия app 096.
- lovii-core: branch_id в `msp/orders|products|branch/settings` (+ чужой id
  → 404 branch_not_found; товары в сети выбранной точки), MspAccess переезжен
  в Domain → правки на актуальном origin-коде (rebase из state behind 14).
  Pint 🟢, MspEndpointsTest 33/33 🟢. phpunit.xml герметизирован от хостовых
  секретов (VK/TG/MAX-токены зеркалились со стейджинга в локальный .env и
  меняли канальную стратегию в тестах). Сессия core 029.
- Фикс демо: `isOpenNow` падал на часах с тире `08:00–21:00` (innerHTML правки
  из §9) — нормализация тире + guard; заявка МСП в демке — добавлено поле ИНН.
- FINDINGS F-052: required_with рубит пустые `overrides:[]` в расписании
  точки (валидатор), тесты каналов стали герметичными; 16 предсуществующих
  красных в core на origin-tip локально (VK/Payment/Listeners) — вне scope.
- Открытое: e2e «две точки» нужен seeded-владелец с двумя филиалами на
  стейджинге; каналы OTP на локальном стенде выровнены со staging (MAX·TG·VK).

## 2026-09-13 (вечер) — zcode: SZ-045 — полное зеркало чарджбэка + возврат баллов (код готов, ждёт «го»)

- Спека: `canon/FINANCIAL_CONTOUR.md` §5 v0.5 (утверждён, 23f3971) + финальный гайд
  большой модели `canon/TASKS/SZ-045_financial-contour-s5-code-fixes.md` (36348b2).
- Аудит трассируемости §5.5 П7 по staging (MCP read-only): инцидентов НЕТ (12 заказов,
  все ноги на месте; карточные — двухсторонние через номинал SZ-043); дублей
  wallet_transactions нет → обычный unique-индекс.
- Фича-ветки (НЕ запушены, ждут отчёта-сверки владельца):
  core `feat/sz-045-chargeback-mirror` (498f513) — ReverseOrderPoolAction (зеркало всех
  ног по order_id, обе стороны; инбокс да, эквайринг-fee остаётся), PostChargebackBankFeeAction
  (Dispute Fee на Точку, ручной ввод — П2), PaymentStatus::Chargeback, вебхук разворачивает
  атомарно, unique wallet_transactions + refundSpendForOrder + машина статусов в транзакции +
  листенер b2b-пути, internal POST /orders/{id}/chargeback + chargeback_logs; +9 тестов,
  целевые 26 🟢, полный гейт — только известные локальные красные (10 на чистом staging,
  гейт CI).
  app `feat/sz-045-pay-card-points` — карта PAY показывает баллы (balanceUnit, apiGetWallet
  в профиле и кошельке), yarn test 🟢.
  admin `feat/sz-045-chargeback-admin` — ChargebackResource (симметрия +/−, ручная комиссия,
  подтверждение через core), CoreApiClient (паттерн SZ-037), переводы ru/en; гейт по CI.
- Открытое: assumption «банк возвращает эквайринг-fee» — подтвердить; ENV admin
  (CORE_API_URL, INTERNAL_ADMIN_SECRET); Shield-права на ресурс; деплой с migrate.
- Сессии: core 028, app 095, admin 004.

## 2026-09-13 (день) — lovii-app: двухколоночные профиль и кошелёк на широких экранах (fold)

- Владелец: «на fold карта растягивается на всю ширину, так быть не должно; должно быть
  две колонки». Причина — конвенция F-041 оставляла профиль/кошелёк одноколоночными,
  карта LOVII PAY занимала всю ширину колонки.
- Ветка `feat/wallet-wide-cols` → `staging` (мерж `d4167c4` + docs `b390e4b`), сессия-док
  `lovii-app/docs/sessions/094-profile-wallet-wide-cols.md`.
- Канон демо `.pay-cols`: ≥640px сетка 5fr/6fr — профиль (карты | меню), кошелёк
  (карта+счёт | история); ≥1024px gap 28px; мобильный порядок прежний; карта ≈390px на
  fold-раскладе (~900 CSS px). `yarn test` 🟢 (423); визуально проверено на 908/1280.
- **Итерация 2** (фидбек владельца, `be51e77` в staging): логическая группировка блоков.
  Лево — «покупатель»: карта (+единственная кнопка «Выписка», дубль «Кошелёк» убран) →
  Деньги → Адреса → Покупки. Право — «роли и настройки»: ЛОВИ Бизнес/Моя точка →
  кабинеты → быстрый вход → оповещения → выйти. Принцип: секция = группа, новые пункты
  добираются в конец своей группы без пересортицы.
- NB: PWA SW на стенде держит старый бандл — приёмку владельцем после сброса SW/caches.

## 2026-09-13 — zcode: канон-правка — сущность LOVII PAY

- Диктовка владельца, дословно: в `lovii_docs/canon/FINANCIAL_CONTOUR.md` добавлен
  подраздел **§5.1 «Сущность LOVII PAY»** — виртуальная карта-витрина при регистрации;
  на карте ТОЛЬКО баланс баллов (`wallets`), не рубли (`accounts`); номер карты =
  уникальный идентификатор пользователя (UID) для внутренних переводов и будущих
  интеграций; рубли personal-счетов — только внутренний учёт долей. Пуш `da685d4`
  в lovii_docs main.
- `lovii-docs/LOVII_master_brief_v3.md` §5.1 «Для клиента»: после однострочника добавлен
  абзац про единую карту LOVII PAY (баллы тратятся у любой точки района — кросс-обмен).
  Поставлен в §5.1, а не в элеватор-питч §3.1, чтобы не ломать ритм 30-секундного питча.
- Код не трогался; при реализации — сущность «карта LOVII PAY» в core ещё не существует.
- Развитие того же дня: владелец заменил раздел 5 ЦЕЛИКОМ (финальная структура
  5.1–5.4, пуш `50bdfe7`; промежуточные §5.1/§5.7 из пушей `da685d4`/`9630087`
  перекрыты): §5.1 LOVII PAY = карта-витрина и универсальный счёт по умолчанию +
  маршрутизация (нет рублёвого счёта МСП у UID → начисления авто в PAY/`wallets`);
  §5.2 балловый инвариант `wallets.balance = Σ earn − Σ spend + Σ adjustment/refund`;
  §5.3 принцип зеркального списания (обратный след: списание с того счёта, куда
  начисляли; минус-баланс = долг, блок выплат); §5.4 уровни сверки + реакция P1.
  Прежние детали раздела 5 (пример верификационного платежа, append-only сверки)
  выпали по воле владельца — история в git. Ссылки чек-листа §7 поправлены на §5.4,
  в п.6 добавлено «зеркальные списания по возвратам (§5.3)».
- Вечером того же дня — уточнение §5.1 (пуш `6898ae0`, ребейз поверх чужого
  `ded31f0` по бэклогу): UID = первичный идентификатор пользователя, номер карты
  LOVII PAY = идентификатор конкретного счёта, один UID → любое число счетов и карт;
  «универсальный счёт по умолчанию» и хвост про приоритетный интерфейс выпали по
  воле владельца.
- Вопрос владельца про идентификатор → полный разбор БД оформлен как артефакт:
  **`lovii_docs/artifacts/db-schema-analysis.md`** (пуш `d1ac71a`, CI 🟢, реестр
  REGISTRY обновлён; локальная копия из lovii-docs удалена — SSOT в artifacts) —
  все 85 таблиц трёх схем (public 55+PostGIS, lovii_b2b 14, lovii_admin 15) с
  колонками, ключами, точными числами строк и наблюдениями (дубль
  email_notification_log, деньги в копейках, таймзоны, логические FK без
  ограничений). В БД идентификатор = `users.id`, колонки `uid` нет; wallets →
  user_id, accounts → owner_type+owner_id.
- Попутно разблокирован CI lovii_docs: чужой коммит ded31f0 задублировал порог
  VIP «300 000 ₽/мес» в BACKLOG без allow-маркера (гард красил все пуши с
  22:25) — дубль переформулирован в `62a673b`; ранее §5.7 красил гард литералом
  «90%» — самоустранилось при замене раздела 5.

## 2026-09-12 — zcode: SZ-040 A-3/A-4 — API счетов + экран «Кошелёк» на staging

- `GET /balance` — карты: личный счёт + компании владельца (цепочка
  lovii_b2b.partner_users.core_user_id → partners.owner_user_id);
  `GET /balance/entries` — история своих счетов с фильтрами (счёт/роль/даты),
  чужие счета 404. `ledger_entries.order_id` + бэкфилл. Core `076b5ed`+`cc3a027`
  (CI ✓ после починки Carbon-типизации: фильтры дат строками + whereDate).
- App «Кошелёк» /profile/wallet (CI ✓, деплой ✓): карты с балансами и ИНН,
  история с лейблами по ролям; баллы отдельно на «Мои баллы». App `51e2d0a`.
- Live: API и экран проверены тестовым юзером; у владельца — Личный 1 143,78 ₽,
  АТМОСФЕРА 25 083 ₽, АКСИОМА 762,52 ₽ (+ черновик Grand без счёта).
- Исправлена моя ошибка привязки: partners.owner_user_id — это id b2b-пользователя
  (у владельца = 258), 263/265 возвращены на 258 (утренняя привязка на 51 цепляла
  чужой профиль РОЗА ВЕТРОВ).
- Грабли в session 027: CI-immutable Carbon, lovii_b2b в тестах через
  default-коннект, intval(массив)=1.

## 2026-09-11 (ночь) — zcode: SZ-040 «оператор = Аксиома» + чистый лист + структура AA2222

- Решения владельца: Аксиома (ИНН 7842223709) и есть оператор платформы —
  `OPERATOR_PARTNER_ID=265`, доля компании идёт на её счёт ИНН (merge eb98961,
  CI ✓); «МСП без промокода не бывает» — фолбэк на AA2222 Основателя (d9902f0).
- Структура: владелец = амбассадор ветки AA (roles:assign-ambassador) +
  представитель AA2222; партнёры 263/265 → owner_user_id 51; Пышки привязаны
  к AA2222 (partner_applications id=6, verified). Карта счетов: Личный /
  АТМОСФЕРА / АКСИОМА (3 карты, роли не плодят кошельки).
- Чистый лист: ledger/accounts/wallets/wallet_tx обнулены по просьбе владельца.
  Живая проверка после сброса: заказ #144 (130 ₽) → 263 +117 ₽ (90%),
  265 +3,55 ₽ (40% пула), user 51 +5,34 ₽ (реп+амб); Σ = чек − fee ✓.
- ИНЦИДЕНТ: ручной `docker compose up` без `-f docker-compose.prod.yml`
  уронил pgsql/redis/meili на staging (Networks: {} → DNS fail → API 500).
  Восстановлено force-recreate; правило — серверные команды сверять с CI
  (DC="docker compose -f docker-compose.prod.yml"). Пары заказов владельца
  (#142 не оплачен) можно дооплатить — контур посчитает.
- Дальше: A-3/A-4 (отчёты + экраны счетов: 3 карты в профиле).

## 2026-09-11 — zcode: SZ-040 — рублёвый ledger (A-1) и пул 40/40/20 (A-2) на staging

- Владелец: «го» после приёмки SZ-039. Спека: `lovii_docs/canon/TASKS/SZ-040-billing-ledger-pool.md`.
- Core `7245faf` (CI совмещённого пуша с SZ-038 фазой 2 — success, деплой ✓):
  `accounts` (user/partner/platform, копейки, row-lock) + `ledger_entries`
  (append-only, знаковые суммы, split-аналитика, unique-идемпотентность);
  `LedgerService::post` — проводка+баланс в одной транзакции.
- `DistributeOrderPoolAction` на CONFIRMED (после кэшбэка): точке 90% на счёт
  ИНН, платформе 10% − комиссия банка (карта 3,16%/СБП 0,7%/T-Pay 2,59%,
  мин 3,49 ₽; баллы → fee 0, решение №9), пул 40/40/20, остаток → Компания.
  Реп — по заявке точки с апрувом (SZ-038 фаза 1), амб — по префиксу ветки;
  без связки доля Компании. Гейт подписки — флаг до Б-3.
- Тесты: табличные канона + мин-fee + округление + идемпотентность + инвариант
  (8 новых, все зелёные; popутно фикс латентного бага PartnerApplicationFactory).
- Live-проверка: первый заказ после деплоя создаст проводки; роутинг реп/амб —
  на точке с SZ-038-связкой. Session: `lovii-core/docs/sessions/026-…`.

## 2026-09-11 — zcode: срез 59 (SZ-038 фаза 1 в staging) — роли rep/amb/msp в core

**Фаза 1 реализована и смержена в staging** (core PR #4, merge `8eca72e`,
CI+deploy 🟢, session 026):
- Промокоды представителей `PPXXXX` (BRD §3.1): `representative_promo_codes` +
  `RepresentativePromoService` (алфавит 33 без 0/1/I/O, смена кода по §3.5);
  artisan `roles:issue-promo` / `roles:assign-ambassador` (UI админки — бэклог).
- Амбассадоры: `ambassadors` (user + префикс ветки; иерархия rep→amb = префикс,
  дерева нет — SZ-034A §1.1).
- Заявка SZ-007: опц. `promo` → снимок `promo_code` + `representative_user_id` +
  статус `awaiting_rep_approval`; без промокода — старый путь (регресс-тест).
- Апрув/отклон репа: `/representative/approvals/{id}/verify|reject` →
  `invoice_issued`/`failed` + `partners.representative_approved_at` → витрина
  **teaser** (гейт SZ-035); слот опционален (в dev нет колонки).
- `merchants.tariff` (start|basic|pro, PARAMS §1.3).
- Read-API: `/representative/{profile,points,approvals}`,
  `/ambassador/{dashboard,reps,income,training}` (статика из config/roles.php),
  `/msp/{overview,payment}` (read-only, каталог в b2b; доходы — нули до фазы 3).
- 24 новых теста (Roles + PromoBinding); полный локальный прогон 948 passed,
  65 failed — все средовые (Kuper/Meilisearch/Payment/E2e).

**Грабли**: в core база контроллеров `Illuminate\Routing\Controller` (App-базы
нет); в docker-прогонах worktree чистить `bootstrap/cache/*.php` и задавать
тестовую БД env-ами явно (.env перекрывает phpunit.xml); CI-rector строже
локального. Работа шла в git-worktree `~/LOVII/lovii-core-sz038`
(параллельно SZ-039 в основном дереве — границу соблюдали).

**Дальше**: фаза 2 — lovii-app (переключатель роли, экраны rep по SZ-034B, amb,
msp-слой, чаты-заглушки). **Сумма счёта решена владельцем в тот же день: 1 ₽**
(PARAMS §5.1 обновлён; core PR #5 → staging, verification.amount = 100 коп. —
`/msp/payment` отдаёт `amount: 100`). Осталось 🔶: механика возврата
у Т-Банка (SZ-007 §7.1).

## 2026-09-11 — zcode: SZ-039 балловый контур MVP — начисления кэшбэка ЖИВЫ на staging

- Запрос владельца «контур для баллов — куда приходят начисления». Спека:
  `lovii_docs/canon/TASKS/SZ-039-points-accrual-mvp.md` (SZ-038 занят ролями
  Rep/Amb/MSP, параллельная сессия в lovii_docs — её unstaged файлы не трогал).
- Факт до задачи: каркас wallets/API был, но `earnFromOrder` нигде не вызван,
  `loyalty_rules` пуста, в enum было сгорание (Expiration) — против решения №8.
- Core `a0fa4c9` (staging `e2bd41c`, CI ✓): CONFIRMED вебхука Т-Банка → кэшбэк
  клиенту по правилу `loyalty_rules` (идемпотентно, банк ретраит вебхуки);
  REVERSED/REFUNDED → возврат начисления; Expiration удалён. Тесты: unit
  инвариант «баланс = знаковая сумма проводок» + e2e через fake-notify
  (двойной notify → одна earn).
- App `abd10d6` (staging, CI ✓, деплой ✓): экран «Мои баллы» /profile/balance
  (остаток, «1 балл = 1 ₽ · бессрочные», история с лейблами, empty/error),
  вход из профиля, testid `profile-balance-entry`.
- Ops: в staging `loyalty_rules` создано правило id=1 «Кэшбэк 5% (SZ-039)»
  (earn 5%, max_spend 100% — решение №9). SSH `deploy@89.19.223.16`.
- Вне MVP: P2P, вывод в деньги (Г-4), конструктор кэшбэка на точке + 25%,
  сверка В-4, b2b-экран правил.
- Приёмка владельцем: живой заказ с тестовой картой → «Мои баллы» +5%.
- Приёмка 1 (владелец): начисление РАБОТАЕТ (заказ #138, 510 ₽ → +25,5 б.,
  проводка и bonus_earned в БД), но экран показывал 0: бэк принимает только
  `per_page` (макс 48), app слал `page=1` → 422, и Promise.all ронял и баланс.
  Хотфикс app `e6e8b33` (staging, CI ✓, деплой ✓): page убран, запросы
  развязаны (баланс виден даже при сбое истории).
- Приёмка 2 (владелец): копейки показывались как целые баллы («2 550 б.»
  вместо 25,5) → фикс: сумма через formatPrice (F-022, баллы = рубли),
  app-коммит на staging, CI ✓.
- Приёмка 3 (владелец): **ПРИНЯТО** — «теперь сходится», провёл несколько
  заказов: 4 earn-проводки, баланс 146 836 коп. = 1468,36 б., инвариант
  «баланс = знаковая сумма проводок» сходится. **SZ-039 закрыта.**
- Session: `lovii-core/docs/sessions/025-sz039-points-accrual-mvp.md`.

## 2026-09-11 — zcode: срез 58 (SZ-038 открыта) — спека ролей проверена по системе

- Владелец принёс `lovii_docs/artifacts/roles-screens-spec.md` (экраны
  Rep/Amb/MSP, выведена из демо dash.js) — «берём на реализацию».
- Проверка тремя ресёрчами (core / app+b2b+demo / канон): UI 1:1 с демо,
  тарифы/ранги/цвета совпадают с каноном; НО 6 конфликтов: 1 ₽-платёж vs
  VER-* с р/с, коды REP-ГОРОД vs PPXXXX, «роли не пересекаются» vs BRD §2.4,
  витрина до платежа vs гейт SZ-035, формулы доходов = заглушки демо
  (views в системе нет), дубль rep-части с SZ-034B и msp-части с SZ-007 ф.2.
- Решения владельца: МСП в app = mobile-слой (каталог остаётся в b2b, не
  дублируем), чаты отложены (вкладки-заглушки).
- Сделано: artifact-спека скорректирована по месту (сводка правок в шапке
  файла); создан `canon/TASKS/SZ-038-roles-rep-amb-msp.md` (фаза 1 = core:
  роли совмещаемые, связка PPXXXX + апрув репа в машине SZ-007, тариф точки,
  read-API /representative|ambassador|msp); TASKS/README + STATUS обновлены.

**Дальше**: реализация SZ-038 фаза 1 в lovii-core (фича-ветка от staging).

## 2026-09-11 — zcode: срез 56 (SZ-030/031/032/033/036/037) — Live на staging

**Приёмки zcode** (все «Достаточно»):
- **SZ-030** (анимации): 3 опции (system/enabled/disabled), `data-motion` + глобальный гейт, `prefers-reduced-motion` — переключатель сохраняет выбор, живой тест на staging (3 сценария).
- **SZ-031** (версия): `staging·0854421` (git SHA из build-arg, не локальная dev), обновляется после каждого деплоя.
- **SZ-032** (статусы + кэш): статусы карточки (Pending/Отправлен/Принят/Выполнен), кнопка «Очистить кэш» работает (SW/caches + reload, token сохранён), честная ошибка `delivery_not_supported` в чекауте.
- **SZ-033** (спека пушей): MVP-приоритеты, архитектура, анти-дубль, анти-дроп — канон PUSH_NOTIFICATIONS_SPEC.md завершён.
- **SZ-036** (чекаут честный): не-пустой экран, `v-if="cart && checkout"` очищен, честная 422 при `delivery_not_supported` (проверено владельцем).
- **SZ-037** (мост b2b→core): варп-воркер guard + ENV + queue worker — сквозняк подтверждён (канбан + маркер идемпотентности), client push живой (нужен тест перед продом).

**Коммиты**:
- Core: `7de1b58` (EmitOrderStatusEventController)
- B2B: `5a67c14` (guard + ENV), `97649d4` (queue worker)
- App: `017329e+0854421` (версия + build-arg fix)

**Следующие по приоритету**:
- SZ-034 Phase B: представитель cabinet (дока создана zcode, владелец делает демо → Super Z делает UI + API)
- SZ-008/SZ-009: email (завершён)
- SZ-006: фото 404 + наполнение «Пышек»
- SZ-011: ревизия b2b-кабинета
- «Го» на прод + Яндекс-ключ app.lovii.ru

**SZ-034 Phase B (zcode — срез 57) — ЗАВЕРШЕНА (2026-09-11)**:
- Спецификация со сценариями: `lovii-docs/canon/TASKS/SZ-034-representative-cabinet-stage.md`
- Демо сделано владельцем с Sisyphus в lovii-demo: реп-дашборд в `js/dash.js`
  (вкладки Обзор / Подключение / Точки / Доход / Профиль / Чаты; фильтры точек,
  ранги Мэр/Губернатор, экономика 40/20, QR-код представителя, чаты с точками).
- Демо = эталон для реализации в lovii-app; отдельная задача-дока для Super Z
  НЕ создавалась — контур закрыт владельцем, реализация демо → по мере очереди.

- Собран по канону (BRD §7–9, FINANCIAL_CONTOUR v0.2, FINANCIAL_MODEL v2.4, SAGA-2,
  SZ-034 §1.1) и сверен с кодом/БД staging: T-Bank Init+вебхук УЖЕ работают
  (payments, deal_id в pull-модели не нужен), wallets = каркас баллового контура,
  partner_applications уже генерит verification_suffix; промокодов/подписок/
  балансов/выплат в БД нет.
- Модель владельца зафиксирована: два контура (рублёвый финансовый + балловый
  кэшбэк 1:1), один баланс на профиль (все роли), один на ИНН (разбивки —
  аналитика проводок), P2P-переводы кэшбэка, вывод по legal_status (физик —
  только через подписку представителя), баллы бессрочные (код правим — убрать
  expires_at), расчёты не зависят от способа оплаты (балловый чек = пул 10%),
  pull + дневной реестровый цикл (реестры в банк в 10:00; задержка = окно
  проверок), график выплат по канону BRD §8.4 (база «ежедневно»), сверка
  нулевой суммы обязательна.
- Файл: `lovii_docs/canon/FIN_BALANCE_REFERRAL_BRIEF.md` (запушен, 7d5342d);
  отложен в бэклог владельца (BACKLOG §6, decisions-needed #22–24). Владелец
  разберёт позже; при «го» — разложить в canon/TASKS как SZ-038+ (SZ-037 занят).

## 2026-09-11 (ночь, 3-я серия) — zcode: «Оплатить» на свежем заказе падал — SubmitOrderJob схлопывал окно оплаты; фикс b5d69f6 на staging

- Владелец на новом заказе #133 (Created) нажал «Оплатить» → 422 «Order 133
  is not awaiting payment». Причина: после починки очереди (b2b23c8)
  `SubmitOrderJob` уводит Created→Submitted через секунды после чекаута,
  а `CreateOrderPaymentAction` пускал только Created — окно оплаты стало
  неосуществимым (раньше «работало», потому что очередь была мертва).
- Фикс: оплата открыта для всех статусов до терминального исхода, закрыта
  на Completed/Cancelled/Failed — теперь UI (кнопка до самого оплаты) и
  бэкенд совпадают. Тест: Submitted → 201, Completed → order_not_payable
  (кейсу нужен terminal_key в конфиге — в CI .env нет). CI 🟢 → деплой;
  #133 (submitted) теперь оплачиваем.
- Напоминание про SZ-037: пуш клиенту из канбана b2b по-прежнему не ходит
  (мост не написан), проверка через отмену из app.

## 2026-09-11 (продолжение ночи) — zcode: пуш продавцу подтверждён владельцем; пуш клиенту из канбана не ходит = архитектурная дыра → SZ-037; «Очистить кэш» (SZ-027) задеплоена

- Владелец: «пуш пришёл, пуш по оформлению заказа пришёл» (SZ-010 работает
  end-to-end: подписка → заказ #132 → пуш продавцу). Но протащив заказы по
  всему канбану b2b, пуша клиенту не получил ни разу.
- Корень: `OrderResource::applyTransition` (b2b) пишет статус **напрямую в
  общую БД** — события core не диспатчатся, все листенеры (пуш SZ-010, email
  SZ-009, MAX/TG) молчат. Уведомления о статусах через канбан не работали
  никогда. Заведена **SZ-037** (мост b2b→core, P1, Super Z). Обход для
  проверки клиентского пуша: отмена заказа из app (client-cancel идёт через
  core API).
- «Оплатить» с ошибкой: `CreateOrderPaymentAction` принимает только заказ в
  `Created` — протащенный канбаном до `Completed` честно даёт 422 (текст в
  app generic — отметил в SZ-036/037 NB). Проверять оплату на новом заказе
  до смены статусов.
- **SZ-027 реализована и задеплоена** (app `0fdf130`, CI 🟢): в настройках
  секция «Приложение» → «Очистить кэш и обновить» (двойной тап-подтверждение,
  SW+caches, localStorage не трогается). Владельцу: обновить PWA — теперь
  кнопкой, не DevTools.

## 2026-09-11 (ночь) — zcode: SZ-010 пуши — сквозная отладка до доставки, канал ЖИВОЙ; попутно чинил staging, заваленный красными коммитами Super Z

- E2E с владельцем (iPhone PWA, user 51): подписка ✅ → заказ #131 создан →
  пуш НЕ пришёл. Две причины, обе найдены по логам и починены:
  1) **Очередь staging была мертва с 2026-09-06**: `config/horizon.php` знал
  только production/local, а `APP_ENV=staging` → Horizon-мастер стартовал
  «успешно», но **ни одного supervisor'а** — job'ы копились в pending
  (пуши, email SZ-009, SubmitOrderJob). Вскрылось при моём force-recreate
  (старый мастер жил в памяти со старым env). Фикс `b2b23c8`.
  2) **VAPID не попадал в WebPush**: `new WebPush($options, $auth)` — у v9
  сигнатура `(auth, options)`; FCM/APNs отвечали 401 «Authorization header
  must be specified». Регресс-тест `WebPushSenderTest` (Reflection по auth).
  Фикс `dcfdbcd`.
- Итог: прямой тест-пуш user 51 **доставлен** (оба job'а completed,
  error_count=0) — владелец подтверждает пуш на телефоне. Пуш «Новый заказ»
  к #131 не доехал (job'ы исчерпали ретраи до фикса), проверку повторить
  новым заказом. Подписок у владельца 2 (Apple + Chrome), обе живы.
- ⚠️ Super Z завалил CI staging двумя незавершёнными коммитами подряд:
  `83d54b5` (SZ-035: parse error в замыкании + вызов несуществующего
  `MerchantVisibility::enabled()`) — **развернут revert'ом** `ac1de4f`;
  `ef2c90e` (SZ-022 core: readonly-свойства вне конструктора = фатал на
  PHP 8.5 в диспетчерах push/email, тип возврата `rules()` с `In`,
  Rector HasFactory) — CI красный, в деплой не поехал. Super Z пушит без
  локального docker-гейта — второй раз за ночь (плюс Pint-фиксы zcode
  `6afda45` по его SZ-021).
- Из-за блокировки CI мой VAPID-фикс выехал **ручным деплоем** ac1de4f на
  сервер (по docs/DEPLOY.md); когда CI у Super Z позеленеет, деплой
  сбросится на origin/staging — ac1de4f там уже есть, ничего не потеряется.

## 2026-09-11 — zcode: живой тест пушей владельцем — чекаут «завис» = 422 delivery_not_supported (Пышки без доставки) → SZ-036

- Владелец на шаге «заказ в Пышки» получил пустой экран чекаута. Логи core:
  `POST /orders/preview` → 422 «Merchant Пышки & Пончики does not support
  delivery» (филиал 1024: `delivery_available=false`, `pickup_available=true`;
  чекаут стартует с «Доставка»). App показывает обманчивую строку «проверьте
  адрес» и пустой экран — выглядит как зависание.
- Разблокировка владельцу: тап «Самовывоз» на экране чекаута (preview
  перезагрузится) либо включить тумблер «Доставка» у филиала в b2b-staging
  (зона не нужна: без зоны fee = 0). Баг UX оформлен карточкой **SZ-036**
  (lovii_docs) — тексты по кодам ошибок, автопереключение режима, не пустой экран.

## 2026-09-11 — zcode: SZ-010 пуши — VAPID-ключи на staging установлены, канал активен, готов к проверке владельцем

- Запрос владельца «пуши готовы — как проверить?». Факт: код SZ-010 на staging
  (core `9b261ff`+ремонты zcode, app `62a5009`), но `GET /api/v1/push/public-key`
  отдавал `null` — VAPID-ключей в env стенда не было, сервис молчал по гарду.
- Сделано (ops, staging): `artisan push:generate-vapid` в контейнере →
  `PUSH_VAPID_*` в `/opt/lovii-core-staging/.env` (бэкап `.env.bak-push-20260911`)
  → `up -d --force-recreate app horizon scheduler` + `config:cache`. Проверено:
  ключ отдаётся, `push_subscriptions` есть (0 строк), warning в логе нет,
  Horizon healthy. ⚠️ Gotcha: `set -e`-скрипт по ssh оборвался после
  `horizon:terminate` — `up`/`config:cache` пришлось догонять отдельной командой;
  всегда сверять `docker ps` (Up N seconds) после recreate.
- Аудитория продавца проверена SQL: «Пышки & Пончики» (merchant 280, точка
  1024) → партнёр 263 АТМОСФЕРА → core user 51 владельца, owner, все точки.
  Тест-план владельцу: PWA app-staging на главный экран → Настройки →
  Уведомления → включить; заказ в Пышки → пуш продавцу; статус в b2b-staging
  «Готовится/Передан курьеру/Выполнен/Отменён» → пуш клиенту.
- Прямая тест-отправка (tinker в прод-образе НЕТ):
  `docker exec lovii-core-staging-app-1 php -r 'require "vendor/autoload.php";
  $app = require "bootstrap/app.php";
  $app->make(Illuminate\Contracts\Console\Kernel::class)->bootstrap();
  echo app(App\Infrastructure\Notification\PushNotificationDispatcher::class)
  ->pushToUserIds([51], new App\Infrastructure\Notification\DTO\PushPayload(
  title: "Тест ЛОВИ", body: "Пуши работают", url: "/profile"));'` → число
  job'ов = живых подписок (сейчас 0). Прод: ключи генерировать отдельно в
  `/opt/lovii-core/.env` перед деплоем. Карточка SZ-010 дополнена (lovii_docs).

## 2026-09-10 — zcode: выдана Super Z пачка SZ-025…028 — настройки приложения, волна 2

- Вслед за шитом настроек (session 022 app) рекомендации оформлены карточками
  в `lovii_docs/canon/TASKS/` (пуш `2dcbbbb`), в каждой — ожидаемый конечный
  результат и приёмка zcode:
  - **SZ-025** (P1, lovii-app): «Анимации» — Системные/Включены/Выключены,
    `data-motion` + глобальный CSS-гейт, live-следование prefers-reduced-motion;
  - **SZ-026** (P2, lovii-app): версия сборки (`__APP_VERSION__` из git в
    vite.config define) внизу шита, тап-копирование;
  - **SZ-027** (P2, lovii-app): «Очистить кэш» с подтверждением (caches+SW
    unregister+reload; localStorage не трогать) — самопомощь вместо DevTools;
  - **SZ-028** (P1, lovii_docs, код не пишется): ресёрч пуш-уведомлений PWA —
    матрица сред (iOS PWA 16.4+/Android/мини-аппы), бекенд-скелет (VAPID,
    подписки, очередь), связка с живыми бот-каналами MAX/TG, UX согласия →
    canon/PUSH_NOTIFICATIONS_RESEARCH.md + 🔶-решения.
- Язык/i18n осознанно НЕ завёл: второго языка нет, настройка была бы
  заглушкой — вернуться при реальном запросе.
- Очередь Super Z теперь: SZ-007 фаза 2 / SZ-011 / SZ-015–018 + новые
  SZ-025…028; приёмки пачки — zcode.

## 2026-09-10 — zcode: lovii-app — настройки приложения нижним шитом (тема с «Системной», размер текста) — live на staging

- Запрос владельца: настройки из профиля → кнопкой + нижняя панель как список
  адресов, эстетичнее, тема с системной + размер шрифта. Исполнено в app
  (ветка `feat/settings-sheet`, staging `32687f3`): строка «Настройки» в
  профиле открывает `SettingsSheet` (AppBottomSheet) — карточки темы с превью
  (Системная-сплит / Светлая-солнце / Тёмная-луна, канон-цвета #f8f5f0 /
  #171219) и 4 размера «Aa»; выбор применяется немедленно.
- Ядро: `colorModeChoice` (system/light/dark); «Системная» = 'auto' vueuse —
  живое следование prefers-color-scheme; зеркало для анти-FOUC инлайна
  index.html сохранено, миграция старых ключей (Task 90) — выбор тест-покупателей
  не теряется. Тумблер в шапке пишет через тот же `writeThemeChoice`.
- Гейты: `yarn test` 🟢 (281 тест; новые SettingsSheet + color-mode, B5-тест
  профиля переписан), живой прогон в IAB 🟢 (темы/шрифт/перезагрузка/свайп).
  Session-док: `lovii-app/docs/sessions/022-settings-sheet.md`.
- Владельцу (рекомендации из лучших практик, НЕ реализовано): анимации
  (prefers-reduced-motion) — следующий кандидат; уведомления — когда будут
  пуши; язык — когда будет EN; версия сборки — завести VITE_APP_VERSION;
  «очистить кэш» — отдельно и осознанно (PWA). Приёмка глазами:
  app-staging (сбросить SW/кэш!) → Профиль → «Настройки».

## 2026-09-10 — zcode: SZ-014 приёмка-1 «Достаточно» + дополнение владельца (аудитории ×4, бизнес-кейс, дорожная карта)

- Super Z исполнил волну 1 (6ebe42c, срез 53): `canon/MINI_APPS_RESEARCH.md`
  v1.0.0 — все 3 платформы × 10 осей; валидация TG и MAX — одна схема
  HMAC/WebAppData (подтверждено дословно по офиц. докам), VK — HMAC vk_*
  (вторично; ⚠️ sign=base64 vs hex — учесть в спеке валидатора M-2); тихая
  связка «user бота ↔ телефон» подтверждена на всех трёх; MAX — только внутри
  ботов + лицензионный договор business.max.ru; платежи хостов — НЕ брать.
  Находки: TG paid-broadcast ≥10k Stars+MAU; TG OIDC scope `phone` — кандидат
  для входа PWA.
- Приёмка-1 zcode «Достаточно» (f32a4e8, срез 54) + ДОПОЛНЕНИЕ владельца:
  SZ-014 возвращена Super Z за волной 2 (v1.1.0): §9 карта «аудитория ×
  платформа × сценарий» для ВСЕХ типов клиентов (клиенты / МСП /
  представители / амбассадоры — система распространения), §10 бизнес-кейс
  волн, §11 конструкция внедрения — дорожная карта этапов.
- Оценка zcode (владельцу): включаться — да, но по аудиториям: ценность
  СЕЙЧАС — мини-апп представителя/амбассадора («подключить точку» + атрибуция
  start_param → сплиты 40/40/20); клиентская витрина — после наполнения
  ассортимента; МСП — после первой живой точки. Волна 1 MAX+TG дешёвая
  (адаптер + валидатор, 0 новых объектов в мессенджерах), VK — после
  модерации.

## 2026-09-10 — zcode: SZ-014 выдана Super Z — ресёрч мини-приложений VK / MAX / Telegram

- Запрос владельца: три дефолтных источника трафика (VK, MAX, TG) → глубокий
  ресёрч мини-аппов каждой платформы: публикация/модерация, попадание
  пользователей, авторизация (валидация launch-параметров, телефон, тихая
  связка «user бота ↔ телефон» через уже живых OTP-ботов), базовые/уникальные
  возможности, уведомления после визита, платежи, реалии РФ.
- Карточка: `lovii_docs/canon/TASKS/SZ-014-mini-apps-vk-max-telegram-research.md`
  (P1, код не пишется, объекты во внешних UI не создаём — кнопки владельца).
  Артефакт: канон-док `canon/MINI_APPS_RESEARCH.md` + матрица + рекомендация
  MVP + 🔶-список. BACKLOG: трек M открыт (M-1), срез 52 в STATUS. Коммит
  lovii_docs `367a842`.

## 2026-09-10 (ночь) — zcode: батч-приёмка (6 задач) + ops-фикс поиска + гейт верификации партнёров (I-5) — live на staging

- БАТЧ-ПРИЁМКА (WORK_PROTOCOL §3.1, все «Достаточно»): T-005/006/007 (трек U),
  SZ-003, SZ-004 §2.2–2.4, SZ-006 §1, SZ-008 — факт-чек по коду staging,
  секции приёмки в карточках; закрытие коллегиально — после приёмки-2 Super Z.
- OPS: T-7 (500 в /v1/search/offers?branch_id=) закрыт — это
  `scout:sync-index-settings` (выполнен на staging; branch 1024 → 200,
  контроль на 108 → 96 позиций). Находка F-039: в SZ-005 §6.5 «модель A
  выбрана» против 🔶 в шапке — V1 не начинать до явного решения владельца.
- I-5 (трек I): гейт витрины/поиска/заказов по `verified_at` партнёра —
  core `56db2ef` (PR #2, CI 🟢, staging live), session-док 023. Флаг
  `STOREFRONT_VERIFICATION_GATE=true` включён на staging; Kuper-витрина
  сохранена (исключение «импортирован интеграцией»), неверифицированные
  тестовые мерчанты отдают 404. 🔶 владельцу: UX скрытия (плашка vs 404).
  Попутно починен пинт в файлах Super Z (их `7aa2edc` упал на CI — теперь
  их SZ-006 F-6 тоже задеплоен).

## 2026-09-09 (вечер) — zcode: платёжный UX покупателя + fake-режим банка — ПОЛНЫЙ СЦЕНАРИЙ на staging

- По согласованному плану (новая вкладка банка; кнопка оплаты на экране успеха;
  заглушка-fake-терминал) реализовано: core (реюз живого платежа, `{order_id}` в
  SuccessURL/FailURL, `PaymentProviderClient`+`FakeTBankClient` под
  `PAYMENTS_FAKE`, dev-роут `/payments/fake/notify` гоняющий РЕАЛЬНУЮ вебхук-
  логику, бейджи «Ожидает оплаты»/«Оплачено», `payment_enabled` в API) + app
  (шаг оплаты на экране успеха, `/payment/result` с поллингом, `/payment/fake`,
  блок «Оплатить N ₽» в заказе; session-доки: core 021, app 020).
- e2e на staging: scenario-03 (оплата успех/отказ) 2/2 🟢; scenario-02 падает на
  известном OTP-429 (аудит, зона супер-агента). Локальный e2e-target исправлен
  на порт 80 (`:3000` не существует).
- Staging env: `PAYMENTS_FAKE=true`, SUCCESS/FAIL URL на app-staging.
  **Когда банк включит WL: `PAYMENTS_FAKE=false` — тот же UI против настоящей
  формы Т-Банка, живой тест по чек-листу спеки §5.1.**
- Владелец может прогнать сценарий руками: app-staging (сбросить SW/кэш PWA!) →
  корзина → оформление → «Оплатить» → заглушка → «Успешно оплатить» → «Заказ
  оплачен» → заказ `submitted`, бейдж «Оплачено».

## 2026-09-09 (день) — zcode: Т-Банк платежи фаза 1 — платёжное ядро на staging, включено

- Реализовано в lovii-core (мерж `5e96a38`, CI 🟢, задеплоено): таблица `payments`,
  TBankSignature/Token, TBankClient (Init/Confirm/Cancel/GetState), `POST /orders/{id}/payment`
  (двухстадийный Init, deal NN, recipient=merchant id до регистрации точек), вебхук
  `POST /payments/tbank/webhook` (подпись + авторитетный GetState, ответ строго OK),
  CONFIRMED → заказ created→submitted. Гейт `PAYMENTS_ENABLED`. Спека:
  `lovii-core/docs/specs/tbank-payments.md`, session 020. Тесты 807 passed.
- Staging env добавлен (PAYMENTS_ENABLED=true, тестовый терминал 1788865566220,
  api rest-api-test.tinkoff.ru/v2, notification URL) — контейнеры пересозданы через
  `docker compose -f docker-compose.prod.yml -p lovii-core-staging up -d`, config:cache OK.
- ⚠️ Gotcha (чуть не положил staging): в /opt/lovii-core-staging НЕЛЬЗЯ голый
  `docker compose up` — подхватывается dev docker-compose.yml, который пытается
  пересоздать внешнюю сеть lovii-core-staging_default (на ней сидят b2b/admin/pollers).
  Только `-f docker-compose.prod.yml -p lovii-core-staging`.
- Живой тест: вебхук отвечает OK (200); Init к rest-api-test.tinkoff.ru из контейнера
  даёт 403 nginx = НУЦ-CA в контейнере ок, но IP НЕ в whitelist банка.
  **Блокер живого теста — письмо acq_help@tbank.ru — текст отправлен владельцу.**
  ⚠️ У сервера 2 IP: `89.19.223.16` входящий (домены) и `89.19.223.31` ИСХОДЯЩИЙ —
  банк видит `.31` (проверено ifconfig.me ×3, стабильно). Staging+prod на одном
  сервере → WL один на оба контура. В спеке/сессии 020 исправлено (`9c24c90`).
- Дальше: WL от банка → живой Init с заказа; UI оплаты в app; фаза 2 (точки acqapi+mTLS,
  выплаты, Confirm по передаче + I-1 фискализация, возвраты).

## 2026-09-09 (день) — zcode: MCP-серверы воркспейса — Context7 + PostgreSQL staging (read-only)

- Владелец попросил собрать MCP. Конфиг: `LOVII/.zcode/config.json` (workspace-scope, инструменты
  появляются после перезапуска сессии). Пакеты ставятся в `~/.zcode/mcp-servers/<name>` + абсолютный
  путь node (GUI-процесс не наследует shell-PATH, npx-шим ненадёжен).
- `context7` — свежие доки Laravel/Filament/Vue (без ключей).
- `lovii-pg-staging` — read-only SQL к staging-БД (запрошено владельцем: нужно Т-Банк-сессии и для
  его вопросов по базе). Роль `lovii_mcp_ro` (SELECT на public/lovii_b2b/lovii_admin + default
  privileges на будущие таблицы; 76 таблиц) в `lovii-core-staging-pgsql-1` (PG 17.7, БД `lovii-core`;
  стейдж-env: DB_USERNAME=lovii-core, НЕ `lovii` из старой справки «доступ к серверу»). Порт контейнера
  наружу не открыт — постоянный SSH-туннель launchd `ru.lovii.pg-tunnel`: 127.0.0.1:15433 → контейнер:5432,
  IP резолвится при каждом старте (переживает recreate). Пароль только в `LOVII/.zcode/config.json`
  (chmod 600, не гит; в память/git не класть). Прод не тронут.
- Проверено probe по протоколу MCP: initialize → tools/list (`query`, «Run a read-only SQL query») →
  SELECT = 76 таблиц; DELETE ролью — permission denied (двойная защита: роль + сам сервер).
- Вопросы владельца по БД (из decisions-needed: «номер офиса» — поля нет; КБЖУ/нутриенты; и т.п.) —
  теперь любая сессия может сама глянуть в данные, вопрос владельцу не нужен.

## 2026-09-09 (день) — zcode: терминал Т-Банк готов — ключи тест+бой забраны, доки Мультирасчётов скачаны

- Владелец получил письмо «Терминал готов к работе» (`/Users/best/LOVII/Терминал Тбанк.md`,
  содержит ссылку NextCloud+пароль — СЕКРЕТ, в git не класть). Данные терминала забраны:
  тест (оплатный 1788865566220 / выплатный 1788865566045) и бой (1788867330861 / 1788867330886)
  + логины кабинета LOVII → `lovii-docs/tbank/terminal-keys-test-and-prod.md` (нелокально-гит).
- Доки скачаны в `lovii-docs/tbank/`: оплата (EACQ Мультирасчёты v1.13), выплаты,
  регистрация/обновление точек партнёров, описание продукта. МСС 7372.
- Открыто у банка: лимиты (вопрос 3 без ответа), ЭДО-подписание (тестовый контур «с данными
  после подписания», но ключи уже выданы). Выплаты: ЮЛ «перечисление третьему лицу» 0,5%;
  ФЛ/самозанятые — СБП или карта (1%, мин. 30 ₽).
- План: интеграция в lovii-core (Трек I бэклога; новых репо не нужно): регистрация точек API →
  Init/нотификации/статусы на тестовых ключах → прод-ключи при прод-деплое.

## 2026-09-09 (ночь) — zcode: цикл пары переведён на 10 минут; SZ-001 Super Z выполнил — мне T-002–T-004

- Владелец: час — редко, сверяться каждые 10 минут. Мой cron пересоздан `*/10`
  (id automation-9a21a059…), WORK_PROTOCOL → v1.2.1 (цикл обеих сторон, большие
  задачи по шагу за цикл с фиксацией прогресса).
- Для Super Z подготовлена инструкция планировщику: `lovii-docs/superz-schedule-setup.md`
  (владельцу вставить в его инструмент — со своей стороны я его сессии планировать не могу).
- Super Z тем временем ВЫПОЛНИЛ SZ-001: спека VK §7 в otp-delivery-channels.md
  (core `a2f62ed`+`465a2e3`, docs-only), рекомендация Callback API (F-030 был
  Telegram-специфичен), F-038 (миграция unique(bot,phone)), создал **T-002 (core-реализация),
  T-003 (клиент vk_link_required), T-004 (staging env+live)** — подхват в ближайших циклах,
  как и приёмка SZ-001 (приёмка-1 — моя).
- Gotcha: один маркер конфликта уехал в main при спешке — пойман, убран (`8bca7bd`);
  правило в память: грепать маркеры до коммита.

## 2026-09-09 (ночь) — zcode: парный режим запущен — WORK_PROTOCOL v1.2.0 + почасовой автоцикл

- Владелец утвердил работу «на троих» максимально автономно. WORK_PROTOCOL → v1.2.0
  (`6434daf`, lovii_docs): zcode — серверная зона + ко-приоритизация бэклога + приёмка
  работ Super Z + постановка SZ-NNN; Super Z — кодинг/ресёрч; §3.1: выдача задачи не
  блокирует (файл в TASKS/ = выдана, владелец с вето), коллегиальная приёмка
  «Достаточно / Доработка» обеими сторонами (спор 2+ кругов → владелец).
- BACKLOG §6: раздел «Очередь владельца» (прод-деплои app/core, хедер-канон, офис,
  SMS, VK-кнопки, §6 мелочей) — всё, что требует человека, зеркалится в
  decisions-needed.md.
- **Почасовой cron-автомат** (каждый час :13): синк репо → чтение канона → исполнение
  zcode-задач → приёмка Super Z → ко-приоритизация → очередь владельца; нет изменений —
  тихий выход. При коллизии пушей с Super Z уступает пушащий позже (перенумерация себя).
  Срез 32 (коллизия с его задачей 31: он зачистил мёртвый whatsapp-ключ `fdd1867` и
  заархивировал T-001; его NB о моём `4d544ed` без session-дока — принято, прикрыто
  его session 018).

## 2026-09-09 (ночь) — zcode: решение владельца «скрыть заглушки» внедрено + SZ-001 (VK-изыскание) поставлена Super Z

- Владелец подтвердил: активных каналов два (Telegram, MAX); все заглушки скрыть,
  в коде — комменты «реализация по приоритизации бэклога».
- Реализовано: `feat/hide-stub-otp-channels` → `4d544ed` в staging core
  (`AllChannelsStrategy` = max+telegram; `config/otp.php` комментарии; тест списка
  переписан). Локально pest 761 passed, pint/rector/phpstan чисто (поднимал
  OrbStack+docker-стек); staging CI 🟢; live-ответ request-code — только max+telegram.
- Канон lovii_docs (`3841ec1`, fact-guard 🟢): F-037 → Fixed, O-1 закрыта, T-001 →
  «Закрыта решением владельца», BACKLOG трек V (VK), срез задача 30.
- **SZ-001 → Super Z** (первая задача в обратную сторону): изыскание и полная спека
  VK-канала (сообщения сообщества `vk.ru/loviiru`): механика VK API, Callback vs
  Long Poll (учёт F-030), фаза в otp-delivery-channels.md, T-задачи для zcode,
  чек-лист владельца с точками «нажать кнопку». Секреты (токен, строка подтверждения)
  — в `OTP codes VK .md` воркспейса, в канон не копируются.

## 2026-09-09 (ночь) — zcode: T-001 по протоколу Super Z → ВЕРНУТА владельцу (§2.4, премиса не подтвердилась)

- Первая задача по новому циклу (lovii_docs `canon/TASKS/T-001-otp-coming-soon-defaults.md`).
  Спека: дефолт `coming_soon.whatsapp` false→true в core `config/otp.php` + тест флагов.
- При сверке с кодом премиса O-1 НЕ подтвердилась: WhatsApp скрыт из списка каналов
  намеренно (`AllChannelsStrategy`, коммит `f9b1955` «hide WhatsApp channel»), клиент его
  не показывает; дефолт — мёртвый код (контроллер читает конфиг только для каналов из
  списка); эндпоинта `GET /auth/otp/request` из спеки нет (реальный — `POST /auth/request-code`).
- По WORK_PROTOCOL §2.4: код lovii-core НЕ менялся, пушей нет. Зафиксировано в каноне:
  FINDINGS **F-037** («Чинить (ZCode)», варианты решения владельцу: оставить как есть /
  вернуть канал с «Скоро» / зачистить конфиг-ключ), T-001 → «Возвращена владельцу»,
  пометка у O-1 в BACKLOG, срез STATUS.md (задача 28). CI lovii_docs 🟢 (`647d4d9`).
- При пуше была коллизия с параллельным пушем Super Z (обе стороны взяли F-036/задачу 27):
  разрешено ребейзом — его F-036 остался, мой → F-037.
- Клон `lovii_docs` создан в воркспейсе (`/Users/best/LOVII/lovii_docs`) — рабочая копия
  для задач по протоколу.

## 2026-09-09 — аудит-агент: полная проверка стенда + правило фиксации работы в AGENTS.md всех репо

- Проверка по запросу владельца «всё ли в порядке»: рассинхрона нет (loc = origin,
  сервер = origin/staging по core+app), CI зелёный, стенды живы, все контейнеры healthy.
- OTP ходят: живой тест 21:00 08.09 — Telegram-код доставлен на номер владельца (200 OK).
  В логах успешные доставки и утром 08.09.
- Найдено: e2e-сценарий 2 падает на staging (устаревший хелпер: кликает первый канал —
  теперь MAX; API-шаг + UI-шаг ловят троттлинг 429 через 5 сек). Продукт работает верно
  (throttle + ошибка «Не удалось отправить код» — новый корректный UX). Чинить хелпер
  (зона супер-агента, сессии 014–018): переиспользовать код API-шага или ждать окно.
- Спорадические 503 от исходящего прокси у Telegram-поллеров (раз в пару часов,
  поллер переживает ретраем) — наблюдать.
- **Новое правило**: в `<lovii-platform-overview>` всех 4 репо добавлен раздел
  «Фиксация работы» (session-док + запись в status.md + decisions-needed.md в том же
  пуш-цикле, что код). Коммиты `docs(agents): …` в core/app (staging), b2b/admin (master).
  Цель: супер-агент актуализирует доки сразу после правок, владелец и другие агенты
  видят актуальное состояние.

## 2026-09-08 (ночь) — core: MAX доставка кодов РАБОТАЕТ (корень: user_id в query, ЗАВЕРШЕНО)

- Приёмка владельцем: ✅ «всё ок» — код приходит в MAX, тестовые сообщения/коды
  доставлены. Финальный вход на app-staging подтверждён.
- Следующие задачи: супер-агент получил блок «безопасность + другие боты MAX»;
  следующая фаза доставки — **Telegram** (спека `otp-delivery-channels.md` §5,
  читать перед стартом; нужны `TELEGRAM_BOT_TOKEN/BOT_USERNAME/WEBHOOK_SECRET` от владельца).
- Все 400 «Unknown recipient» объяснялись адресацией: MAX Bot API шлёт в личный
  диалог через `POST /messages?user_id={user_id}` (query), а `chat_id` в теле —
  только для чатов/каналов. Транспорт слал chat_id → MAX не мог найти получателя.
- Фикс `feat/max-messages-user-id` (`9d5eba3`+`010c143`, CI зелёный): user_id из
  MaxBotLink/вебхук-апдейта в query, chat_id фолбэк; тесты под адресацию (713 passed).
- Live: `send-code` (+79119287478, max) → `max_link_required: false`, код 4-значный
  ушёл в MAX (лог чист, код в дев-эндпоинте). Бот «Лови» доставил впервые за сессию.
- По пути: `LOG_LEVEL=info` на staging (был error — резал INFO и слепил диагностику),
  логирование сырого тела вебхуков. Доки: core sessions 009–011.
- Прод: env+вебхук+`OTP_DEV_BYPASS=false`; пометить в «Боты Макс.txt», что «Лови» = второй токен.

## 2026-09-07 (вечер) — core: MAX Bot бот «Лови» на staging + фикс протухшего чата (ГОТОВО К ЖИВОМУ ТЕСТУ)

- env: `OTP_MAX_TRANSPORT=bot_api`, `MAX_BOT_TOKEN/USERNAME/WEBHOOK_SECRET`; вебхук
  зарегистрирован (bot_started + message_created). ⚠️ В «Боты Макс.txt» подписи токенов
  перепутаны: «Лови» = второй токен (проверено `GET /me`).
- TLS: platform-api2.max.ru — сертификат НУЦ Минцифры; корень добавлен в образ
  (`docker/ca/`, `feat/max-bot-ru-ca`), иначе cURL error 60.
- Баг: при существующей привязке send-code падал в 500 (MAX: 400 «Unknown recipient» —
  диалог бота протухает). Фикс `feat/fix-max-stale-chat` (деплой зелёный): 4xx →
  `MaxLinkRequiredException` → ответ `max_link_required` + свежий диплинк; вебхук
  подтверждает ok даже при недоставке. Lived-тест: 200 вместо 500.
- Ожидается: владелец жмёт «Открыть MAX»/диплинк → «Начать» → бот шлёт код + кнопку
  «Поделиться контактом» → вход без кода. Доки: core sessions 009–010.

## 2026-09-07 (день) — lovii-app: фиксы UI-аудита 012 (F-015–F-027, на staging)

- F-015: адрес без id авто-сохраняется перед чекаутом; ошибки API инлайн. Полный
  флоу гостя до заказа проверен на yarn dev.
- F-016: первый поиск immediate; F-017: фолбэк DEFAULT_POINT + плашка в /stores и
  /popular; F-019: пин геомодуля — стили в общий бандл; F-020: гидратация без
  самопроизвольного саджеста; F-022: цены «9 800,00» ru-формат; F-023: скрыта
  «Заказ от 0.00»; F-024: фейковый КБЖУ убран; F-025: «Скоро» вместо мёртвых ссылок;
  F-026: фолбэк лого; F-027: тексты/бейджи.
- F-018/F-021 — ждут текст Политики ПД от владельца; F-028 — QA-долг.
- П.7 аудита устарел: coming_soon в /auth/request-code уже отдаётся (внедрено ранее),
  бейджи рендерятся.
- Статусы по карточкам: docs/sessions/012-ui-audit-staging.md §10. e2e 6/6, yarn test 190.
## 2026-09-07 — OTP-каналы: спека бесплатной доставки + «Скоро» (на staging)

- Спека lovii-core/docs/specs/otp-delivery-channels.md — план бесплатной доставки кодов:
  Фаза 1 MAX Bot API, Фаза 2 Telegram deep-link, Фаза 3 WhatsApp (Meta Cloud API,
  комплаентный платный / wa.me обход — решение владельца), Фаза 4 VK.
- Core: /auth/otp/request отдаёт coming_soon per-канал; app: бейдж «Скоро»
  (sms/вк/звонок помечены).
- Владельцу для старта Фаз: создать ботов (MAX @MasterBot, Telegram @BotFather),
  выдать токены (env MAX_BOT_TOKEN / TELEGRAM_BOT_TOKEN).
- Инструкция по входу на staging — lovii-docs/auth-staging.md (1234 работает).
## 2026-09-07 (утро) — lovii-app: адреса по фидбеку (карточка/удаление/ручной ввод)

- Список адресов: карточка целиком = выбор (+ «Текущий адрес доставки»), карандаш =
  сразу редактирование; шестерёнка/AddressActions убраны.
- Удаление — двухшаговое в форме редактирования; при удалении текущего адреса доставка
  очищается (новый экшен systemStore.clearDeliveryAddress).
- Ручной ввод улицы/города починен (инпуты затирали текст до выбора подсказки);
  «Определить автоматически» в обеих формах + сохранение пробует авто-определение.
- Lead главной — по тексту владельца. e2e 6/6, yarn test 190 зелёный.
- Сессия-док: 011 (продолжение).
## 2026-09-07 (ночь) — lovii-app: адресный флоу по фидбеку владельца (на staging)

- Плашка адреса: список сохранённых адресов (тап = текущий, карандаш = редактирование,
  «+ Добавить адрес»); кнопки «Показать на карте» нет (гостю — ссылка на карту).
- Карта: AddressPointMap встроен в формы создания/редактирования — открытие zoom 18
  «в упор», регулятор ±, пин-фокус; перетаскивание → реверс-геокодинг → поля формы.
- Найден и починен корень невидимой метки: слот YandexMapMarker не монтирует DOM —
  пин нарисован глобальным CSS на узле ymaps3--marker.
- БД: доп. поля адреса (подъезд/этаж/квартира/домофон/комментарий) уже есть в
  user_addresses и сохраняются формами; поля «офис» нет — решение за владельцем.
- Локальное превью: .env → staging API, yarn dev показывает данные (проверено до коммита).
- Сессия-док: 011-canon-demo-alignment.md (продолжение в конце файла).
## 2026-09-06 (глубокая ночь) — lovii-app: возврат к канону демо (ЗАВЕРШЕНО, на staging)

- Ветка `feat/visual-audit`, мержи 48a8090/fca94fd, сессия-док 011-canon-demo-alignment.md.
- Хедер по живому демо: inner-контейнер 924px (не по краям), полный лого всегда,
  пилюля с частью адреса (76/64px), тап → плашка «Адрес доставки» с полным адресом
  → «Показать на карте». Подшапка — full-bleed + inner.
- Профиль-редактирование: канон .prof-head (градиентная карточка), секции r24 с кикерами.
- Локальное превью починено: .env → VITE_BACKEND_URL=api-staging (git-ignored),
  yarn dev показывает реальные данные.
- В playground демо согласован вариант хедера V11 «Correct» (44px/аватар) — НЕ внедрял,
  ждёт решения владельца (живой демо-хедер другой).
- e2e 6/6 (обновлены под плашку адреса); на прод не выводилось.
# Статус платформы — «что сейчас в работе»

> Каждый агент в начале задачи читает этот файл, в конце — дописывает свой блок сверху.

## 2026-09-06 (вечер) — lovii-app: типографика канона + масштаб текста (ЗАВЕРШЕНО, на staging)

- Ветка `feat/typography-visual` → `staging` (мерж `f87d63e`), сессия-док:
  `lovii-app/docs/sessions/009-typography-font-scale.md`.
- Типографика выровнена с каноном ДС (заголовки страниц 20/800, пилюля адреса 14/500,
  Inter-стек `--lv-font`, antialiased); все миксины умножаются на `--font-scale`.
- В профиле появилась настройка «Размер текста» (Мелкий/Обычный/Крупный/Большой),
  дефолт «Крупный» (1.1) по решению владельца; localStorage, без бекенда.
- Хедер/подшапка по канону lovii.css: sticky glass, лого 32, пилюля 76px-лейбл,
  подшапка 48px с заголовком 20/800.
- Приёмка: `yarn test` зелёный; e2e staging 6/6; визуальный обход обеих тем.
- Известный край: PWA- SW держит старый бандл до активации нового (проверять стенд
  после сброса SW/caches или повторного reload).

## 2026-09-06 (день) — lovii-app: единый хедер сайта (ЗАВЕРШЕНО, на staging)

- `AppHeader` (лого, адрес, тема, корзина) на всех страницах через `MainLayout`;
  герой главной → `HomeHero`; `AppPageHeader` заменил 8 самодельных back-строк;
  дубли адресных пилюль убраны (popular/catalog/store). Сессия-док: 008-app-header.md.
- e2e staging зелёные; прод ждёт ручного CI-запуска владельцем (как и 008, так и 009).

## 2026-09-06 (ночь) — lovii-app: аудит экранов staging (ЗАВЕРШЕНО, на staging)

- Ветка `feat/visual-audit`, мержи e26038e/9616a3e/ec48219, сессия-док 010-visual-audit.md.
- «Ездящий» хедер/навигация на iOS = WebKit-баг backdrop-filter на fixed/sticky — стекло
  перенесено на ::before (совместно с параллельной сессией).
- Адрес виден полностью: герой «Твой район: …» (2 строки), пилюля «улица, дом» + рондель
  на ≤400px; гео-кнопка на карте всегда видна; лого = домой + обновление (QA).
- «Твоя точка» → «Твой район»; дубль «Получатель» убран; дистанция «6,7 км»; OTP теперь
  настоящий 6-значный (код для тестов: GET /api/v1/dev/otp/last-code?phone=…).
- e2e 6/6; на прод не выводилось — ждёт приёмки владельцем.
