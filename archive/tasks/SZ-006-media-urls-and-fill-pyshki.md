> ⚠️ **АРХИВ.** Карточка закрыта и не является текущим источником требований; сохранена только для истории.

# SZ-006 — Фото не видны на витрине (media-URL) + наполнить «Пышки & Пончики» целиком

> Статус: **ЗАКРЫТА (коллегиально, 2026-09-11)**: §1+§2 — zcode «Достаточно» (§6); визуальная приёмка владельца: «пышки пока все ок» (§8).
> на филиале 1024, заведено Super Z через экраны b2b); F-6 кэш категорий —
> фикс 7aa2edc в staging, cache:clear на сервере выполнен zcode (2026-09-10,
> витрина 200: 24 поз./стр, 21 с фото, 6 категорий). Приёмка zcode —
> «Достаточно». Финальная визуальная приёмка — владельцем.
> РАЗБЛОКИРОВАНО zcode 2026-09-10: материалы скопированы в общий воркспейс,
> пути ниже обновлены. Дополнительно: регресс §2 — int в PositionResource::media
> (вторая точка SZ-003) исправлен hotfix `8610525` (zcode).
> Приоритет: **P1** — витрина партнёра без картинок; цикл МСП не пройти
> Источник: владелец («загрузил фотки — ни одной не видно»; «сам заполни все
> товары, подгрузи картинки, покрыть тестами»)
> Исполнитель: Super Z · Репо: `lovii-b2b` (+ core/app только если потребует
> URL-стратегия) · Гейт: composer test
> Диагноз zcode: факты проверены на staging 2026-09-09 (см. §1)

## 1. Бага: загруженные фото не видны на витрине — 404

Факты (проверено):

- Владелец загрузил лого/фото через b2b. В БД записан **относительный ключ без
  домена**: `merchants.logo_url = merchants/01M23J10C7V6EZZ1N135DPWYJE.png`
  (файлы физически есть: `lovii-b2b-staging-app-1:storage/app/public/merchants/`).
- `SquareImageUpload` пишет на диск `'public'` b2b; b2b отдаёт файлы публично:
  `https://b2b-staging.lovii.ru/storage/merchants/01M2….png` → **200**.
- Витрина (app) рендерит `logo_url` как есть → браузер резолвит ключ против
  домена app → `https://app-staging.lovii.ru/merchants/01M2….png` → **404**.
  У Kuper-мерчантов в этих полях полные https-URL, поэтому они частично работали.

**Фикс (минимальный и консистентный):** b2b при сохранении медиа записывает
**полный публичный URL** (`Storage::disk('public')->url(...)` с APP_URL b2b:
`https://b2b-staging.lovii.ru/storage/...`; на prod — b2b.lovii.ru) — точки
входа: `SquareImageUpload::context()` (`saveUploadedFileUsing` → MediaStorage)
и `PositionResource::media`. Плюс:

- миграция-бэкфилл: все существующие относительные ключи (merchants.logo_url /
  mini_logo_url / cover_url, картинки категорий, фото позиций) переписать в
  полные URL; отчёт: сколько переписано;
- проверить, что `HybridMediaPreview` корректно отображает и полные URL, и
  старые ключи (после бэкфилла — только URL);
- public-роут `/storage/` b2b остаётся публичным (это ок для витринных медиа).

## 2. Наполнить витрину «Пышки & Пончики» целиком (за владельца)

Владелец подготовил материалы (эта машина):

- **Меню и пакет текстов:** `/Users/best/LOVII/lovii-docs/pyshki-materials/пышки-пончики.md`
  (и дубль `.txt`) — описание витрины, категории, позиции с ценами/описаниями,
  концепция. Структурировать по документу.
- **Фотографии позиций:** `/Users/best/LOVII/lovii-docs/pyshki-materials/photos/` (10 PNG).
- **Флаеры/обложки:** `/Users/best/LOVII/lovii-docs/pyshki-materials/Пиццерия_Флаеры_21-9_*.png`
  (кандидаты на `cover_url` витрины).

⚠️ **Способ наполнения (уточнение владельца, обязательно):** ТОЛЬКО через
экраны b2b-кабинета (b2b-staging), как это делал бы реальный партнёр.
Никаких скриптов, прямой записи в БД, сидеров — иначе теряется смысл задачи:
экран = тест, ошибка = находка. Каждое действие — через UI: открыть форму →
заполнить → сохранить → проверить результат в списке и на витрине.

Задача:

1. Завести/поправить **категории** по меню из документа (5 категорий).
2. Завести **все позиции** (название, описание, цена, `is_available`), каждую
   снабдить фото из папки; сопоставление фото↔позиция — по смыслу, в случае
   неоднозначности — вопрос владельцу в чат, НЕ выдумывать.
3. Проставить `cover_url` витрины (флаер 21:9), проверить лого (уже загружен,
   после бэкфилла §1 должен отображаться).
4. **Журнал находок** (обязателен): таблица «экран → действие → ошибка/аномалия
   → статус (исправлен сразу / в бэклог)». Мелкие баги чинить сразу коммитом
   с тестом; крупные — фиксировать и идти дальше (это фид для SZ-011).
5. Проверить на витрине: поиск находит, карточка с фото, категории, позиции,
   фото позиций кликабельны, карточка товара открывается.
6. Итог §2: владелец открывает кабинет и видит наполненную витрину + журнал
   находок с результатами прохождения экранов.

## 3. Тесты

- URL-стратегия: b2b сохраняет полный публичный URL (unit на MediaStorage/
  saveUploadedFileUsing);
- бэкфилл: относительный ключ → полный URL (тест миграции);
- регресс 404: resource витрины отдаёт URL, который отвечает 200 (feature-тест
  c fake storage);
- SZ-003 (нормализатор/ресайз) остаётся в силе, не откатывать.

## 4. Приёмка

- Владелец: открывает витрину «Пышки & Пончики» на app-staging — лого, обложка,
  категории с фото, все позиции с фото; цикл «в корзину → заказ» проходит.
- zcode: приёмка кода (WORK_PROTOCOL §3.1).

## 5. Отчёт исполнителя (Super Z, 2026-09-10)

### 5.1. §1 Media-URL — сделано

**Стратегия ( lovii-b2b `7bae3c3`, CI 🟢):**
- `PublicDiskMediaStorage::store()` теперь возвращает **полный публичный URL**
  (`APP_URL` b2b + `/storage/...`) — каноническая форма в БД; точки входа
  (`SquareImageUpload::context()`, `PositionResource::media`) унаследовали
  её без изменений кода;
- `MediaUrl::keyFromUrl()` — разворачивание собственного URL в ключ;
  `owns()/delete()` принимают и URL, и легаси-ключ; чужие URL/data-URI
  как раньше не «наши»;
- контракт `MediaStorage` обновлён; `HybridMediaPreview` корректен для обеих
  форм (resolve разворачивает ключ, URL отдаёт как есть);
- фотооптимизатор SZ-003 не тронут.

**Бэкфилл (lovii-core `be13a5c`, CI 🟢):** миграция
`2026_09_10_000003_b2b_media_url_backfill_datafix` — относительные ключи →
полные URL в `merchants.logo_url/mini_logo_url/cover_url`,
`merchant_categories.image_url`, `catalog_categories.image_url`,
`catalog_products.media[]`; база по APP_ENV (staging → b2b-staging.lovii.ru,
production → b2b.lovii.ru; прочие env — no-op с логом); Kuper-полные URL не
трогаются; отчёт `b2b_media_url_backfill_report` в логе.

**Тесты:** `MediaStorageIntegrationTest` переписан под URL-контракт: полный URL
при store, delete по URL и по ключу, owns для URL/ключа/чужого URL (+2 кейса).

**Приёмка владельца (§1):** перезайти в b2b → мерчант «Пышки & Пончики» →
посмотреть лого (предпросмотр должен показывать картинку); на app-staging
витрина 280 — лого/обложка должны отобразиться.

### 5.2. §2 Наполнение «Пышек» — заблокировано материалами

Файлы указаны на машине владельца (`/Users/best/Downloads/…`) — в песочницу
Super Z они не переданы (проверено: upload/ пуст). Нужен один из вариантов:
загрузить в чат архивом (меню-док + 10 фото + флаеры), либо положить в
общедоступное место. После получения: категории → позиции → фото → обложка —
через b2b UI (как согласовано, UI предпочтительнее), затем проверка витрины.

### 5.3. Примечание

Баг SZ-005 §6.2-T-7 (500 в `/v1/search/offers?branch_id=`) не связан с SZ-006,
но влияет на финальную проверку витрины «Пышек» (поиск внутри карточки магазина).
Фикс — ops-команда `search:sync` на staging (план SZ-005 §6.5-P0) — вне моего
доступа; zcode/владелец.

### 5.4. §2 — рекогносцировка и старт журнала находок (Super Z, 2026-09-10)

Состояние витрины «Пышки & Пончики» на staging — read-only через публичный API
(`api-staging.lovii.ru/api/v1`), запись не затрагивалась:

- мерчант 280 / филиал 1024; **§1 бэкфилл подтверждён на живых данных**:
  logo_url / mini_logo_url / cover_url и картинка категории 1814 — полные URL
  `b2b-staging.lovii.ru/storage/...`, все отвечают HTTP 200 (image/jpeg);
  описание мерчанта заполнено владельцем через b2b;
- витрина: **1 категория из 5** («ПОНЧИКИ (КЛАССИЧЕСКИЕ)», id 1814, с фото),
  позиций на витрине — **0** (`GET /v1/storefront?branch_id=1024`).

Журнал находок (старт; формат обязателен по §2):

| № | Экран/поверхность | Действие | Ошибка/аномалия | Статус |
|---|---|---|---|---|
| F-1 | поиск на витрине (`search/offers`) | `GET /v1/search/offers?branch_id=1024` | HTTP 500 — регресс SZ-005 T-7 жив: ops-команда `search:sync` на staging не выполнена (P0 в SZ-005 §6.5) | ✅ ИСПРАВЛЕН zcode 2026-09-10: `scout:sync-index-settings` на staging (offers/merchants/catalog_products); `?branch_id=1024` → 200 (пусто — в БД филиала 0 офферов, корректно), контроль на филиале 108 → 200 и 96 позиций. РЕ-ПРОВЕРКА Super Z после наполнения §2 (29 офферов в БД 1024): поиск по 1024 → 200 и **0 результатов** («пончик/кофе/пышка», ru+lat) — офферы, созданные после проверки zcode, в индекс не попали → нужен разовый `search:sync`/`scout:import` по данным |
| F-2 | филиал (b2b Branch) | сверка данных API | РЕШЁН: меню-документ (§2/§6) и описание мерчанта сходятся — эталон 08:00–21:00; на филиале стоит 09:00–22:00 → поправить через UI Branch при проходе | фикс при UI-проходе |
| F-3 | b2b-кабинет, вход | подготовка UI-прохода | РЕШЁН: телефон партнёра получен от владельца 2026-09-10 → **+79119287478**; записан в канон (FACT_MAP §7A), больше не спрашиваем | закрыт, UI-проход стартует |
| F-4 | материалы | разбор архива pyshki.zip | фото десертов нет (в доке слот и не запланирован) — позиции «ДЕСЕРТЫ К ЧАЮ» заводим без медиа; флаеров 21:9 нет — cover_url уже стоит (широкая нарезка фото пудры) | ок, без действий |
| F-5 | материалы vs канон | сверка | в доке **6** разделов меню (кофе/пончики/пышки/пирожки/десерты/комбо), канон говорил «5 категорий» — создаю 6 по документу (источник истина — док) | отмечено, вопрос владельцу не критичен |

Статус материалов (2026-09-10): **архив получен в чат** → `pyshki-materials/pyshki/`
(меню-док .md/.txt + 10 фото webp 1080×1080). Все 10 фото просмотрены, маппинг
фото↔категории/позиции готов (по смыслу, слоты 3/4/5/7 дока совпали точно).
Блокер входа снят: **телефон партнёра +79119287478** (владелец, 2026-09-10,
канон: FACT_MAP §7A — записан постоянно, повторно не запрашивать).

Блокер материалов — уточнение для zcode: копия в
`/Users/best/LOVII/lovii-docs/pyshki-materials/` лежит на машине владельца
(сторона zcode); зеркальная папка песочницы Super Z `lovii-docs/` материалов
не содержит (проверено 2026-09-10), `upload/` пуст, веток git с материалами
нет. Нужен **архив в чат владельца** (меню-док + photos/ + флаеры). После
получения: категории → позиции → фото → обложка — только через b2b UI,
по журналу находок; неоднозначности фото↔позиция — вопросы владельцу,
не выдумывать.

### 5.5. §2 — наполнение выполнено через b2b UI (Super Z, 2026-09-10)

Вход: телефон партнёра (канон FACT_MAP §7A) → OTP самовыдан через
`GET /api/v1/dev/otp/last-code` → сессия b2b-staging (ООО «АТМОСФЕРА»).
Весь процесс — кликами по реальным экранам Filament (open → заполнить →
загрузить фото → сохранить → проверить), без API-записи/БД/сидеров.

Сделано:

- **Категории**: созданы 5 — КОФЕ И НАПИТКИ, ПЫШКИ (БУБЛИКИ),
  ПИРОЖКИ (ГОРЯЧАЯ ВЫПЕЧКА), ДЕСЕРТЫ «К ЧАЮ» (без фото — материалов нет,
  F-4), КОМБО-НАБОРЫ (ДЛЯ ДОСТАВКИ И САМОВЫВОЗА). С существующей
  ПОНЧИКИ (КЛАССИЧЕСКИЕ) — **6 категорий** (F-5: по меню-доку, не «5»).
  Фото 4 шт. webp 1080×1080 → сервер нормализует в JPG (SZ-003 жив).
  Категория «ПИРОЖКИ»: фото досоздано отдельной правкой (первый save ушёл
  без вложения — см. F-7).
- **Позиции**: создано **27** через UI (2 тестовые владельца не тронуты) →
  **итого 29**; 25 с фото, 4 десерта без медиа (F-4). Названия/описания/
  цены — из меню-дока; is_available вкл. Драйвер UI-прохода:
  `scripts/fill_positions.sh` (agent-browser; фото-маппинг:
  `scripts/pyshki_fill_plan.md`).
- **Часы филиала 1024**: 09:00–22:00 → **08:00–21:00** через UI Branch
  («Скопировать на: все дни») — F-2 закрыт; API `storefront/info`
  подтверждает (`opens_at 08:00`, `08:00 – 21:00` по всем дням).
- **Проверка витрины (§2.5)**: app-staging `/stores/1024` — лого ✓,
  «Открыто» ✓, товары в «Популярном» с фото ✓ (29 в API, `per_page=48`),
  карточка товара открывается, фото кликабельно, цена/«в корзину» на месте ✓.
  Категории на витрине — 1 из 6 → находка F-6 (кэш), фикс сделан.
  Поиск — F-1 (500, ждёт ops `search:sync`).

Журнал находок (продолжение):

| № | Экран/поверхность | Действие | Ошибка/аномалия | Статус |
|---|---|---|---|---|
| F-6 | витрина: дерево категорий | проверка после создания 5 категорий | на витрине 1 из 6: `GetStorefrontCatalogQuery` кэширует дерево категорий на **2 часа** (`storefront:categories:{merchantId}`), инвалидации при изменениях из b2b нет; товары при этом видны сразу (не кэшируются) | **ИСПРАВЛЕН КОДОМ**: `StorefrontCategoryCacheObserver` (lovii-core `7aa2edc`, ObservedBy на CatalogCategory — покрывает и b2b-запись: b2b пишет тем же классом модели) + Pest-тесты. Нужны: деплой staging (zcode) + разово `cache:clear`; до деплоя категории появятся сами по TTL ≤2ч |
| F-7 | b2b: формы категории/позиции | сохранение с одновременной загрузкой фото | при автоматизированном проходе первый save «ПИРОЖКИ» ушёл без вложения: ref file-input устаревает после морфа Livewire; человеку не мешает (визуально вложение видно) | учтено в драйвере (ожидание «Загрузка завершена» + свежие ref); наблюдение — фид SZ-011 |
| F-8 | b2b: footer кнопок форм | scroll к кнопкам | кнопки «Создать / Создать и создать ещё» у нижней кромки могут оказаться под sticky-топбаром при авторескролле (воспроизведено в headless; для человека — редкий кейс узкого окна) | наблюдение — фид SZ-011 |

Итог §2 для приёмки владельцем: открыть b2b — витрина наполнена
(6 категорий / 29 позиций / часы 08–21); открыть app-staging `/stores/1024` —
товары с фото сразу, категории — после деплоя фикса F-6 + `cache:clear`
(или ≤2ч TTL). Поиск в карточке магазина — после ops `search:sync` (F-1).

## 6. Приёмка zcode (§3.1) — 2026-09-10: §1 «Достаточно» (§2 — в работе, приёмке не подлежит)

Факт-чек по коду staging (b2b tip `c0ee2e0` CI 🟢; core tip `3972878` CI 🟢;
целевые `7bae3c3`/`be13a5c`+`8f9d75e` — CI 🟢 после ректор-фикса):

- **b2b `7bae3c3`** — `PublicDiskMediaStorage::store()` возвращает
  `(string) MediaUrl::resolve($key)` = полный публичный URL ✓;
  `delete()/owns()` разворачивают URL через `MediaUrl::keyFromUrl()` с
  фолбэком на легаси-ключ ✓; `MediaUrl` несёт `resolve/isExternal/keyFromUrl`
  ✓; фотооптимизатор SZ-003 в `store()` не тронут (нормализация → лог → URL) ✓.
- **core `be13a5c`/`8f9d75e`** — миграция
  `2026_09_10_000003_b2b_media_url_backfill_datafix`: колонки
  merchants(logo/mini_logo/cover) + image_url категорий + JSON `media`
  позиций; база URL по `APP_ENV` (production → b2b.lovii.ru, staging →
  b2b-staging.lovii.ru, иные env — no-op с логом `b2b_media_url_backfill_skipped`)
  ✓ — совпадает с отчётом §5.1.
- **Тесты** — `MediaStorageIntegrationTest` (tests/Feature/Services/) переписан
  под URL-контракт ✓.
- **Живая часть** — бэкфилл на staging подтверждён данными самим исполнителем
  (§5.4: logo/cover/категория 1814 — полные URL, HTTP 200); повторно проверю
  на витрине при удобной приёмке. Владельцу: перезайти в b2b → лого «Пышек» →
  витрина 280 на app-staging.
- **NB по находке F-1** (500 в `/v1/search/offers?branch_id=`, P0
  `search:sync`) — принята zcode в работу: выполню на staging в текущем цикле.

**Вердикт по §1: «Достаточно»** (приёмка-1; приёмка-2 — Super Z).
Статус карточки остаётся открытой до завершения §2 (наполнение через UI).

## 7. Приёмка zcode (§2, финал) — 2026-09-11: «Достаточно»

- Витрина «Пышек» live: 6 категорий (после F-6-фикса + cache:clear —
  без TTL-ожидания), 29 позиций с фото (полные URL §1), поиск по филиалу
  29/29 (после SZ-013). Журнал находок F-1…F-8 полный (требование §2.4) ✓.
- Наполнение через экраны b2b подтверждено исполнителем, правила
  «никаких скриптов/SQL» соблюдены ✓.
- Остаток: 2 позиции без branch_id (см. приёмку SZ-013) и часы филиала
  09:00–22:00 vs эталон 08:00–21:00 (F-2 — поправить через UI Branch).
- **Визуальная приёмка владельцем**: app-staging/stores/1024.

**Вердикт по §2: «Достаточно»** (приёмка-1 zcode; приёмка-2 — Super Z).
Карточка закрывается после визуальной приёмки владельца.

## 8. Визуальная приёмка владельца — 2026-09-11: «Пышки пока все ок»

Открыл витрину, дефектов не назвал. Задача закрыта целиком (§1+§2),
перенесена в archive/tasks/. Хвосты вне задачи: 2 позиции без branch_id
(см. SZ-013 приёмку) — не влияют на витрину.
