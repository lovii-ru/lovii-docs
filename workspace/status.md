# Статус платформы — «что сейчас в работе»

> Каждый агент в начале задачи читает этот файл, в конце — дописывает свой блок **сверху**.
> Что писать: что сделано и где лежит (ветка/коммит/стенд), решения владельца, незакрытые
> действия и действующие грабли. Что НЕ писать: счёты тестов, замеры, подробности приёмок,
> диалоги — всё это в `docs/sessions/` соответствующего репо и в карточках
> `canon/TASKS/`. Старые записи сжимаются до одной строки на дату.
> Полная история до 20.09 — `../archive/old_versions/status-20260920-full.md`.

## SZ-073 — сервис лояльности (бэкенд): НА STAGING (2026-09-23)

> 2026-09-22/23, ветка `lovii-core` `feat/sz-073-loyalty-service` (worktree `lovii-core-sz073`).

- Собран изолированный домен лояльности в core: `promo_rules` (обобщение `loyalty_rules`,
  + группы товаров, состав комбо, счётчик «раз на клиента»), движок `PromoResolver`
  (резолвинг по позиции, стекирование, база кэшбэка = фактически оплаченная сумма, порог/комбо/
  штампы/счастливые часы/бюджет+авто-стоп), публичный контракт `/api/v1/loyalty/*` и
  `/api/internal/v1/loyalty/apply`, события в очередь + инвалидация кэша.
- Витрина: виртуальная категория «Комбо» первой, промо-поля карточки оффера.
- Гейт зелёный: pint/rector/type-coverage 98.8%/openapi; полный сьют **1495 passed** (последовательно,
  `--parallel` не проходит по памяти контейнера).
- **Смержено в `staging`** (fast-forward, `a0b18231`), CI checks+deploy успешны, миграции применены; `/api/v1/loyalty/promos` живьём 200. `master` — вручную.
- **Хвосты:** право доступа к правилам — решить `merchant.update_profile` vs `loyalty.manage`
  (дефолт взят существующий); представление `loyalty_rules` для `lovii-admin` — снять после
  перевода админки на API (F-060); `GET /loyalty/effect` — каркас Ф2; стресс-страж SZ-060 на
  затронутой витрине не гонялся. Контракт: `canon/API_SPEC.md` §3.13.

## Состояние сейчас (поддерживать актуальным)

- **Среды**: прод — чистая БД, ещё не задеплоен; staging — авто-деплой из ветки `staging`
  (копия прод-данных до 05.09); локальный стенд — чистый сброс через
  `lovii-core/scripts/stand-bootstrap.sh` (cities 3, users 1, merchants 0, Основатель + AA2222,
  лоялти 5%, капитал 1000 ₽). Подробности стенда: `lovii-docs/auth-staging.md`.
- **Оплата**: боевой контур не подключён. Локальный стенд — мок Т-Банка
  (`lovii-core/tbank-mock`, PAYMENTS_FAKE=false); на staging — `PAYMENTS_FAKE=true`
  (переключение на мок — отдельный шаг с обновлением e2e-сценария оплаты).
- **Ждёт мержа в staging**: `lovii-core feat/tbank-mock-stand` — мержить ветку ЦЕЛИКОМ
  (фикс подписи Token acb0db8 + консоль платёжных каналов be70997 + автоверификация
  «прогоном рубля» 63b9d9d).
- **Открытые хвосты**: `OPERATOR_PARTNER_ID` в локальном .env закомментирован (вернуть id
  АКСИОМЫ после её создания владельцем через «ЛОВИ Бизнес»); e2e scenario-03 локально сломан
  (ездит на fake-эндпоинте — перевести на мок или временно вернуть PAYMENTS_FAKE=true);
  дельта ядра SpAccumulationId → `payments.deal_id` (момент Confirm — решение владельца);
  вопросы банку: чарджбэк-нотификации, срок жизни сделки, авто-подтверждение холда;
  SZ-061 Ф2/Ф3 (карточки кодов/QR, назначение амба, счёт платформы); SZ-050 Ф3 (каталог
  drill-down); SZ-062 (P2P-переводы).
- **Очередь и блокеры запуска**: `lovii-docs/as-is/00-zapusk-chego-ne-hvataet.md` (главный
  док запуска — вести после каждого мержа/приёмки) + `BACKLOG_REVIEW.md`; карточки задач —
  `lovii_docs/canon/TASKS/`.
- **Действующие процессные правила**: preflight в core перед каждым мержем
  (`bash scripts/preflight.sh` — pint/rector/phpstan + pest с диффом базлайна средовых
  падений); в новом коде core — никаких статических `Carbon::x()` (CI-rector переписывает
  на фасад Date, immutable); миграция колонок — канарейки тем же коммитом; staging app
  мержить ТОЛЬКО через worktree `/private/tmp/lvapp-nocats`; перед коммитом в общем репо
  проверять `git branch --show-current`, своя работа — сразу личный worktree; наполнение
  данных (юрлица/точки/товары) — ТОЛЬКО через экраны UI, сид-скрипты и вставки в БД
  запрещены владельцем; страж производительности — по факту крупных обновлений
  (AGENTS.md §«Страж производительности»).

---

## 2026-09-22 — Т-Банк: бой проверен живьём, реквизиты per-channel (zcode)

- **Бой Т-Банк работает** (проверено Init/Cancel с сервера): терминал 1788867330861, токен ТОЛЬКО lowercase (дока врёт). IP staging = 89.19.223.31, whitelist ок, TLS только с CA Минцифры (~/certs/tbank на сервере).
- **core 0e054b0 (staging)**: bank-test/bank-prod несут свои реквизиты терминала (env TBANK_TEST_/PROD_*), CA-bundle в TBankClient/AccountVerificationClient/вебхуках, bank-prod разрешён на staging (решение владельца: обкатка реальными тест-платежами с бизнес-карты АКСИОМЫ). Консоль /payments/console показывает маску терминала. Тест-контур банка не функционален (Init «терминал не найден» — претензия банку).
- Сгенерированы: e2c-сертификат выплат (LOVII/certs/tbank-e2c, серийник банку на регистрацию) + CSR для mTLS acqapi (LOVII/certs/tbank-acqapi, ⚠️ юрадрес-заглушка, выпуск через Госуслуги).
- Выплатные ключи + CA-путь добавлены в env staging и prod. На прод НЕ едем до «го». Дальше: тест-платёж 100 ₽ бизнес-картой через витрину staging → вебхук/кэшбэк/пул; реализация выплат e2c.

## 2026-09-20 (ночь, 2) — автоверификация МСП «прогоном рубля» (zcode)

- Решение владельца: после аппрува представителем заявка верифицируется АВТОМАТИЧЕСКИ —
  банк прогоняет 1 ₽ по счёту МСП (зачислил-вернул), ручного матчинга и оператора больше нет.
- `feat/tbank-mock-stand` 63b9d9d: мок `POST /av/v1/verification` (синхронный RETURNED),
  ядро `AccountVerificationClient` (канал через PaymentChannelManager — смена мок↔банк
  без правок) + `RunPartnerVerificationAction` (verifying → verified, ставит
  `lovii_b2b.partners.verified_at` = гейт I-5); аппрув представителя тянет прогон сам.
  ⚠️ Смержить в staging вместе с остальными коммитами ветки.

## 2026-09-20 (ночь) — чистый сброс №2 + stand-bootstrap.sh (zcode)

- **`lovii-core/scripts/stand-bootstrap.sh`** (идемпотентный, 0a1f315): схемы b2b/admin →
  миграции → города (СПб/Москва/Всеволожск) → Основатель (OTP-bypass) → амб-код AA2222 →
  лоялти «Кэшбэк 5%» → капитал +1000 ₽. Эталонный чеклист старта — core sessions 046.
- Стенд сброшен и верифицирован. Правило подтверждено владельцем повторно: сценарии
  проходить ВРУЧНУЮ через app-кабинеты, никаких вставок в обход UI.
- Подписка PASS активируется кнопкой в UI (владелец сам), операторские шаги без UI —
  по его команде.

## 2026-09-19 (ночь, 5) — G6 закрыта: кабинеты по наличию промокода (zcode)

- Core 4c49684 → staging (CI 🟢): representative/msp флаги = true при привязанном промокоде;
  `/msp/overview` отдаёт честную пустую структуру вместо 404 (404 — только профилям без
  промокода; амбассадор — по назначению, 403 остаётся). Сквозной тест владельца (тест-1,
  +79999000001) разблокирован: плитки «Представитель» и «ЛОВИ Бизнес» после перезагрузки
  /profile; кабинет амба НЕ появляется (только по назначению).

## 2026-09-19 (ночь, 4) — консоль платёжного канала + мок только по тестовым картам (zcode)

- `GET/POST /api/v1/payments/console` (HTML, без программирования): переключение
  local-мок / staging-мок / тестовый контур банка / боевой; хранится в payment_settings.
  Защита сред (PaymentChannelManager): local = ЖЁСТКО local-mock; staging = мок/тестовый
  контур (боевой запрещён); production = только боевой. На staging консоль за
  `?key=INTERNAL_ADMIN_SECRET`, локально — без ключа.
- Мок ужесточён (решение владельца): платежи ТОЛЬКО по тестовым картам (неизвестная →
  отказ 1236), состояние персистентно (`tbank-mock/data/state.json`, переживает рестарты);
  дубликат PaymentId от банка больше не роняет ядро (попытка Failed → новая сессия Init).
- Ветка `feat/tbank-mock-stand` be70997 — мержить вместе с фиксом подписи. Локальный
  контейнер мока пересоздан вручную (docker run: alias tbank-mock, 127.0.0.1:3100,
  MOCK_STATE_FILE=/srv/data/state.json) — сервиса нет в compose чекаута SZ-061, не потерять.

## 2026-09-19 (ночь, 3) — tbank-mock живой локально: БАГ ПОДПИСИ Token починен (zcode)

- 🔴 **Баг в ядре**: `TBankSignature` дописывала Password в КОНЕЦ конкатенации, банк же
  сортирует Password ВМЕСТЕ с параметрами — реальный банк отверг бы первый же
  Init/нотификацию (не всплывало: ядро с банком ещё не говорило, тесты подпись не проверяли).
  Фикс acb0db8 в `feat/tbank-mock-stand`, тесты переведены на канон — **СМЕРЖАТЬ В STAGING**.
- Локальный стенд переведён на мок (PAYMENTS_FAKE=false, TBANK_API_URL=http://tbank-mock:3100/v2,
  бэкап .env.bak-mockdemo); сквозняк Init → холд → CONFIRMED → кэшбэк 5% → пул 40/40/20
  проверен живьём (ledger копейка в копейку).
- ⚠️ В чекауте lovii-core (ветка SZ-061 Ф2) остались НЕЗАКОММИЧЕННЫМИ: TBankSignature.php +
  TBankSignatureTest.php (= acb0db8), tbank-mock/ (untracked), отладочный лог в server.js —
  не потерять при switch/мерже.

## 2026-09-19 (ночь, 2) — кабинет амба: амб-код вместо префикса (zcode)

- staging a04266f: экран «Структура» и «Скопировать код» переведены с реп-префикса на
  амбассадорский код (фолбэк — префикс), тексты «префикс» → «код».

## 2026-09-19 (ночь) — SZ-061 Ф1 на staging: амб-код как сущность (zcode)

- Core 76e2ab1: миграция `ambassadors.code` (AA2222 переехал с реп-кода Основателя в
  амб-сущность, реп-строка деактивирована — «само-выданного» промокода больше нет);
  привязка (T-010 + цена подписки) по двум таблицам; POST /profile/promo (422/409);
  AssignAmbassador выдаёт амб-код; дашборд амба отдаёт code. App b4c5472: форма «введите
  промокод амбассадора» в блоке подписки. Оба в staging, гейты 🟢.
- Живой путь сошёлся: привязка → активация 199 ₽ promo → активна до 19.10 → личный код
  AAN4PR виден в карточке.
- Урок: коллизия общего чекаута lovii-core с параллельным агентом (чужой checkout+commit
  поверх ветки) — личный worktree сразу.

## 2026-09-19 (вечер, 3) — tbank-mock волна 1 смержена в staging (zcode)

- `lovii-core/tbank-mock/` (zero-dep Node): /v2/{Init,Confirm,Cancel,GetState,CheckOrder},
  платёжная форма с PAN-сценариями (challenge OTP `1qwezxc`), подписанные нотификации
  с ретраями 60с×24, книга сделок, админка /admin. Core 8346d0a → staging, CI+деплой.
- Ядро НЕ переключено (PAYMENTS_FAKE=true) — переключение после апдейта e2e-сценария,
  с владельцем. Открытая дельта ядра: SpAccumulationId → `payments.deal_id`
  (момент Confirm — решение владельца).
- Инцидент (обработан): общий каталог lovii-core переключился параллельной сессией —
  правило: проверять `git branch --show-current` перед коммитом, своя работа — в worktree.

## 2026-09-19 (ночь, 2) — регресс иконок SZ-059 починен; гэп G6; Яндекс-карта на localhost (zcode)

- Регресс SZ-059: словарь масок иконок срезан до 28 при 38 живых компонентах → 10 классов
  «квадратиками»; восстановлены из истории, хотфикс f5d08fd на staging. Урок: при удалении
  иконок grep по `src/components/icons/**` обязателен (lovii-demo масок не содержит —
  там инлайн-SVG).
- Гэп G6: канон «наличие промокода = доступ к кабинетам» не выполнялся в коде — закрыт
  (см. 19.09 ночь,5).
- Карта Яндекса на localhost не инициализируется (Referer-ограничение ключа):
  адреса тестировать на staging, либо добавить localhost:5173 в разрешения ключа
  (действие владельца).

## 2026-09-19 (ночь) — сброс локального стенда №1 + смягчение стража (zcode)

- Локальная БД сброшена (down -v, все тома); на чистом томе сначала создать схемы
  lovii_b2b/lovii_admin, потом миграции; b2b → :8002 (конфликт с admin :8001).
  Стартовые данные — теперь через `stand-bootstrap.sh` (см. 20.09).
- Страж: не гоняется после каждого мержа — только по факту крупных обновлений,
  приоритетно на локальном стенде (правило в AGENTS.md).

## 2026-09-19 (вечер, 2) — SZ-061: карточка реворка рефералки (zcode)

- Решения владельца: экономика без изменений; ввод промокода — при подписке (блок счёта,
  для непривязанных); ранги — только статус/мероприятия; доработку кабинетов владелец ведёт
  сам (карточка их не трогает); P2P — делать, отдельная карточка SZ-062 (Основатель → амбы →
  репы, стартовый баланс на профиле Основателя); G1 решена — `ambassadors.code` как сущность,
  у Основателя два кода (амб AA2222 + личный AA-XXXX).
- Карточка `lovii_docs/canon/TASKS/SZ-061-referral-canon-rework.md`: Ф1 core → Ф2 app
  (шит промокода, QR амба, две карточки кодов) → Ф3 (назначение амба UI/API + счёт
  платформы + данные Grand/269).

## 2026-09-19 (вечер) — Б-8: аудит рефералки по коду (zcode)

- ГОТОВО: генерация личного кода при активации/продлении PASS, QR репа (?ref=КОД),
  Founder = владелец AA2222. ГЭПЫ G1–G5: два кода Основателя (→ решено G1), назначение
  амба только artisan, счёт платформы отсутствует, QR амба, профиль (Grand 268 не
  верифицирован, дубль 269, АКСИОМА без брендов). Всё разложено в карточку SZ-061.

## 2026-09-19 (день, 5) — база знаний Т-Банк в хабе (zcode)

- В хабе lovii-ru/lovii-docs: `tbank/docs/` (4 PDF банка) + `tbank/notes/` (6 конспектов:
  оплата, нотификации+Token, выплаты+сделки+acqapi, тестовые карты, мок-стенд, экосистема)
  + `tbank/openapi/tbank-payouts-openapi.json`. Секретов и условий договора там нет —
  обогащать по ходу разработки.
- Ресёрч: готового мока эквайринга в открытых источниках НЕТ (mock-tbank-api — это
  T-Invest); полезное: OpenAPI «Массовые выплаты», Laravel-реализация флоу prukon/admin.k
  (только читать, без лицензии), TS SDK jfkz/tinkoff-payment-sdk.

## 2026-09-19 (день, 4) — ресёрч Т-Банк Мультисплит + дизайн мок-стенда RES-007 (zcode)

- Артефакт `lovii_docs/artifacts/RES-007-tbank-multisplit-mock-stand.md` (все 4 PDF
  Мультисплита прочитаны). Волны: 1 — Init/Confirm/Cancel/GetState/CheckOrder + нотификации
  (реализовано); 2 — e2c-выплаты (PartnerId=shopCode ЮЛ 0,5%, СБП/карта ФЛ 1%); 3 —
  рекуррент, СБП-QR, sm-register.
- Ключевое: DealId приходит ТОЛЬКО в AUTHORIZED-нотификации (SpAccumulationId участвует
  в Token!) → дельта core писать в payments.deal_id; TBankClient::confirm реализован, но
  никем не вызывается; тестовый контур банка — только после договора + IP-WL + запросы
  С БОЕВОГО терминала → поэтому свой мок-стенд.

## 2026-09-19 (день, 3) — очередь 00-дока перестроена (zcode)

- Б-6/Б-7 (юрист + 54-ФЗ) — стартовать немедленно (длинный цикл); Б-3/Б-4 (верификация
  UI + авто-матчинг) — следующая агентская задача; Б-2 прод-деплой — после них и после
  решения по Б-8; П-5/П-6 (P2P, промо-матрица/RBAC) — внутри реворка рефералки.

## 2026-09-19 (день, 2) — перф-страж (zcode)

- `lovii-app/scripts/stress/`: baseline.json + smoke.sh (~1,5 мин, VU=10/25, односторонние
  пороги) — проверка одной командой перед мержем горячих путей. Правило агентов — в
  AGENTS.md §«Страж производительности». В core `db.slow_requests` в laravel.log
  (>500 мс SQL на цикл) — пассивное обнаружение регрессов.

## 2026-09-19 (день) — Б-1 закрыт: «404-стена» = артефакт жгута (zcode)

- Диагноз по access-логу caddy: 14 204 из 14 363 запросов жгута v1 = 404 на путях БЕЗ
  `/api/v1` + TLS-handshake на каждый запрос; скрытого лимитера нет. Жгут v2
  (`lovii-app/scripts/stress/`) — правильные пути, keep-alive.
- Ёмкость поднята (fpm dynamic 24, app-контейнер 2G): плато ~235 rps, деградация очередью,
  0 ошибок до 200 VU; следующий узел — Postgres (P2 при росте). Гейт минимума заказа под
  нагрузкой работает.
- ⚠️ staging app мержить ТОЛЬКО через worktree `/private/tmp/lvapp-nocats`.

## 2026-09-19 — актуализация as-is + 00-док запуска (zcode)

- as-is 01–13 актуализированы (подписка PASS, выплаты, ПЭП, EC-11, VIP-оборот, min-order,
  T-010, кабинет МСП v2, геокодинг адреса точки, инвайт админом).
- Новый `lovii-docs/as-is/00-zapusk-chego-ne-hvataet.md` — **главный док запуска**:
  блокеры Б-1…Б-7, P2-таблица П-1…П-10, очередь с ролями «агент/владелец», архив решений.
  Вести после каждого мержа/приёмки.

## 2026-09-19 — lovii-site: аудит превью lovii.ru (zcode)

- Репо `/Users/best/Projects/lovii-site`, Pages из `main`. Починен 404 (динамический
  `<base>`), добавлена секция «Подключиться» (#connect).
- Решения владельца: White Paper убран полностью, МЛМ/каскады не упоминать, живое демо →
  `lovii.mobiap.com`, оферты навсегда на `axiiom-ru.github.io/lovii`, `hello@lovii.ru` жив.
- Грабли: клики по якорям в IAB подвисают — JS `.click()`; fullpage-скрин IAB врёт на
  reveal-секциях.

## 2026-09-18 (ночь, 4) — SZ-056: кнопка подписки PASS, оплата только с внутреннего счёта (zcode)

- Решение владельца: подписка оплачивается ТОЛЬКО с внутреннего счёта (Business → PAY) —
  рекуррент и привязка карты не нужны; из юрдокументов остаются оферта (юрист) + чек 54-ФЗ.
- Core: `POST /subscription/activate` — подключение/возобновление, промо 199 ₽ по коду
  Амбассадора иначе 599 ₽, честные 409/422; попутно **фикс латентного бага крона SZ-053** —
  ключ оператора `payments.split.operator_partner_id` (старый путь давал бы 500 и гасил
  подписки в grace).
- App: блок «Подписка LOVII PASS» на «Счёте и операциях» — CTA Подключить/Возобновить,
  честные состояния (активна до {дата} / grace / без средств).

## 2026-09-18 (вечер, 3) — гейт EC-11 включён на staging (zcode)

- `DistributeOrderPoolAction`: реп без льготной подписки (актив/grace) теряет долю пула
  в пользу Компании; амбассадор не задет. Флаг включён на staging — 40% пула только
  подписанным. Карточка-дубль T-011 (Super Z) закрыт SZ-053.

## 2026-09-18 (вечер, 2) — T-010 + SZ-051 + preflight-процесс (zcode)

- T-010: любой суффикс `?ключ=` — носитель промокода (резервные token/cart_id/utm_* не
  сканируются); ядро валидирует `referral_code` по `representative_promo_codes` при
  подтверждении номера → `users.promo_code` (пишется только на пустое, идемпотентно;
  не найден — тихий игнор).
- SZ-051: `media:check-orphaned [--clear]` — отчёт/очистка осиротевших медиа
  (на staging очищено 4 битых из 514 ссылок).
- **Preflight-процесс** (ответ на 2 красных CI за день): перед каждым мержем в core —
  `bash scripts/preflight.sh` (блокирует новые красные). Правила: миграция колонок →
  канарейки тем же коммитом; статических `Carbon::x()` в новом коде нет.

## 2026-09-18 (вечер) — SZ-055: ПЭП-реестр НПД (zcode)

- Таблица `pep_reports` (payout_id unique, payload jsonb, content_hash, otp_hash + expiry,
  статус pending|signed); `PepReportService` (issue/confirm/resend + verifyIntegrity —
  хеш по канонической форме jsonb); хук в `PayoutService::requestPull` — после коммита
  выплаты создаётся документ и уходит пуш «Код: N…» (сбой пуша не роняет — resend).
  API: `/pep/reports/pending`, confirm/resend.
- Грабли: final-классы не мокаются → контракт + bind; код после `return DB::transaction()`
  — мёртвый (ловится тестом).

## 2026-09-18 (день, 2) — SZ-054: выплаты pull НПД + суточный push-реестр (zcode)

- Таблица `payouts` (push|pull, идемпотентность push за день, суммы amount/net/commission,
  статус pending→sent); `users.legal_status` (nullable — презумпция НПД);
  `accounts.payouts_blocked` (минус-баланс блокирует).
- `PayoutService` **pull**: гейты — подписка `grantsBenefits()` (в grace пускает),
  legal_status≠ip/ooo, blocked, минимум 100 ₽; ≥3000 ₽ → 1% платит оператор (получателю
  100%), <3000 ₽ → 30 ₽ удержание. **push** `payouts:push-daily` 09:00 МСК — реестр по
  partner-счетам (0,5% bearer=operator), минус → blocked. API `POST /payout/request`.
- До тестового контура банка реестр уходит в интернет-банк руками (статус pending).
  Канон §1.6 «min 100 ₽» устарел — владелец правит.

## 2026-09-18 (день) — SZ-053: sub-billing, каскад списаний + retry-cron (zcode)

- Журнал попыток `subscription_charges`; каскад Business (счёта юрлиц по мосту
  `partners.owner_user_id`) → PAY (личный) → внешняя карта (каркас: failed
  `external_unavailable` до тест-контура банка); cron `subscriptions:charge` 03/09/15/21 МСК;
  grace 48 ч сохраняет льготы; аннулирование сбрасывает акционную цену (199→599,
  promo_code=null); идемпотентность: row-lock + уникальность ledger.
- Грабли: B2b-модели и `B2bSchema::ensure()` живут в транзакции pgsql_b2b — сырой SQL по
  `lovii_b2b.*` через `DB::connection('pgsql_b2b')`; даты моделей immutable → `new Carbon($d)`.

## 2026-09-18 (утро) — SZ-052: домен подписки Представителя (zcode)

- Таблицы `subscriptions` (одна на профиль, снимок цены 59900/19900, календарный период,
  окно grace) + `subscription_status_changes` (append-only); машина
  `SubscriptionStatusMachine` (active/grace_period/suspended/cancelled; `grantsBenefits()`:
  в grace льготы сохраняются — §A2); read-API `GET /api/v1/subscription` (data:null —
  честная пустота); конфиг `config/subscription.php`.
- Грабли: регистр namespace macOS прощает, CI на Linux — нет; тяжёлые свипы →
  `php -d memory_limit=2G vendor/bin/pest`.

## 2026-09-18 (ночь) — №1 min-order слита в staging (zcode)

- core 9bf7992 + app e147433: платформенный минимум заказа (дефолт 600 ₽ / пол 500 ₽) жив
  на стенде — `/stores` отдаёт `min_order_amount_effective[_rubles]` (витрина, корзина,
  чекаут, настройки точки, страница в супер-админке).
- Средовый тест: геокод-кейс MspEndpointsTest падает локально при наличии
  YANDEX_GEOCODER_API_KEY (тест без Http::fake) — кандидат на обёртку fake.

## 2026-09-18 (вечер) — GitHub разблокирован, пуш всего выполнен (zcode)

- `gh auth` = bestdeejay-design (repo+workflow), пуш по HTTPS. Запушены все репо —
  карта и порядок: `~/LOVII/ПУШ-ПЛАН.md` (исполнен; перед новым пушем — fetch+сверка).
  Локальный staging в core был позади GitHub — fast-forward.
- NB для Super Z: его ДС-ремап e0e82fb перекрыт локальной линией app — идеи (.rc-*,
  сег-табы, bottom-sheet-апрувы) переносить ПОВЕРХ сессий 107–116, файлы не восстанавливать.

## 2026-09-18 — lovii-admin: страница «Платформенный минимум заказа» (zcode)

- Первая и единственная редактируемая страница админки: дефолт (600 ₽) и пол (500 ₽,
  пол ≤ дефолта) в core `platform_order_settings` через соединение pgsql_core. Право
  `View:PlatformOrderSettings` — по умолчанию только super_admin. На карточке точки —
  «Минимум на чекауте» с эффективным значением.
- Грабля сборки lovii-admin: `docker compose build` падает на pecl redis — собрать базовый
  слой отдельно (`docker build --target base -f docker/Dockerfile .`); для миграций нужна
  схема `lovii_admin`.

## 2026-09-17 (ночь) — SZ-050 Ф2: «+ Бренд» / «+ Точка» + правило ИНН (zcode)

- Core: `POST /msp/merchants` (бренд под своим юрлицем, active у верифицированного),
  `POST /msp/branches` (точка под брендом; адрес геокодирует ядерный Yandex-геокодер —
  точке нужны city_id и координаты). **Правило ИНН**: заявка с ИНН уже верифицированного
  своего юрлица → новый бренд в существующее юрлицо, сразу Verified, без счёта 1 ₽ —
  дубли юрлиц исключены.
- App: шит форм роста из кабинета, CTA в пустых состояниях Обзора.

## 2026-09-17 (вечер) — SZ-050 Ф1: свитчер юрлиц «МСП = ИНН» (zcode)

- Спека кабинета v2 утверждена владельцем («го»): юрлицо → бренды → точки, один кабинет,
  свитчер ИНН в шапке (паттерн СберБизнес), каталог drill-down, iiko-готовность —
  `lovii_docs/artifacts/SPEC-msp-cabinet-v2.md`, карточка SZ-050 (Ф1–Ф4).
- Core: `partners[]` в обзоре (включая пустые юрлица) + team по partner_id; app:
  `PartnerSwitcher` (чип с ИНН-маской → шит «Мои юрлица» + «Добавить юрлицо»), контекст
  фильтрует Обзор/Заказы/Товары/Настройки/Команду.
- Находка: заявка «Пончики» с тем же ИНН создала ДУБЛЬ юрлица (271) — слито
  («Пончики» = 2-й бренд АТМОСФЕРЫ), правило ИНН зафиксировано в Ф2.

## 2026-09-17 (день, 2) — юрлица на экране «Команда» (zcode)

- Термины синхронизированы: юрлицо → бренд (= мерчант, один уровень) → точки.
  Core f2a4f06: обзор отдаёт partner_id/partner_name. App: бренды на «Команде»
  сгруппированы по юрлицам, состав подписан юрлицом.

## 2026-09-17 (день) — SZ-047: канон приглашения в команду (zcode)

- Канон владельца: бренд → точки бренда → роль → телефон; **имени в форме нет** (сотрудник
  задаёт сам в профиле); заголовок «Команда»; бренд виден всегда. Матрица прав:
  администратор = всё как владелец, сотрудник = заказы + наличие.
- App: точки = филиалы выбранного бренда; `branch_ids` при нескольких брендах уходят
  явным списком (пустота = «все точки юрлица» — иначе выдало бы чужие бренды).

## 2026-09-17 (ночь) — SZ-047 §3: смена роли и отзыв доступа (zcode)

- Core: `PATCH /api/v1/msp/team/{membership}/role` + `DELETE` (перевод в suspended — все
  выборки MspAccess фильтруют active, отозванный сразу теряет доступ). Защиты: себя не
  трогаем, роль выше своей не выдаём, чужой участник = 404 member_not_found.
  `GET /team` отдаёт membership_id/branch_ids/status.
- Отклонение от карточки (решение владельца): manager получил team.update_role/team.revoke.
- Аудит — существующий механизм: события team.role_updated/revoked в
  `lovii_b2b.partner_audit_logs`.

## 2026-09-17 (ночь) — кабинеты: канон-классы без стилей (сессия 116, zcode)

- Аудит `roles-module/**` (25 файлов): кнопки строки товара рендерились 0×20 без глифа
  (класс role-icon-btn без правила) — подключён миксин; ручные копии миксинов заменены
  (копии уже разошлись с каноном). Урок: у LvIcon нет width/height — размер задаёт только CSS.

## 2026-09-17 (ночь) — перки уровня vs карточки привилегий (сессия 114, zcode)

- Дубль выгод в профиле снят фильтром-нормализацией (`pay-privileges-match.ts`); карточки
  привилегий — единственный источник описания выгод. Ждёт владельца: маппинг VIP-пар
  синонимов и источник состояния уровня.

## 2026-09-17 — серия «LOVII как есть» 01–13 (zcode)

- `lovii-docs/as-is/` — 13 доков по модулям (витрина … админка + БД + финконтур в коде +
  словарь сущностей b2b) с фактами file:line и живым SQL staging. Находки для фазы правок
  (частично закрыты позже): min_order не энфорсился; списание баллов без UI; Idempotency-Key
  клиент не шлёт; OTP_DEV_BYPASS default true (прод-риск); верификация юрлиц только
  ручным UPDATE.

## 2026-09-15 (вечер) — auto-designer: аудит app + дизайн-система (auto-designer)

- Системная причина «неоформленных блоков» — фантомные токены ДС (20+ имён без значений,
  браузер отбрасывал свойства) → блок компат-алиасов в `src/scss/tokens.scss`.
  Кнопки-фиксаторы под навбаром → `--lv-navbar-offset` (16 экранов).
- Избранное: `favorites.store` (localStorage) + FavoriteButton (тихая иконка в строке /
  плавающая пилюля на карточке точки); серверного избранного в ядре нет — отдельная задача.
- Дизайн-система: ритм 16px внутри группы / 24px между секциями; цепочка `--lv-accent*`
  (в клиенте розовый, в кабинете роли — цвет роли); храповик
  `design-system.guard.test.ts` (уровень долга только понижать); правила —
  `lovii-app/docs/DESIGN_RULES.md`.
- История операций LOVII PAY: русские подписи из словаря app (ядро пишет проводки по-русски).

## 2026-09-15 (вечер) — кабинеты ролей пересобраны по канону демо (zcode)

- Три кабинета (Амб/Реп/МСП) на одном визуальном языке (`cabinet-ui.scss`); «битые иконки»
  закрыты системно (normalize.scss: наследование цвета svg). Реферальная цепочка end-to-end:
  перехват ?ref= → привязка при регистрации → роли по промокоду → гейты кабинетов.
  GitHub тогда был недоступен (403 suspended) — коммиты ждали пуша (увидели свет 18.09).

## 2026-09-15 — кабинеты ролей работают по промокоду (БАТЧ 3.2, zcode)

- Канон владельца: доступ к кабинетам ролей — по наличию промокода; нет промокода — нет
  доступа; есть — пользователь сам выбирает роль. Core: EnsureRepresentative пускает
  приглашённого, гейт role.msp (EnsureMsp); честные пустые состояния вместо 404.
  App: rolesGuard уводит в профиль без промокода.

## 2026-09-15 (утро 2) — кабинет МСП на общем языке кабинетов (БАТЧ 3, zcode)

- 7 экранов МСП → партиал `cabinet-ui.scss` (20 миксинов). Починено 17 дефектов, в т.ч.:
  MspPayment использовал AppButton без импорта (кнопки не рендерились вовсе); Стартер грузил
  данные без branch_id (показывал чужую точку) и рисовал выдуманное «5 из 5 готово»;
  ошибка чтения настроек оставляла форму с дефолтами (сохранение затёрло бы расписание).

## 2026-09-15 — иконки: системный фикс цвета (zcode)

- Корень: `* { color: … }` в normalize.scss красил svg-узлы → иконка теряла цвет контейнера.
  Фикс: `:where(svg, svg *) { color: inherit }` сразу после `*` (нулевая специфичность);
  сняты 43 избыточных `:deep`-блока.

## 2026-09-14 (вечер 2) — путь подключения точки МСП пройден целиком (zcode)

- Прод-аудит Обзора МСП: дубли точек слиты в один селектор, заказы Обзора подчиняются
  выбранной точке, строки тапабельны в деталь. VITE_DISABLE_SW=true — против рассинхрона
  SW-кэша и бандла после серии пересборок.

## 2026-09-14 (вечер) — иконки профиля выровнены (zcode)

- Урок: стили демо-блоков `> .icon`, а LvIcon рендерит голый `<svg>` без класса — размер
  задавать селектором `svg` (эталон: плитка 36px / иконка 17px; .row-item gap 12px).

## 2026-09-14 (день) — SZ-038 фаза D: multi-point МСП (Sisyphus)

- «Добавить точку» + селектор «Рабочая точка» (msp-стор, persist); branch_id в
  msp/orders|products|settings core (чужой id → 404 branch_not_found). phpunit.xml
  герметизирован от хостовых секретов (VK/TG/MAX-токены зеркалились со стейджинга).

## 2026-09-13 (вечер) — SZ-045: полное зеркало чарджбэка (zcode)

- Core `feat/sz-045-chargeback-mirror`: ReverseOrderPoolAction (зеркало всех ног по order_id,
  обе стороны; эквайринг-fee остаётся), PostChargebackBankFeeAction (Dispute Fee на Точку),
  PaymentStatus::Chargeback, вебхук разворачивает атомарно, internal
  POST /orders/{id}/chargeback + chargeback_logs. App: карта PAY показывает баллы.
  Admin: ChargebackResource + CoreApiClient. Запушено и слито 18.09.
- Открытое: assumption «банк возвращает эквайринг-fee» — подтвердить.

## 2026-09-13 (день) — fold: двухколоночные профиль и кошелёк (zcode)

- Канон демо `.pay-cols`: ≥640px сетка 5fr/6fr (карта | меню/история), ≥1024px gap 28px;
  карта ≈390px на fold (~900 CSS px). Итерация 2: логическая группировка блоков
  («покупатель» слева, «роли и настройки» справа).

## 2026-09-13 — канон: сущность LOVII PAY §5.1–5.4 (zcode)

- `FINANCIAL_CONTOUR.md` раздел 5 заменён владельцем ЦЕЛИКОМ: LOVII PAY = карта-витрина,
  на карте ТОЛЬКО баллы (wallets), не рубли; UID = идентификатор ПОЛЬЗОВАТЕЛЯ, номер карты =
  идентификатор КОНКРЕТНОГО СЧЁТА (один UID → любое число карт); маршрутизация начислений
  (нет рублёвого счёта МСП → всё в PAY); балловый инвариант; зеркальное списание (обратный
  след, минус = долг, блок выплат); §5.5 регресс чарджбэков — 8 правил (полное зеркало по
  order_id, Dispute Fee на Точку, идемпотентность/аудит/симуляция).
- Артефакт `lovii_docs/artifacts/db-schema-analysis.md` — все 85 таблиц трёх схем
  (public/lovii_b2b/lovii_admin) с колонками, ключами, числами строк. Идентификатор =
  `users.id` (колонки uid нет); wallets → user_id; accounts → owner_type+owner_id;
  деньги в копейках.

## 2026-09-12 — SZ-040 A-3/A-4: API счетов + экран «Кошелёк» (zcode)

- `GET /balance` (карты: личный счёт + компании по цепочке partner_users.core_user_id →
  partners.owner_user_id) + `GET /balance/entries` (история с фильтрами, чужие счета 404).
  App «Кошелёк» /profile/wallet. Грабля привязки: partners.owner_user_id = id
  b2b-пользователя (у владельца 258), НЕ users.id.

## 2026-09-11 — сводка дня (zcode)

- **SZ-039 балловый контур**: CONFIRMED вебхука Т-Банка → кэшбэк 5% по `loyalty_rules`
  (идемпотентно — банк ретраит вебхуки), REFUNDED → возврат начисления; Expiration удалён
  (баллы бессрочные). App «Мои баллы». Приёмки владельцем закрыты: бэк принимает только
  `per_page` (макс 48), суммы через formatPrice (баллы = рубли). **ПРИНЯТО**, инвариант
  «баланс = знаковая сумма проводок» сходится.
- **SZ-038 фаза 1 (роли rep/amb/msp в core)**: промокоды представителей PPXXXX
  (`representative_promo_codes`, artisan roles:issue-promo / roles:assign-ambassador),
  амбассадоры (префикс-иерархия, дерева нет), заявка с promo → awaiting_rep_approval,
  апрув репа → `partners.representative_approved_at` → витрина-гейт, тариф merchants,
  read-API /representative|ambassador|msp. Сумма счёта верификации = 1 ₽ (решение владельца).
- **SZ-040 A-1/A-2**: `accounts` (user/partner/platform, копейки, row-lock) +
  `ledger_entries` (append-only, знаковые, уникальность = идемпотентность); пул 40/40/20 на
  CONFIRMED: точке 90% на счёт ИНН, платформе 10% − комиссия банка (карта 3,16%/СБП 0,7%/
  T-Pay 2,59%, мин 3,49 ₽), остаток → Компания. Оператор = АКСИОМА (265) — решение
  владельца; «МСП без промокода не бывает» — фолбэк AA2222. Чистый лист ledger по просьбе
  владельца (после сброса заказ #144 сошёлся копейка в копейку).
- **Пуши SZ-010 живые**: очередь staging была мертва с 06.09 (horizon.php знал только
  production/local → ни одного supervisor'а при APP_ENV=staging) — фикс b2b23c8; VAPID не
  попадал в WebPush (v9 сигнатура `(auth, options)`) — фикс dcfdbcd. Пуш продавцу
  подтверждён владельцем. Гвард-урок: серверные команды только с
  `-f docker-compose.prod.yml -p lovii-core-staging` (голый `docker compose up` уронил
  staging: dev-compose пересоздал внешнюю сеть b2b/admin).
- **SZ-037 мост b2b→core**: `applyTransition` канбана b2b пишет статус прямо в БД мимо
  событий core — уведомления о статусах через канбан не работали никогда; мост реализован
  (guard + ENV + queue worker), сквозняк подтверждён. Тест перед продом — client push.
- Оплата открыта до терминального статуса (фикс b5d69f6: SubmitOrderJob схлопывал окно —
  CreateOrderPaymentAction пускал только Created). SZ-027 «Очистить кэш» задеплоена.
  Чекаут при delivery_not_supported — честная ошибка вместо пустого экрана (SZ-036).
- ⚠️ У сервера 2 IP: 89.19.223.16 входящий, **89.19.223.31 исходящий** — банк видит .31
  (важно для IP-WL).

## 2026-09-10 — сводка дня (zcode)

- Настройки приложения нижним шитом (тема Системная/Светлая/Тёмная + 4 размера текста,
  colorModeChoice system/light/dark) — на staging.
- Карточки SZ-025…028 (анимации, версия сборки, «Очистить кэш», ресёрч пушей) выданы
  Super Z; язык/i18n осознанно не заведён.
- SZ-014 ресёрч мини-аппов VK/MAX/TG (Super Z, приёмка «Достаточно»): валидация TG и MAX —
  одна схема HMAC/WebAppData, VK — HMAC vk_*; артефакт `canon/MINI_APPS_RESEARCH.md`.
  Ценность сейчас — мини-апп представителя/амбассадора (атрибуция start_param → пул).
- Батч-приёмка «Достаточно»: T-005/006/007, SZ-003, SZ-004, SZ-006 §1, SZ-008.
- I-5: гейт витрины/поиска/заказов по `verified_at` партнёра (флаг
  STOREFRONT_VERIFICATION_GATE=true на staging; исключение «импортирован интеграцией»
  спасло Kuper-витрину).

## 2026-09-09 — сводка дня (zcode)

- **Т-Банк платёжное ядро фаза 1** (core 5e96a38, на staging): таблица `payments`,
  TBankSignature/Token, TBankClient (Init/Confirm/Cancel/GetState), вебхук с подписью +
  авторитетным GetState, гейт PAYMENTS_ENABLED. Fake-режим PAYMENTS_FAKE=true + dev-роут
  /payments/fake/notify (гоняет реальную вебхук-логику); в app — шаг оплаты на экране
  успеха, /payment/result с поллингом. ⚠️ В /opt/lovii-core-staging НЕЛЬЗЯ голый
  `docker compose up` — только `-f docker-compose.prod.yml -p lovii-core-staging`.
- Терминал Т-Банка получен (тест+бой, оплатный+выплатный; ключи в нелокально-гитовом
  `lovii-docs/tbank/` — секреты в git не класть). Блокер живого Init: IP сервера не в
  whitelist банка (письмо acq_help отправлено).
- MCP-серверы воркспейса: context7 + lovii-pg-staging (read-only SQL, роль lovii_mcp_ro,
  SSH-туннель launchd 127.0.0.1:15433); конфиг `LOVII/.zcode/config.json`, инструменты
  появляются после перезапуска сессии.
- Парный режим zcode/Super Z (WORK_PROTOCOL v1.2): zcode — серверная зона + приёмка работ +
  постановка SZ; Super Z — кодинг/ресёрч; приёмка коллегиальная «Достаточно/Доработка».
  Позже автоматические циклы удалены (лимит токенов) — ритм ручной.
- VK-канал: заглушки скрыты (AllChannelsStrategy = max+telegram); SZ-001 (спека VK) выдана
  Super Z; T-001 возвращена владельцу (премиса не подтвердилась, FINDINGS F-037).
- Аудит-агент: правило «Фиксация работы» (session-док + запись в status.md тем же
  пуш-циклом, что код) добавлено в AGENTS.md всех репо.

## 2026-09-08 — MAX-доставка OTP работает (zcode)

- Корень 400 «Unknown recipient»: MAX Bot API адресует личный диалог через
  `POST /messages?user_id={user_id}` (query), chat_id в теле — только для чатов/каналов.
  Фикс feat/max-messages-user_id. Приёмка владельцем: коды доставлены.
- ⚠️ В «Боты Макс.txt» подписи токенов перепутаны: «Лови» = второй токен.
  LOG_LEVEL=info на staging (был error — резал диагностику).

## 2026-09-07 — сводка дня (zcode)

- MAX-бот «Лови» на staging: OTP_MAX_TRANSPORT=bot_api; НУЦ-сертификат Минцифры добавлен
  в образ (иначе cURL error 60); протухший чат → max_link_required + свежий диплинк
  (вместо 500).
- Спека `lovii-core/docs/specs/otp-delivery-channels.md`: фазы MAX → Telegram → WhatsApp →
  VK; /auth/otp/request отдаёт coming_soon, app — бейдж «Скоро».
- Адресный флоу app по фидбеку: карточка = выбор, карандаш = правка, карта в форме
  (пин → реверс-геокодинг), доп. поля адреса уже в user_addresses; фикс UI-аудита
  F-015…F-027.

## 2026-09-06 — первые UI-сессии app на staging (zcode)

- Аудит экранов («ездящий» хедер iOS = WebKit-баг backdrop-filter → стекло на ::before),
  единый AppHeader, типографика канона + настройка «Размер текста», возврат к канону демо.
  Сессии app 008–011. OTP dev-эндпоинт для тестов:
  `GET /api/v1/dev/otp/last-code?phone=…`.
