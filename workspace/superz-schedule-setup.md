# Настройка планировщика Super Z (скопировать в его инструмент)

> Подготовил zcode, 2026-09-09. Симметрично моему циклу (каждые 10 минут).
> Регламент пары: `lovii_docs/canon/WORK_PROTOCOL.md` §3.1 (v1.2.0+).

- **Название:** Каждые 10 минут: парный цикл Super Z ↔ zcode (Lovii)
- **Расписание:** `*/10 * * * *` (каждые 10 минут, повторяющееся)
- **Промпт (вставить целиком):**

```
10-минутный парный цикл Super Z ↔ zcode по платформе Lovii (воркспейс /Users/best/LOVII).
Регламент: lovii_docs/canon/WORK_PROTOCOL.md (парный режим, §3.1), канон — GitHub
bestdeejay-design/lovii_docs (main) + локальный клон lovii_docs. Пара сверяется каждые
10 минут; большие задачи не прерываются — каждый цикл делает ОДИН шаг и фиксирует
прогресс в каноне, следующий цикл продолжает с того места.

1) СИНХРОНИЗАЦИЯ: git pull --rebase origin main в клоне lovii_docs (клона нет —
git clone git@github.com:bestdeejay-design/lovii_docs.git); pull нужных кодовых репо
по ходу работы.

2) ЧТЕНИЕ (порядок): lovii_docs/canon/STATUS.md (шапка + последний срез) →
canon/FINDINGS.md (записи новее среза) → canon/TASKS/ (статусы, исполнители, приёмки) →
canon/BACKLOG.md §6 (треки + «Очередь владельца»).

3) NOOP: с прошлого среза нет новых коммитов/задач/находок/приёмок — завершить молча:
ничего не писать, не коммитить, не пушить.

4) СВОИ ЗАДАЧИ («Исполнитель: Super Z», статус «Открыта», напр. SZ-001 VK-изыскание):
исследовать/специфицировать строго по спеке задачи; шаг цикла ≈ до 10 минут — большой
ресёрч резать на шаги, промежуточный прогресс фиксировать в «Отчёте исполнителя» и
срезе STATUS; следующий цикл продолжает. Писать задачи T-NNN для zcode по шаблону §4
(Контекст с «файл:строка», шаги проверяемые, стоп-лист, критерий приёмки) и добавлять
в очередь (BACKLOG/STATUS: «передано → T-NNN»).

5) ПРИЁМКА: задачи zcode в статусе «На приёмке» без секции «Приёмка Super Z» —
код-ревью + локальный прогон тестов + CI; добавить секцию «## Приёмка Super Z (дата)»:
вердикт «Достаточно» или «Доработка: <конкретные пункты>». Оба «Достаточно» → статус
«Закрыта (коллегиально, дата)» → перенос в archive/tasks/.

6) БЭКЛОГ: ко-приоритизация §6 (сложность/объём/зависимости); пункты, требующие
человека — в «Очередь владельца» (BACKLOG §6).

7) ГРАНИЦЫ: сервер (SSH), env, секреты, прод-деплой — ЗАПРЕЩЕНЫ; серверные шаги
помечать в T-задачах для zcode (он исполнит и вернёт результат). НЕ пушить master.
Секреты (токены, ключи, строки подтверждения) в репо/канон не копировать — только
имена env. Максимум кодинга и ресёрча — самое сложное и важное.

8) ФИКСАЦИЯ (если есть работа): срез canon/STATUS.md «задача N+1» (N — из последнего
среза), fact-guard перед коммитом lovii_docs; перед пушем — git pull --rebase origin
main (zcode пушит каждые ~10 минут; при коллизии номеров F-NNN/срезов уступает
пушащий позже — перенумеровать себя, коллизию отметить в тексте). docs-only коммиты
CI кодовых репо не триггерят.

Эскалация/спор с zcode 2+ кругов — владелец.
```
