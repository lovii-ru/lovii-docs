# LOVII — Release Readiness (этап 2)

<!-- fact-guard: allow — чек-лист релиза цитирует канон-числа (5%, 1 ₽, НДС 22%, пороги) ТОЛЬКО с указанием источника; канон чисел — PARAMS.md / FINANCIAL_CONTOUR.md, здесь не переопределяются -->

> **Документ:** рабочая заметка (класс W), не канон. Версия черновика: 0.1 · **Срез:** 2026-09-17.
> **Назначение:** собрать честную картину «можно ли релизить v1.0» по семи осям и
> дать единый артефакт для решения владельца.
> **Метод:** каноны `lovii_docs/canon/` (BRD/PRD/PARAMS/FINANCIAL_CONTOUR/STATUS/BACKLOG/FINDINGS/FEATURES/...),
> as-is `lovii-docs/as-is/` (13 модулей, что код реально делает),
> `workspace/decisions-needed.md` (открытые владельческие развилки).
>
> **Формат вердикта по разделу:** 🟢 готово · 🟡 условно · 🔴 блокер.
> Все ссылки на источники — в [Приложении B](#b-ссылки-на-canon).

---

## 0. TL;DR — общий вердикт

**Предварительное решение: 🔴 не готов к prod-релизу для публичных платежей покупателей.**
**Предварительное решение: 🟡 условно готов к промежуточному «pilot M1»-запуску на своей БД.**

Три причины, почему рано:

1. **Прод-деплой не отработан ни разу.** `git status lovii-app/lovii-core/lovii-admin` подсказывает, что prod = master через `gh workflow run … --ref master` вручную. По `STATUS.md` 2026-09-13 («Прод-деплой — ручной, не выполнялся»). На staging — многое, на prod — ничего.
2. **Фискализация (54-ФЗ) описана в офертах — не реализована в коде.** F-034 (закрыт как Documented, инженерный пробел в BACKLOG §6 трек I-1). Чек покупателю выдать нельзя — есть только нефискальная квитанция.
3. **Подсистемы выплат в коде отсутствуют.** F-049: `LedgerEntryType::Payout` объявлен, ноль использований; эндпоинтов вывода нет. В пилотной реальности — накопление денег у платформы без вывода точкам.

Параллельно — реестр банковских операций уже идёт (CSV на email по договору №МР-08.26/АКС_01, Прил. №2), но API-доступ к р/с АКСИОМА не запрошен. Сверка пока ручная.

**Что готово и зрелищно выглядит (для «пилот M1 без реальных денег»):**
- Витрина, каталог, корзина, чекаут, заказы, баллы и кэшбэк — работают в staging (as-is/01–04, 07).
- OTP через MAX + Telegram работают (стабильно), VK в работе.
- Финансовый контур (сплит-пул 40/40/20, чарджбэк-зеркало SZ-045) — канонизирован и работает на staging для **уже начисленных** средств.
- Дизайн-система и Прод-качество бара — жёсткие, живые.
- Банковский контракт закрыт, тарифы канонизированы (PARAMS v1.6).

**Что нужно решить перед «зелёным светом»:** см. §3 (🔴 блокеры) и §4 (🟡 условные риски).

---

## 1. Объём релиза (Scope)

### 1.1. In scope для v1.0 (Milestone M1 по ROADMAP.md, цель Q4 2026)

- **Клиент покупателя** (app `lovii-app`): витрина, каталог, корзина, чекаут, заказы, профиль, баллы и кэшбэк, QR-лояльность, push-уведомления.
- **Вход/авторизация:** OTP через MAX и Telegram (VK в работе).
- **Доставка/адреса/гео:** Яндекс-карты в клиенте (staging-ключ есть, для прода — нет), выбор адреса.
- **Партнёрский кабинет (b2b):** команды, роли (представитель/амбассадор/МСП), приглашение, онбординг.
- **Супер-админка:** починена и смержена в master (STATUS.md 2026-09-13, PR #1), но **в прод не выкачена**.
- **Финансовое ядро в staging:** платёжный шлюз Т-Банка (Init, вебхук, сплит), чарджбэк-зеркало (SZ-045), чекаут → баллы (loyalty engine).
- **Документы-оферты:** публичная оферта LOVII, оферта присоединения — опубликованы.
- **Политика обработки ПД** — v1.0.0 опубликована, F-018 закрыт.

### 1.2. Out of scope (явно не в релизе)

- **Реальная фискализация (54-ФЗ, ККТ, ОФД)** — F-034, BACKLOG §6 трек I-1.
- **Авто-выплаты партнёрам на р/с** — F-049, BACKLOG §1 «три пустых слоя».
- **WhatsApp OTP** — скрыт из списка каналов; Meta-интеграция отложена.
- **SMS-OTP для прода** — решения #6 в `decisions-needed.md`: канал не требуется.
- **Kuper-импорт и внешние интеграции витрины** — `INTEGRATIONS_ENABLED=false`, ассортимент сейчас только МСП (as-is/01 § 12).
- **Оплата товаров в UI для покупателя** (`POST /orders/{id}/payment`) существует в API, но **прод-сценарий не верифицирован** (см. §2.5).
- **Мини-аппы (MAX/Telegram/VK)** — отложено (BACKLOG §6, MINИ_APPS_RESEARCH).
- **Бренд-сайт lovii.ru (внешний)** — staging работает; прод — отдельная задача.
- **Франшиза (M2)** — Q1 2027 по ROADMAP, не M1.
- **Рекламная сеть и партнёрская программа** (M4–M5).
- **Чартджбэки и комиссии §5.5** — частично: правила 1, 2, 5, 6, 7 в каноне; правила 3, 4, 8 — в процессе.

---

## 2. Категории готовности (7 осей)

### 2.1. Функциональная полнота (FEATURES.md ↔ as-is/)

**Вердикт: 🟡 условно — 26 из 57 фич в каталоге**

- **Готово в staging:** витрина + каталог + поиск (as-is/01), вход по OTP (as-is/06), корзина и чекаут (as-is/02), заказы с жизненным циклом (as-is/03), баллы/кошелёк/платежи (as-is/04), роли и кабинеты (as-is/05), пуш-уведомления (as-is/07), базовая география (as-is/08), b2b-кабинет (as-is/09), admin (as-is/10).
- **Открыто из FEATURES.md:** 31 фича в статусе `planned`, в т.ч.:
  - **Касса и фискализация** (54-ФЗ, ККТ, ОФД) — инженерно отсутствует (F-034).
  - **Системная безопасность (auth, KYC/AML, 152-ФЗ, PCI DSS)** — только упоминания; KYC для вывода средств не описана в рабочих доках.
  - **Курьерская роль** — упомянута в README, в кодовой части — пустое место.
  - **Гео-изоляция и white-label по городам (City Franchise)** — принцип в VISION, инженерно отложено до M2 (Q1 2027).
  - **Конвертер баллов и офлайн-оплата** — описаны, но не реализованы.
- **Прямая сверка с as-is выявила** (as-is/05 §6 «Кандидаты в „не хватает“»):
  - Idempotency-Key клиент не шлёт → риск двойного списания при повторе.
  - Min-order в коде не энфорсится.
  - Зоны доставки не проверяются гео-точкой.
  - Списание баллов бэкенд умеет — UI не передаёт (as-is/02 §2).
  - Форма заявки app не передаёт промокод (2 формата ссылок `?ref=` vs `?promo=`).
  - Пуши на `accepted`/`ready` не отправляются.
  - Чеков нет (фискализация).

**Что подтверждает ось:** продукт собран вокруг MVP-сценария «купить в одной точке за баллы». Сложные сценарии (мульти-город, реклама, реферальные статусы) — в коде частично, в каноне — частично, целостной картинки «всё работает как в BRD» нет.

### 2.2. UX/UI качество (PRODUCT_QUALITY_BAR.md)

**Вердикт: 🟢 хорошо (на стенде), 🟡 с оговорками на прод-этапе**

- Канон PRODUCT_QUALITY_BAR.md v0 — действует (обязателен для каждого агента).
- Дизайн-система: токены `lovii-design` живые, стражи-тесты (baseline) 🟢 (STATUS.md 2026-09-17 SZ-115).
- Адаптив mobile-first (390+) и web (1024) проверен, скрин-тесты делаются.
- a11y: контрасты, тап-таргеты, фокус — на стенде соблюдены (e2e сценарии 🟢).
- Честные цифры: «кэшбэк 5%» правило есть, иначе earn = 0 (as-is/12 § «Перед продом: убедиться, что `loyalty_rules` содержит активное правило»).
- Сторож: F-041 (контейнер не паддит) и F-042 (мёртвый интерактив запрещён) — встроены в приёмочные гейты.
- **Сомнения для прода:** премиальный банк-уровень скрин-теста (PRODUCT_QUALITY_BAR §0) делается в lovii-demo, но сравнение с реальным Тинькофф/Revolut не стандартизовано.

### 2.3. Производительность и устойчивость

**Вердикт: 🟡 нужно дозамерить**

- Lazy-чанки + prefetch на requestIdleCallback (SZ-008 §6.3) — есть, приоритет «витрина → корзина → профиль».
- RUM-точки `window.__loviiPerf` (SZ-008 §6.4) — есть в core, финальный замер на телефоне не сделан.
- Устойчивость: Horizon активен, очереди в БД — 0 строк, Redis есть. Банковский вебхук → индемпотентность подтверждена (F-031, post-fix).
- **Открыто:**
  - Не проводились нагрузочные тесты под реальный M1-поток (50 точек × 144 000 ₽ GMV, ~144 тыс. ₽/мес на точку → выплаты).
  - Кеш `index.html` 60 сек (F-005) — намеренный «жёсткий» кеш, «не вижу изменений после деплоя» (принято, Open P3).
  - PWA-обновление и SW-кэш не должны прятать новые API — задокументировано в PRODUCT_QUALITY_BAR как грабли.

### 2.4. Безопасность и комплаенс

**Вердикт: 🔴 блокер для публичного prod, 🟡 для закрытого pilot**

- **Политика обработки ПД — опубликована (F-018 Fixed).** Шит авторизации ссылается на неё корректно (F-021 Fixed).
- **152-ФЗ:** публичный документ есть, ссылка в шите авторизации работает (приёмка 2026-09-09).
- **OTP-каналы:** MAX + Telegram работают; VK в работе; SMS/WhatsApp/Call заглушки **скрыты** из списка покупателя (F-037 Fixed). Email-канал добавлен на Яндекс SMTP (E-1/E-2 закрыты).
- **OTP-bypass `OTP_DEV_BYPASS=true`** — default в config/otp.php → as-is/06 зафиксировал как прод-риск. **Должно быть `false` на prod** (BACKLOG §6 «Прод-деплой core»).
- **2FA (S-1) и Passkeys (S-2)** — не сделаны (BACKLOG §5). Отмечены «P1, к деньгам». Без них — нет второго фактора для вывода средств.
- **KYC/AML для вывода** — только упоминание в каноне, инженерного описания нет (FEATURES §1 done=0, planned=1).
- **Каналы OTP и Telegram/MAX:** в РФ-хостинге вебхуки блокированы, исходящие через прокси — long-poll воркеры (F-030). Для prod надо альтернативный канал.
- **Ключи, секреты, ротация:** есть `canon/SECRETS_ROTATION.md`, чек-лист выполнения — за владельцем (S-3, владелец до прода).
- **Auth на API:** Sanctum-токены (`personal_access_tokens`), abilities `['*']`, TTL null — широко. Уровень доступа по правам средний.
- **Гигиена:** в `.zcode/config.json` (станция агента) был DSN с read-only паролем — обнаружен F-047, ротация «за владельцем».
- **CVEs / deps:** не проведено в этой оценке.

### 2.5. Финансовый контур (FINANCIAL_CONTOUR.md ↔ FINANCIAL_MODEL.md ↔ as-is/12)

**Вердикт: 🟡 в staging работает; 🔴 для прод-выпуска с реальными деньгами партнёров**

- **Что в коде и работает (as-is/12, SZ-045):**
  - Два независимых контура: баллы (`wallets`) + рубли (`accounts`); инвариант нулевого баланса выполняется (SQL-проверки в as-is/12 §4).
  - Платёж T-Bank Init + двухстадийная схема + вебхук с повторной проверкой `GetState`.
  - Сплит-пул 40/40/20 — `DistributeOrderPoolAction`; канал оплаты берётся из `payments.provider_payload.method`, иначе «card».
  - Возвраты и чарджбэки: возврат кэшбэка + зеркальный разворот пула (`ReverseOrderPoolAction`) — атомарно.
  - Отмена заказа → возврат списанных баллов (`refundSpendForOrder`) — идемпотентно по `wallet+order+adjustment`.
- **Что НЕ работает в коде (as-is/12 § «Чего нет»):**
  - `orders.method` не хранится → для СБП/T-Pay точность пула теряется, считается как «карта».
  - Карта LOVII PAY как витрина счетов — нет сущности; `cards` — внутренние id; номер рисуется клиентом (FNV-хэш account_id, BIN 9643).
  - Подписка представителя 599/199 — нет реализации (только комментарии «фаза 3»); гейт доли выключен (`PAYMENTS_SPLIT_REP_SUBSCRIPTION_GATE=false`).
  - Доход представителя/амбассадора — API и экраны возвращают 0.
  - **Вывод средств (payout)** — enum `LedgerEntryType::Payout` объявлен, ноль использований (F-049). Это **самый критичный пробел**.
  - Номинальный счёт в банке — только зеркальный учёт `platform_nominal` в коде, **банковского номинального счёта ещё нет**.
  - ККТ, агентская схема — нет; квитанция «не фискальный документ».
  - Авто-матчинг верификационного платежа — нет, `verified_at` ставится руками.
- **Что согласовано владельцем, но требует операционных хвостов:**
  - API-доступ к р/с АКСИОМА (запрос в банк — нужен для сверки в §5.4 канона).
  - Формулировка в Оферту присоединения: «верификация», самозанятые — декларация.
  - Экран ручной очереди верификаций — только в admin (раздел «Партнёры» пока не существует).
- **Что инвариантно:** на staging 21 заказ с подтверждённым платежом, но без проводок в `ledger_entries` (F-048 Wontfix). Это **дырка в трассируемости**, для prod-релиза непозволительна.

**Сводно по оси:** в staging денежный поток технически «живой», но **между «деньги пришли на р/с платформы» и «деньги ушли партнёру»** стоят три пустых слоя (BACKLOG §1). Для первой публичной выплаты реальных денег — не готов.

### 2.6. Данные и миграции

**Вердикт: 🟡 в staging устойчиво, 🟢 по инвариантам**

- Схемы: `public` (ядро, 55 таблиц), `lovii_b2b` (Filament, 14), `lovii_admin` (Filament+Shield, 15) — по `db-schema-analysis.md` (2026-09-13).
- Миграций применено: public — 92, b2b — 13, admin — 29.
- Деньги: integer (копейки), bigint для балансов; float нигде не участвует; округление `ROUND_HALF_UP` в сплите (as-is/12).
- Уникальные индексы:
  - `ledger_entries(source_type, source_id, type, account_id, split_role)` — идемпотентность.
  - `wallet_transactions(wallet_id, order_id, type)` — добавлен в SZ-045.
- **Инвариант (as-is/12 §4):**
  ```sql
  -- accounts.balance = Σ ledger_entries (должно вернуть 0 строк)
  -- wallets.balance = Σearn − Σspend + Σrefund/adjustment (0 строк)
  ```
- **Что нужно для prod:**
  - Бэкап-стратегия и точка восстановления (не документирована публично).
  - Playbook миграций под прод (SZ-038 уже применил кросс-схемные ALTER, F-040 задокументирована — синхронный деплой core+b2b).
  - Прогон миграций под малыми таблицами (выполнялось только в dev/staging).
  - `loyalty_rules` — на staging одно правило «Кэшбэк 5%»; без активного правила кэшбэк = 0 (as-is/12).

### 2.7. Операционная готовность

**Вердикт: 🔴 prod-деплой не отработан; 🟡 dev/staging ОК**

- **CI/CD:** staging автодеплой из ветки `staging` во всех репо (lovii-app, lovii-core, lovii-b2b, lovii-admin). Прод = master через `gh workflow run ci.yml … --ref master` руками. **В прод не выкатывалось.**
- **Runbooks:** `auth-staging.md`, `superz-schedule-setup.md`, `manual-msp-cycle.md` — есть в workspace. Общеprodового runbook нет.
- **Мониторинг/алерты:** RUM-точки `window.__loviiPerf` есть, но не в проде; бот-поллеры для админ-алертов развёрнуты, но не end-to-end протестированы.
- **Секреты и ротация:** `SECRETS_ROTATION.md` есть, чек-лист — за владельцем.
- **Бэкапы:** описание отсутствует.
- **Восстановление после сбоя:** не описано.
- **Инфраструктура:** SW-кэш (PWA) не должен прятать новые API — есть в PRODUCT_QUALITY_BAR как грабли.
- **Доступность GitHub:** «новый профиль» lovii-ru создан из-за приостановки `bestdeejay-design` 2026-09-13 (F-051). Публичное зеркало `axiiom-ru/lovii` работает; `axiiom-ru/lovii` **не пушим напрямую** — через `scripts/sync-public.sh`.

---

## 3. 🔴 Открытые блокеры

> Реестр из `decisions-needed.md` и `BACKLOG.md` §1, что нельзя пропустить для запуска v1.0.

### 3.1. Прод-деплой не выполнен ни разу (OPERATIONAL)
- Источник: STATUS.md 2026-09-13, AGENTS.md «поток … master (прод **только вручную**)».
- Что нужно: первая попытка `gh workflow run ci.yml … --ref master` для каждого репо (4 команды) + наблюдение.

### 3.2. Фискализация 54-ФЗ (TECHNICAL)
- Источник: F-034, FEATURES §9 (planned), BACKLOG §6 трек I-1.
- Что нужно: решение по провайдеру ОФД, инженерная спека, миграция `accounting`-слоя.

### 3.3. Авто-выплаты партнёрам (TECHNICAL + FINANCIAL)
- Источник: BACKLOG §1 (три пункта), F-049.
- Что нужно: спроектировать счёт МСП + реестр ежедневных выплат + эндпоинт вывода + правило «минус блокирует исходящие». **Без этого — деньги копятся у платформы, а партнёры их не получают.**

### 3.4. OTP_DEV_BYPASS в проде (OPERATIONAL/SECURITY)
- Источник: as-is/06 § «OTP_DEV_BYPASS=true default», BACKLOG §6 «Прод-деплой core».
- Что нужно: `OTP_DEV_BYPASS=false`, фиксация webhook/long-poll на проде.

### 3.5. KYC/AML для вывода средств (COMPLIANCE)
- Источник: FEATURES §1 «planned», PRD §Area 9 FR-9.5.
- Что нужно: инженерная спека (выбор провайдера или ручной режим), как минимум — точка входа до вывода.

### 3.6. Банковский API к р/с АКСИОМА (OPERATIONAL + TECHNICAL)
- Источник: FINANCIAL_CONTOUR.md §2.2.
- Что нужно: запросить у банка (письменно), запустить. Без этого — ручная сверка через интернет-банк.

### 3.7. Верификационный платёж — авто-матчинг (TECHNICAL)
- Источник: as-is/12 § 7 «Чего нет», as-is/05 §6.
- Что нужно: парсер входящих платежей по regex `VER-<partner_id>-<suffix>` + сверка плательщика.

### 3.8. Оферта присоединения — формулировка верификации (LEGAL)
- Источник: FINANCIAL_CONTOUR.md §3.2 (✅ «утверждено владельцем»).
- Что нужно: через юриста внести «верификация» + «самозанятые — декларация».

### 3.9. Экран ручной очереди верификаций в admin (TECHNICAL)
- Источник: FINANCIAL_CONTOUR.md §6, FINDINGS §6 «admin-раздела „Партнёры“ не существует».
- Что нужно: проектирование и реализация этого экрана в `lovii-admin`.

### 3.10. PROD-стейдж для критичных PR (TECHNICAL + GOVERNANCE)
- Источник: SZ-045 история (потеря коммита F-045, рассинхрон веток F-040).
- Что нужно: протокол prod-выката с dry-run, чек-листом и фолбэком.

---

## 4. 🟡 Условные риски (известные, допустимые для pilot M1)

| ID | Риск | Источник | Что нужно для приёмки |
|:---|:---|:---|:---|
| C-1 | PROD-секреты и .env.example неполный: `INTERNAL_ADMIN_SECRET` отсутствует | as-is/12 «Чего нет» | Добавить в `.env.example`, проверить в prod |
| C-2 | `orders.method` не хранится → СБП/T-Pay путаются | as-is/12 | Миграция + сохранение в `orders.method` |
| C-3 | SMS-OTP, WhatsApp OTP скрыты — но если потребуется — нет готового канала | decisions-needed #4, F-035, F-037 | Документировать «не входит в v1.0» в roadmap |
| C-4 | VK OTP в работе (T2.x, серия T-002/T-003/T-004) | BACKLOG §5 | Решение о включении/исключении до v1.0 |
| C-5 | Карта LOVII PAY — отображение через FNV-хэш (визуально похоже, но в БД нет сущности) | as-is/12, F-054 | Миграция `cards` уже сделана; UI биндинг «баллы» — отчёт |
| C-6 | 21 заказ без проводок в `ledger_entries` (F-048 Wontfix) | FINDINGS | Бэкфилл не требуется (тестовые данные), но для prod — политика «не держать такие» |
| C-7 | Брендовые логотипы на static.kuper.ru (F-026) 404 | FINDINGS F-026 | Не критично для v1.0 (Kuper выключен), но вернуть в backlog |
| C-8 | UI карта LOVII PAY (баллы/рубли) — код не применён | artifacts/financial-contour-s5-staging-report.md | Применить патчи (3 файла app) |
| C-9 | Passkeys/2FA — отложено | BACKLOG §5 S-1/S-2 | Документировать в Privacy/Consent |
| C-10 | §6 аудита app «мелочи» (title по роутам, плейсхолдер поиска, тосты) — ждёт «approve all» | BACKLOG §6 | Решение владельца |
| C-11 | Почтовый провайдер для прода (Mail.ru/yandex/Т-Банк) | BACKLOG §6 | Решение владельца |
| C-12 | Мини-аппы (MAX/Telegram/VK) | BACKLOG §6 (MINI_APPS_RESEARCH §7) | Вне v1.0 — явно |
| C-13 | Тестовая БД содержит «бардак кабинета МСП» (ИНН-дубли) | as-is/13 (13-sushchnosti-b2b.md) | Cleanup (T-008 → zcode) |

---

## 5. ✅ Решённые владельцем / закрытые (что подтверждает готовность по другим осям)

- **Все 6 решений по верификации партнёров** (FINANCIAL_CONTOUR v0.5, 2026-09-13): сумма 1 ₽ возвратный, спец-счёт АКСИОМА, формулировка «верификация», самозанятые — декларация, счёт 14 дней / 2 напоминания, экран в admin.
- **OTP-каналы:** MAX + Telegram (live), скрытие заглушек (F-037), Magic link (P-1).
- **Политика обработки ПД** v1.0.0 — опубликована, ссылки в шите/профиле (F-018, F-021).
- **E-mail-канал** (E-1/E-2): SMTP Яндекс подтверждён.
- **Банковский контракт:** тарифы канонизированы (PARAMS v1.6 — НДС 22%, T-Pay без НДС).
- **Реестр операций Т-Банка** ежедневно CSV на email (Прил. №2 к договору) — готовый машинный источник сверки для мультирасчётного контура.
- **Логика чарджбэка-зеркала** (SZ-045): канонизирована, протестирована на реальных данных заказа №149, в staging, closed через PR.
- **Карточка F-045** (потеря коммита при `reset`) — извлечена, есть recovery-теги.
- **ГИГИЕНА СТАНЦИИ** F-047 — 165 чужих веток и битый файл удалены.
- **F-048** (21 заказ без проводок) — решён как Wontfix, не блокер.

---

## 6. Решение

- [ ] **🟢 Готово к продакшен-релизу v1.0** — *дата, канареечный %, владелец подтвердил*
- [ ] **🟡 Условный релиз (pilot M1)** — при условиях **(A) — (F)**:
  - **(A)** PROD-деплой выполнен сухим прогоном (см. §3.1)
  - **(B)** OTP_DEV_BYPASS=false подтверждено (см. §3.4)
  - **(C)** Экран ручной очереди верификаций в admin — выкатить до первого подключения партнёра (см. §3.9)
  - **(D)** Бэкфилл или миграция для 21 заказа без `ledger_entries` принята (см. §4 C-6)
  - **(E)** UI карты LOVII PAY (баллы/рубли) — патч применён (см. §4 C-8)
  - **(F)** Оферта присоединения с новой формулировкой — опубликована (см. §3.8)
- [ ] **🔴 Не готово** — блокеры выше не закрыты

**Дата принятия решения:** *______________*
**Подпись владельца:** *______________*

---

## Приложение A. Ссылки на as-is/*

13 модулей опубликованы в `lovii-ru/lovii-docs` 17.09.2026 (срез кода):

1. `01-vitrina-katalog.md` — Витрина и каталог
2. `02-korzina-checkout.md` — Корзина и чекаут
3. `03-zakazy.md` — Заказы: жизненный цикл
4. `04-bally-koshelek-platezhi.md` — Баллы, кошелёк, счета, платежи
5. `05-rol-i-kabinety.md` — Роли и кабинеты (реп / амб / МСП)
6. `06-vhod-otp.md` — Вход и OTP (каналы доставки)
7. `07-pushi-uvedomleniya.md` — Пуши и уведомления
8. `08-dostavka-adresa-geo.md` — Доставка, адреса, география
9. `09-kabinet-b2b.md` — Кабинет b2b (Filament)
10. `10-admin.md` — Админка lovii-admin
11. `11-baza-dannyh.md` — База данных
12. `12-finkontur-v-kode.md` — Финансовый контур в коде
13. `13-sushchnosti-b2b.md` — Сущности b2b: юрлицо/магазин/точка

## Приложение B. Ссылки на canon/*

| Док | Назначение | Срез |
|---|---|---|
| `STATUS.md` | Текущий срез задач (SZ-*) и оперативные индексы | 2026-09-14 |
| `PARAMS.md` v1.6 | Цифры — единый источник | 2026-08-29 |
| `FINANCIAL_CONTOUR.md` v0.5 | Финконтур: спец-счёт, верификация, нулевой баланс | утв. 2026-09-13 |
| `FINANCIAL_MODEL.md` v2.4 | Количественная модель, юнит-экономика | 2026-08 |
| `BRD.md`, `PRD.md` | Требования и user stories | M1 → Q4 2026 |
| `FEATURES.md` | Каталог фич (26 done / 31 planned) | 2026-09 |
| `FINDINGS.md` | 54 находки (F-001…F-054) | 2026-09-15 |
| `BACKLOG.md` | Очередь + платформенные эпики P1–P3 | 2026-09 |
| `DATA_MODEL.md` v1.1 | Концептуальная модель + ER | 2026-08-25 |
| `ARCHITECTURE.md` | Архитектура **демо** (lovii_demo) — отстаёт от прода (BACKLOG §2) | draft |
| `ROADMAP.md` | 7 майлстоунов M1–M7 | 2026-09-09 |
| `VISION.md` | Видение SaaS + агентская модель | 2026-09 |
| `PRODUCT_QUALITY_BAR.md` | Стандарт качества (обязателен) | 2026-09-12 |
| `SECRETS_ROTATION.md` | Ротация секретов | S-3 closed |

## Приложение C. История решений (decisions-needed.md)

См. `/Users/best/LOVII/lovii-docs/decisions-needed.md` — 24 пункта:
- 🔴 F-018 (закрыт), MAX/Telegram токены (live), SMS для прода (closed), VK OTP (in work), WhatsApp (deferred), хедер («живой» vs V11), QA-жесты (closed), прод-деплой (manual pending), F-024 КБЖУ (deferred), 2FA/Passkeys.
- 💰 Сумма верификации 1₽ (closed), формулировка Оферты (pending), самозанятые (closed — без проверки), срок жизни счёта 14 дней (closed), экран очереди в admin (closed), «ЛОВИ Бизнес» 30/2990 (closed).

## Приложение D. Деплой-маршрут

1. **staging автодеплой** из ветки `staging` в каждом репо (`lovii-tech/{app,core,b2b,admin}`).
2. **master → прод** вручную: `gh workflow run ci.yml --repo lovii-tech/<repo> --ref master`.
3. **Публикация документации:** новый путь `axiiom-ru/lovii` через `scripts/sync-public.sh` — НЕ пушим напрямую. Зеркало для документации сейчас `lovii-ru/lovii-docs` (новый профиль после F-051).
4. **Канареечный %** не определён — нужна политика ramp-up (первые 1%, 5%, 25%, 100%).
