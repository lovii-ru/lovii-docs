Ты — эксперт по CSS и дизайн-системам. Ранее ты дал рекомендации по нашему CSS (сократить, ввести базовые компоненты .card/.btn/.chip/.field/.list-row, @layer, оставить уникальное и адаптив в продуктовом слое).

Мы сделали часть: (1) удалили мёртвый CSS; (2) ввели компонент .card и перевели 13 типов карточек (86 мест); (3) свели 206 сырых теней/радиусов в токены; (4) объединили 157 правил-дублей в группированные селекторы — без правки разметки; (5) уточнили токены. Визуальный результат не изменился (сверка 16 экранов × 2 темы = 0 отличий). Итог: 3915 → 3437 строк, 1755 → 1546 правил.

Во вложении обновлённый lovii.css и краткий RESULTS.md.

Ответь по пунктам:
1) 3437 строк / 1546 правил для такой витрины (десятки экранов, статика без сборки) — нормально или много? Где реалистичный пол и за счёт чего?
2) Стоит ли дальше компонентизировать разметку под .btn/.chip/.field/.list-row — или польза только в поддержке, а не в строках?
3) Что делать следующим приоритетно?
4) @layer: вводить ли, и как не сломать каскад (правила вне слоя бьют слои)?
Отвечай конкретно, с примерами. Язык — русский.
