# SZ-062 · Фаза 3 — Оптимизация CSS (ART-3, транш 1)

**Задача:** SZ-062 · **Фаза:** 3 — оптимизация CSS
**Загружен:** 2026-09-20 · **Проверено на:** lovii-demo `master` a8a86be + локальные правки (клон 2026-09-20)
**Статус:** 🟧 — транш 1 применён и проверен; слияние дублей, слои и размеры иконок — следующий транш

> Гейт №1 пройден: все 6 решений утверждены владельцем (иконки — не удалять, а расширять).
> Архитектура: правки — в `lovii-demo`; канон — в `lovii-design`; каталог ДС — в `lovii-demo/design/ds/`.

## Применено (транш 1)

| # | Изменение | Объём | Визуальный эффект |
|---|---|---|---|
| 1 | Navbar-фикс: `--lv-navbar-height: 64px` в `:root` (offset брал fallback 56px при таббаре 64px) | 1 объявление + 4 задействованных `var(...,56px)` | +8px нижнего отступа на экранах с таббаром (утверждено, п.6) |
| 2 | Удаление мёртвого CSS: 38 классов → **59 правил** (в т.ч. составные/групповые с мёртвым классом) | −119 строк в `css/lovii.css` | нет (классы не встречались в коде и DOM) |
| 3 | Токенизация theme-стабильных цветов: `--lv-pink`, `--lv-pink-dark`, `--lv-tiffany`, `--lv-gold`, `--lv-badge-hit/new/sale` | 35 вхождений hex → `var(--lv-*)` | нет (значения идентичны в обеих темах) |
| 4 | Бамп кэша `css/lovii.css?v=53 → ?v=54` | 1 строка `index.html` | нет |

**Итог по файлу:** 3915 → 3796 строк; уникальных hex в `css/lovii.css`: 52 → 42.

## Верификация транша 1

| Проверка | Результат |
|---|---|
| `node --check` (18 js + sw.js) | 0 ошибок |
| `lv_collisions.py` | 0 фатальных (10 перекрытий function — без изменений) |
| Рантайм-обход роутов (390/768/1280 × светлая/тёмная), локальный стенд | 0 ошибок консоли, 0 переполнений, 0 видимых SVG 0×0 |
| Скриншот-регрессия «до/после» (home/profile/wallet-dark/msp) | 3 из 4 кадров — 0 отличий; msp 1280 — только зона navbar (низ экрана), ожидаемо по п.6 |

Лог рантайма: `_audit/runtime-after.txt`. Кадры «до/после»: `deliverables/sz062-after/assets/`.

## Важное наблюдение (для методики)

Наивная токенизация **по значению** ломает тёмную тему: токены `--lv-page`, `--lv-ink`, `--lv-dim`, `--lv-gold-text` инвертируются под `[data-theme="dark"]`, а исходные hex были фиксированными. Первый прогон дал регрессию кошелька в тёмной теме (10.5% пикселей). **Правило:** токенизировать hex можно только в **theme-стабильный** токен (его значение не меняется в dark-оверрайдах) — иначе это правка визуала и она идёт через таблицу правок/Гейт №2.

## Остаётся по фазе 3 (транш 2)

- Слияние 103 повторных селекторов и 9 дублей `@media (max-width:640px)`/`(min-width:900px)`/`(min-width:1024px)`.
- Декомпозиция `css/lovii.css` по слоям `tokens → base → primitives → components → utilities`.
- Явный размер у 195 вызовов иконок + шкала размеров (после Гейта №2).

## Обновление — полный рантайм-прогон (2026-09-20)

Полный обход (22 роута × 390/768/1280 × светлая/тёмная, локальный стенд \`:8090\`):
**0 ошибок консоли, 0 горизонтальных переполнений, 0 видимых SVG 0×0.** Лог: \`_audit/runtime-after.txt\`.

Замер фактических размеров иконок (390, ключевые роуты): встречаются **10, 12, 13, 14, 16, 17, 18, 20, 22, 24, 32, 70 px** — шкалы нет, размеры «расползлись». Это подтверждает необходимость введения шкалы (Гейт №2), причём базовые текущие — 16 и 18, а не только 16.
