# SZ-062 · Фаза 1 — Полный аудит CSS (ART-1)

**Задача:** SZ-062 · **Фаза:** 1 — аудит CSS
**Загружен:** 2026-09-20 · **Проверено на:** lovii-demo `master` a8a86be (клон 2026-09-20)
**Статус:** 🟧 — измерения сделаны; списки на утверждение (ГЕЙТ №1)

## 1. Мёртвые классы

Метод: класс определён в CSS, но отсутствует и в строках HTML/JS (\`index.html\`, \`js/*.js\`, \`design/*.html\`), и в рантайме (обход 29 роутов, 549 классов собрано из DOM). Динамически конструируемые (\`prefix_${var}\`) вынесены отдельно.

### 1a. Крепко мёртвые — 38 (нет ни в коде, ни в рантайме)

\`\`\`
app-badge_gold        auth-channel-ico_max   auth-channel-ico_telegram  auth-channel-ico_vkontakte
cabinets__tag_todo    cabinets__tag_work     count-badge                crumb
filters-indicator     flow-steps             home-popular__types        install-card-btn
msp-times             pass__cta              pass__error                pay-skin-note
prof-chips            prof-head              pv                         quick
quick-row             roles-hbars            roles-hbars__label         roles-hbars__row
roles-hbars__track    roles-hbars__val       set-group-gap              shadow-lift
shadow-soft           stores__types          theme-ico-moon             theme-ico-sun
tier-levels           tl                     tone-ink                   tone-sand
wallet__period        warn
\`\`\`

⚠️ Особые случаи для ручного разбора перед удалением:
- \`app-badge_gold\`, \`tone-ink\`, \`tone-sand\`, \`theme-ico-*\` — выглядят как результаты \`prefix+\${var}\`; в текущем коде их значения не встречаются, но проверить при ГЕЙТЕ (возможно, значения данных сменятся).
- \`shadow-soft\`, \`shadow-lift\` — утилиты-тени; сейчас заменены токенами \`--lv-shadow-*\`.

### 1b. Динамические (живые, НЕ удалять) — 6

\`priv__card_gold/pink/tiffany\` (\`priv__card_\${tone}\`), \`skin-nominal\` (\`skin-\${skin}\`), \`tier__level_cur/done\` (\`tier__level_\${state}\`) — присутствуют в рантайме, отсутствуют как литералы в коде. Удаление запрещено.

> Примечание: 384 CSS-класса не попали в базовый обход (глубокие состояния — корзина с товарами, карточка заказа, чат, под-табы МСП), но присутствуют в коде → **живые**, не кандидаты.

## 2. Дубли и коллизии

- **Повторные определения селекторов:** уточнено 2026-09-20 — реальных дублей мало: **2** совпадения в одном контексте; **20** селекторов встречаются дважды, но в разных медиа-контекстах (легитимные адаптивные оверрайды). Ранняя цифра «103» была завышена — в неё попали заголовки `@media` и шаги `@keyframes` (исправлено).
- **Массовые повторы:** блоки-«патчи» в конце файла переопределяют каркас — \`.app\` (30/640), \`.app-header-in\` (37/3784), \`.hero\` (68/664), \`.app-nav\` (384/652), \`.prod-grid\` (179/643/660), \`.seg\` (203/646/1183), \`.pill-btn .lbl\` (47/51/3786/3792).
- **Дубли медиа-запросов:** \`@media (min-width: 640px)\` — **9 раз**; \`@media (min-width: 900px)\` — 3; \`@media (min-width: 1024px)\` — 3; \`@media (width < 540px)\` — 3.
- **Коллизии между JS-файлами** (\`lv_collisions.py\`): фатальных (let/const/class) — **0**; перекрытия \`function\` — 10 (cabinets.js ↔ dash.js: \`renderRepDash\`, \`renderAmbDash\`, \`renderMspCabinet\`, \`dashHeadHtml\`, \`repInviteCode\` и др. — известная связка, тихая перезапись, не падение).

## 3. Статика

| Проверка | Результат |
|---|---|
| \`node --check\` (18 js + sw.js) | 0 ошибок |
| eslint (\`lv_eslint.mjs\`) | 0 ошибок, **59 предупреждений** (в осн. \`no-unused-vars\`) |
| \`lv_collisions.py\` | 0 фатальных, 10 перекрытий |
| HTML-аудит \`index.html\` | 0 дублей id, 0 несбалансированных тегов; \`data-action\`=1, \`data-go\`=7 |

## 4. Хардкод-инвентарь

- **52 уникальных hex** вне токен-слоя (в \`lovii.css\`), включая 3-значные сокращения \`#fff\` (25×), \`#000\` (8×), \`#999\` (5×), \`#bbb\`, \`#888\`.
- Самые частые: \`#fff\` (25), \`#f64a8a\` (11), \`#c92a6a\` (9), \`#ffffff\` (9), \`#000\` (8), \`#1a1a1a\` (7), \`#f3d9a4\` (7).
- **Повторяющиеся размеры** (кандидаты на токенизацию): 16px (386×), 12px (338×), 14px (203×), 8px (179×), 10px (163×), 13px (82×); радиус-pill 999px (106×).

## 5. Специфичность / каскад

- \`!important\`: **3**, все в блоке \`prefers-reduced-motion\` (строки 3346–3348) — оправданы, не трогать.
- Дубли селекторов и поздние блоки-патчи (§2) — основной источник конфликтов каскада; сводятся в фазе 3.

## 6. Требует ГЕЙТА №1

Утвердить: список удаления (38 кандидатов §1a с учётом ручного разбора), план слияния дублей/медиа, план токенизации хардкода (§4). **Правки не начинаются до ответа владельца.**
