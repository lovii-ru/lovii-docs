# SZ-074 — Интерфейсы лояльности (фронтенд): UI-набор из демо + конструктор в приложении и b2b

> Статус: **Открыта** · Приоритет: **P1** · Этап 2 программы
> [`SZ-072`](SZ-072-promo-loyalty-program.md)
> Исполнитель: **не назначен** · Репо: **`lovii-app`**, **`lovii-b2b`**
> Зависит от: контракт [`SZ-073`](SZ-073-loyalty-service-backend.md) (каркас можно
> вести параллельно — интерфейсы рисуются к контракту)
> Срез кода: app `a292b9f` — перепроверить на HEAD.

## 1. Цель

Базовый конструктор — **в приложении**, в кабинете точки (как в демо); сложные
механики — в `lovii-b2b`. Обе поверхности собираются из **одного UI-набора
лояльности**. Визуальный эталон — демо.

## 2. Шаг 0 — зафиксировать дизайн-канон из демо (обязательный первый шаг)

Качество демо — приёмка. Переносим **компоненты, поведение и визуальный язык**
(каркас в приложении другой, поэтому это не копия HTML, а порт на токены
`lovii-design`).

| Эталон демо | Что переносим |
|---|---|
| <https://lovii.mobiap.com/#/msp> → «Промо» | живое поведение всех механик |
| `lovii-demo/js/loyalty.js` | логика разделов: список, мастер акции, редакторы механик, состояние, предпросмотры |
| `lovii-demo/css/loyalty.css` | визуальные компоненты `lv-*` |
| `lovii-demo/index.html` | подключение раздела в кабинете (вкладка, `cabinet-mode`) |
| `loyalty/prototypes/assets/lv-demo/` (13–16) | кадры-эталон трёх состояний |

Поведение-эталон (сохранить дословно смысл):

- **Раздел точки:** «Акции» — карточка «Кэшбэк по умолчанию», «Группы товаров»,
  список «Акции» с типом и статусом («На витрине», «Стоп по бюджету»), «Итог по точке».
- **Мастер акции:** «Сумма чека», «Комбо», «Штампы · N-й в подарок»,
  «Счастливые часы», «Группа товаров».
- **Редакторы:** шаговый ввод «− / +» (тап-таргет ≥44px), чипы дней недели, поля
  времени `ЧЧ:ММ`, карта штампов, метр бюджета, блок «Как увидит покупатель».
- **Бюджет:** «Авто-стоп по бюджету», демо-действие «Смоделировать исчерпание».
- **Подписи покупателю:** «Кэшбэк N% · +K балл.», «Счастливые часы · +N%»,
  «Штампы 2 из 6», «Подарок … 0 ₽».

## 3. UI-набор лояльности (общий для app и b2b)

| Компонент | Назначение |
|---|---|
| `LoyaltyRuleCard` | карточка правила в списке (имя, мета, тип/статус, тумблер) |
| `MechanicPicker` | выбор механики (5 вариантов из демо) |
| `PercentStepper` | шаговый ввод процента/количества (44px) |
| `DayChips` / `TimeField` | окно «счастливых часов» |
| `StampCard` | карта штампов |
| `BudgetMeter` | бюджет и авто-стоп с прогнозом стоимости |
| `BuyerPreview` | предпросмотр «как увидит покупатель» |
| `PromoBadge` | бейдж на товаре/в корзине |

Все — на токенах `lovii-design`, без локальных цветов.

## 4. Экран «Акции» в кабинете точки (приложение)

- Новый экран `src/modules/roles-module/msp/MspLoyalty.vue` + дочерние представления.
- Маршрут рядом с `MspOverview`; вкладка в `cabinetTabs`
  (`src/router/index.ts:726–756`) с правом (`loyalty.manage` или `merchant.update` —
  по рамочному вопросу SZ-072 §8).
- Каркас — существующий `CabinetLayout` (нижний бар, `cabinetAccent`).
- Три состояния: загрузка (скелетон), пусто (продающее + CTA), ошибка (честный текст).

## 5. b2b — сложные механики

- Расширенный редактор правил и **массовые настройки по сети точек** (одна акция на
  бренд/мерчантов), пресеты и дублирование.
- Использует **тот же** UI-набор и тот же контракт `SZ-073`.

## 6. Визуальная приёмка (гейт)

Для каждого экрана — скриншоты:
- «приложение vs демо» в одинаковой ширине **390px**;
- состояния: заполнено / пусто / загрузка;
- сверка токенов с `lovii-design`.

Расхождение — либо поправить, либо обосновать в отчёте (например, каркас кабинета
в приложении отличается). Кадры приложить к отчёту.

## 7. Критерии приёмки
1. Владелец точки настраивает акции базовых механик из приложения без веб-кабинета.
2. Сложные механики доступны в b2b; источник правил один (сервис).
3. Все 5 механик из демо настраиваются и сохраняются; значения читаются обратно.
4. Визуальная приёмка (§6) пройдена; три состояния есть у каждого экрана; a11y
   (контраст AA, тап ≥44px, подписи) и адаптив 390px+/десктоп.
5. Права: сотрудник без нужного ability не видит раздел.

## 8. Гейты
`yarn test` (format → lint → unit → type-check → build) в `lovii-app`;
`docker exec lovii-b2b-app-1 composer test` в `lovii-b2b`; cross-tenant тест для
b2b-правил; e2e по теме.

## 9. Стоп-лист
Не дублировать правила в кабинете; не рисовать локальные цвета/компоненты вместо
UI-набора; не публиковать в прод-путь без прохождения визуальной приёмки.

## 10. План отчёта
Экраны и компоненты (файлы, коммиты); скриншоты «app vs демо» и трёх состояний;
список компонентов UI-набора; расхождения с демо и их обоснование.

---

## Отчёт исполнителя (срез 1–2, `lovii-app`)

**Дата:** 2026-09-23 · **Ветка:** `lovii-app` `feat/sz-074-loyalty-ui` · **Статус:** в работе
(часть SZ-074 сделана и на staging; b2b и скрин-сверка — дальше).

**Сделано (app):**
- API-клиент лояльности (`roles-api.ts`) — контракт SZ-073 §3.13;
- чистая логика (`msp/loyalty.ts`): статусы, резолвинг процента, состав группы, бюджет, итог,
  черновик/валидация/сборка контракта, подписи покупателя;
- UI-набор (§3): `PercentStepper`, `PromoBadge`, `BudgetMeter`, `DayChips`, `TimeField`,
  `StampCard`, `MechanicPicker`, `BuyerPreview` — токены `lovii-design`, тап ≥44px;
- экран «Акции» (`msp/MspLoyalty.vue`) + мастер 3 шага на 5 механик (`LoyaltyRuleWizard.vue`);
- маршрут/вкладка «Акции» (ability `merchant.update_profile`, как в `PromoAccess`), `ROUTE_TITLES`;
- тесты `msp/__tests__/loyalty.test.ts` (15) + правка `CabinetLayout.test.ts`.

**Гейт:** `yarn test` — зелёно, **905 passed**. **Деплой:** `staging` app, CI checks+deploy success.

**Не сделано / не проверено:**
- скрин-сверка «app vs демо» 390px (§6) — нужен вход МСП на стенде и браузерная съёмка;
- редактор состава группы (создание групп) — следующим срезом;
- b2b (сложные механики, массовые правила) — отдельный этап;
- категория для «штампов» пока nullable (вместе с редактором групп).

**Session-док:** `lovii-app/docs/sessions/126-sz074-loyalty-ui-app.md`.
### Дополнение (2026-09-23): редактор групп + каталог

- `lovii-app` `feat/sz-074-loyalty-groups`: редактор групп (`LoyaltyGroupEditor.vue`) — имя,
  состав (категории и товары точки), активность; метод `loyaltyCatalog`; тесты (+2); гейт
  `yarn test` 912 passed; на staging (`51548b8`).
- `lovii-core` `feat/sz-075-loyalty-catalog` (`94325030`): `GET /api/v1/loyalty/catalog` —
  каталог точки для конструктива групп (состав хранится каталожными id); тест + openapi + API_SPEC §3.13.9.
- Не сделано: скрин-сверка с демо (390px), b2b-механики.
### Подача под демо (2026-09-24)

По скрин-сверке устранены расхождения с демо «Промо»: добавлен hero-блок «МСП · точка»
с пояснением механик; у групп в списке процент и состав; «Итог по точке» — сетка 2×2;
кнопки «+ Новая группа/акция» — контурные; из блока кэшбэка убраны лишние чипы/превью.
Осталось (данные, не код): у профиля на стенде 0 групп — строки групп появятся при их создании
владельцем через UI; вкладка в нашем кабинете называется «Акции» (канон), в демо — «Промо».
### Выравнивание по демо-экранам (2026-09-24, итерация 2)

Сняты демо-экраны Playwright-ом (`lovii-app/e2e/tests/demo-promo-screens.spec.ts`) и сверены:
- выбор механик приведён к демо (5 = Сумма чека, Комбо, Штампы, Счастливые часы, **Группа товаров**);
- «Сумма чека»: быстрые суммы 500/1000/2000/3000 ₽, сегмент Подарок/Кэшбэк +N%, список
  товаров-подарков с ценами, кнопки «К списку»/«На витрину»/«Удалить»;
- редактор группы: процент группы, «Без категории», список товаров с ценами и тумблерами,
  предпросмотр, «Удалить группу»/«Готово»; процент сохраняется правилом кэшбэка с group_id;
- каталог конструктива получил цену (core `/loyalty/catalog.products.price`).
Данные стенда заполнены через API приложения (ветка 1024: 2 группы, 5 правил).
### Визуальный слой демо (2026-09-24, итерация 3)

Причина «совсем не как в демо»: экран собирался на generic-классах кабинета (`role-*`),
а в демо собственные компоненты (`lv-*`). Перенесён визуальный слой демо
(`lovii-demo/css/loyalty.css`) в приложение — `lv-*` на токенах lovii-design
(`loyalty/loyalty.scss`); переписаны экран, мастер, редактор группы, механик-пикер,
дни/время/штампы. Итог: карточки со скруглениями, строки с разделителями, пилюли-бейджи,
контурные кнопки, сетка KPI 2×2 — как в демо.
