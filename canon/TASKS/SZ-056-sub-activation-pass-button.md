# SZ-056 — Кнопка подписки LOVII PASS (активация с внутреннего счёта)

> Статус: **На приёмке** (2026-09-18 zcode; в staging смержено 19.09 — core cc4e18d,
> CI 🟢; живая приёмка владельца)
> Приоритет: **P1** (запрос владельца 18.09 вечера: «надо сделать сразу
> подписку с карты LOVII PAY»)
> Репо: `lovii-core` + `lovii-app` (вертикаль одним агентом)
> Решения владельца: подписка на старте — ТОЛЬКО с внутреннего счёта
> (Business → PAY); рекуррент и привязка карты не нужны.

## Что сделано

1. **core**: `POST /api/v1/subscription/activate` — подключение/возобновление
   списанием по каскаду Business → PAY; промо 199 ₽ по коду Амбассадора,
   иначе 599 ₽ (снимок цены — основа Price Lock P2); 409 уже активна;
   422 `insufficient_funds` без создания подписки и проводок.
   Сессия: `lovii-core/docs/sessions/042-sz056-subscription-activation.md`.
2. **core FIX**: ключ оператора — `payments.split.operator_partner_id`
   (латентный баг крона SZ-053: старый путь давал бы 500 на продлениях).
3. **app**: блок «Подписка LOVII PASS» на «Счёте и операциях» — CTA
   «Подключить»/«Возобновить», честные состояния active/grace/без средств;
   бейдж уровня и скин личной карты — от реального статуса.
   Сессия: `lovii-app/docs/sessions/120-sz056-pass-subscription-ui.md`.

## Приёмка

- [x] Гейты core (preflight: pint/rector/phpstan/pest vs базлайн) — 🟢
- [x] Гейты app (`yarn test` целиком) — 🟢
- [x] Локальный стенд: API-сценарий (422→201→409, проводки копейка в копейку)
      и оба UI-состояния в браузере (:8081)
- [ ] CI staging + автодеплой, проверка кнопки на app-staging
- [ ] Живая приёмка владельца

## Следующие шаги (вне карточки)

- Юрист: раздел «Подписка LOVII PASS» в Публичную оферту (условия §A2:
  599/199, price lock, отмена); чек 54-ФЗ — до реальных денег.
- Статус-блок в профиле (`/profile`) прокинуть на `apiGetSubscription`
  (сейчас показывает `EMPTY_TIER_STATE`).
- Отмена подписки пользователем — машина умеет (`user_cancelled`), UI/endpoint
  — отдельно (SZ-053 оставил как отдельную задачу).
